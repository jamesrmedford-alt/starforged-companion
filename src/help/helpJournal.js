export const CONTENT_VERSION = "1.7.13";

export const JOURNAL_NAME = "Starforged Companion — Help & Reference";

export const PAGES = [
  {
    name: "Quick Start",
    sort: 100,
    text: { content: "<h2>No Proxy Required</h2><p>API calls go directly from the Foundry renderer to Anthropic (for move interpretation and narration) and OpenRouter (for image generation). Both providers support browser-side calls with the user's own API key. This works identically on Foundry desktop and on The Forge — no local proxy, no setup difference.</p><h2>First-Time Setup</h2><ol><li>Open <strong>Companion Settings → About</strong> (toolbar 🛡 button, GM only)</li><li>Enter your <strong>Claude API Key</strong> (from console.anthropic.com) and click Save Keys</li><li>For art generation: enter your <strong>OpenRouter API Key</strong> (from openrouter.ai) and click Save Keys</li></ol><p>Keys are stored in your browser only (client scope) and are never visible in the standard Configure Settings UI. They are never transmitted to Foundry's server.</p><p><em>New campaign? See the <strong>First Session</strong> page for the full step-by-step order to stand up a world.</em></p><h2>Basic Workflow</h2><p>Type your narration naturally in chat. The companion intercepts it, identifies the move, and shows a confirmation dialog. Accept to roll. The narrator responds after every move.</p><blockquote><em>Example: \"I throw myself at the controls and try to stabilise the ship before we hit the debris field.\"</em></blockquote><p>To bypass the interpreter, start your message with <code>\\</code>.</p>", format: 1 },
  },
  {
    name: "First Session",
    sort: 150,
    text: { content: "<h2>First Session — Setup Walkthrough</h2><p>This is the recommended order for standing up a new campaign from an empty world. Steps 1–7 are one-time campaign setup; steps 8–11 begin play. Most are GM actions.</p><p><strong>In a hurry (or testing)?</strong> The <strong>✦ Playtest Quickstart</strong> hotbar macro does the world-building steps below (World Truths, sector, character, starship) in one click. This page is the hands-on path — useful when you want to make each choice yourself, and to understand what every step does.</p><h3>Before you start</h3><ul><li>Your world must use the <strong>Ironsworn: Starforged</strong> system with the Starforged Companion module enabled.</li><li>The companion's panels live on the <strong>floating toolbar</strong> — a small draggable bar (grab the <strong>✦</strong> handle to move it; its position is remembered). The buttons named below — <em>Companion Settings, World Truths, Sector Creator, Entities, Progress Tracks, Session</em> — all live there. Most have a chat-command equivalent too.</li></ul><h3>Campaign setup (one time)</h3><ol><li><strong>Add your API keys.</strong> Open <strong>Companion Settings → About</strong> (GM only). Enter your <strong>Claude API key</strong> (narration) and, for portraits and sector art, your <strong>OpenRouter API key</strong>, then Save Keys. Without keys the companion still runs on oracle rolls alone — only the AI prose and art are skipped.</li><li><strong>Set your safety tools.</strong> In <strong>Companion Settings → Safety</strong>, set <strong>Lines</strong> (hard limits) and <strong>Veils</strong> (soft limits); each player can add <strong>Private Lines</strong> on their own client. The <code>!x</code> X-Card is always available to everyone, no explanation required.</li><li><strong>Establish your World Truths.</strong> Open the <strong>World Truths</strong> button (or type <code>!truths</code>) and set the 14 axioms — cataclysm, exodus, laws, AI, precursors, and so on. These are your campaign's immutable foundation; the narrator is built never to contradict them. <code>!lore</code> posts a recap of them.</li><li><strong>Create your character.</strong> Use the system's character creation (Create Actor → <em>character</em>): set stats, condition meters, and assets. Your player character is filed in the <strong>PCs</strong> folder, and the companion detects your active character automatically in solo play.</li><li><strong>Create and finalise your starship.</strong> Create Actor → <em>starship</em>. It starts <strong>blank</strong> so you can name and set it up first. When ready, open the <strong>Entities</strong> panel, select the ship, and click <strong>✦ Finalise</strong> — that rolls its type / first look / mission, installs matching modules, writes its Notes, and generates its portrait and token. (<code>!ship envision</code> and <code>!ship history</code> add more detail later.)</li><li><strong>Build your starting sector.</strong> Open the <strong>Sector Creator</strong> button (or type <code>!sector new</code>) and follow the wizard. It generates your settlements, a local connection (an important NPC), and a sector-wide trouble. Settlements are filed under <strong>Sectors / &lt;name&gt;</strong>; the connection's NPC card under <strong>Sectors / &lt;name&gt; / NPCs</strong>.</li><li><strong>Finalise the new entities.</strong> In the <strong>Entities</strong> panel, click <strong>✦ Finalise</strong> on each settlement and on the connection NPC to roll their details, write their flavour, and generate portraits and tokens. (Settlements can finalise automatically — see <em>Generate Sector Entity Portraits</em> in Settings.) Use <code>!at &lt;Settlement&gt;</code> to set where the opening scene takes place.</li></ol><h3>Begin play</h3><ol start=\"8\"><li><strong>Begin the session.</strong> Open the <strong>Session</strong> panel and click <strong>Begin Session</strong> (or type <code>!begin-session</code>). This reviews any flagged content, offers an optional Spotlight Vignette (+1 momentum to everyone), and turns on the live pipeline — from here, plain chat narration is read as moves. A short galley vignette of your crew opens the scene.</li><li><strong>Envision the inciting incident.</strong> On the <strong>Session</strong> panel, click <strong>✦ Envision Inciting Incident</strong> (or type <code>!incite</code>). It rolls an Action + Theme spark and the narrator composes your campaign's opening dramatic event — grounded in your truths, sector, connection, and character — and proposes a starting vow.</li><li><strong>Swear your first iron vow.</strong> The inciting-incident card has a <strong>⚔ Swear this vow</strong> button that creates the vow on your character for you — and, when the incident has time pressure, its progress clock and the target NPC too. Prefer to do it by hand? Open the <strong>Progress Tracks</strong> panel and create a <strong>vow</strong> track at the suggested rank. Either way, this vow is the spine of your first story arc.</li><li><strong>Make your first move.</strong> Type what your character does, in plain chat. The companion identifies the move, rolls the dice, and the narrator responds. You're playing.</li></ol><h3>Tips</h3><ul><li><strong>No API key?</strong> Every step still works — oracle rolls, sector generation, and the inciting-incident spark all post on their own; only the AI prose and generated art are skipped.</li><li><strong>Nothing is final.</strong> You can re-run <strong>✦ Finalise</strong>, <strong>✦ Envision Inciting Incident</strong>, and any oracle roll as many times as you like until you're happy.</li><li>Start a chat message with <code>\\</code> to post plain text the move interpreter ignores; use <code>@scene &lt;question&gt;</code> to ask the narrator about the scene without rolling.</li></ul>", format: 1 },
  },
  {
    name: "Chat Commands",
    sort: 200,
    text: { content: "<h2>Chat Commands</h2><p>Type these directly into the Foundry chat bar.</p><h3>Core Commands</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><em>type narration normally</em></td><td>Intercepted and routed through the move pipeline. Dice are rolled, a result card is posted, and the narrator responds.</td></tr><tr><td><code>\\your message here</code></td><td>Bypass the move interpreter entirely. Posts as normal chat — nothing is intercepted.</td></tr><tr><td><code>@scene your question</code></td><td>Ask the narrator to describe something about the current scene without triggering a move.<br><em>Example: @scene What do the hull markings look like up close?</em></td></tr></tbody></table><h3>Safety Commands</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!x</code></td><td>X-Card. Immediately suppresses the current scene. Usable by any player at any time — no explanation required.</td></tr></tbody></table><h3>Recap Commands (GM only)</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!recap</code></td><td>Post a campaign recap — the full story arc compressed to 3–5 paragraphs.</td></tr><tr><td><code>!recap campaign</code></td><td>Same as !recap.</td></tr><tr><td><code>!recap session</code></td><td>Post a recap of the current session's moves and outcomes.</td></tr><tr><td><code>!recap session 3</code></td><td>Post a recap of a specific past session by number.</td></tr></tbody></table><h3>Lore Commands (GM only)</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!truths</code></td><td>Open the foundry-ironsworn World Truths dialog so the campaign's canonical truths can be set or revised.</td></tr><tr><td><code>!lore</code></td><td>Post a narrator-generated lore recap card summarising the campaign's confirmed World Truths. Requires a Claude API key.</td></tr></tbody></table><h3>Location & Sector Commands (GM only)</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!at Name</code></td><td>Set the current location by name. Matches any tracked settlement, location, or planet — used by the narrator for scene context.</td></tr><tr><td><code>!at</code></td><td>Clear the current location.</td></tr><tr><td><code>!sector list</code></td><td>Post a chat card listing every sector created in this world; the active sector is marked.</td></tr><tr><td><code>!sector new</code></td><td>Open the Sector Creator wizard.</td></tr><tr><td><code>!sector Name</code></td><td>Switch the active sector to the named one.</td></tr></tbody></table><h3>Encounter Commands</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!sfc encounter Name</code></td><td>Spawn a canonical foundry-ironsworn encounter by name (e.g. <code>Bogfaller</code>). Falls back to a chat card if the named encounter isn't in the system compendium.</td></tr></tbody></table><h3>Pacing Commands (GM only)</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!pace hot</code></td><td>Shift the pacing dials up by 3 for the current scene — more inputs route to the move pipeline.</td></tr><tr><td><code>!pace quiet</code></td><td>Shift the pacing dials down by 3 — more inputs stay narrative.</td></tr><tr><td><code>!pace clear</code></td><td>Remove the scene override and return to dial defaults.</td></tr><tr><td><code>!pace status</code></td><td>Post a card showing current effective dial values.</td></tr><tr><td><code>!roll</code></td><td>Force the next undecorated chat input through the move interpreter, bypassing the classifier. Use when the classifier sent something to narration that you wanted to roll.</td></tr></tbody></table><h3>World Journal Commands (GM only)</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!journal faction \"Name\" attitude — summary</code></td><td>Record faction intelligence. Attitude: hostile, neutral, allied, unknown.</td></tr><tr><td><code>!journal location \"Name\" type — description</code></td><td>Record a location. Type: settlement, derelict, vault, planet, ship, other.</td></tr><tr><td><code>!journal lore \"Title\" confirmed — text</code></td><td>Record a lore discovery. Use \"confirmed\" or \"rumour\".</td></tr><tr><td><code>!journal threat \"Name\" severity — summary</code></td><td>Record an active threat. Severity: looming, active, immediate, resolved.</td></tr></tbody></table><h3>Fact Continuity Commands (GM only)</h3><p>See the <strong>Fact Continuity</strong> page for the full feature description.</p><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!scene start</code></td><td>Begin a fresh scene moment. Any leftover scene is flushed first (truths migrate to entity records, scene/free-text truths archive to World Journal Lore).</td></tr><tr><td><code>!scene end</code></td><td>End the active scene. Same migration logic; the active-scene ledgers are cleared.</td></tr><tr><td><code>!truth strike &lt;id-prefix&gt;</code></td><td>Strike a specific truth from the active scene. Accepts a unique 4+ character ID prefix from the Correct-a-fact dialog. Audit-preserving — the entry is marked <code>retracted</code>, not deleted.</td></tr><tr><td><code>!truth set &lt;subject&gt; &lt;fact&gt;</code></td><td>Assert a new truth directly, without going through the narrator. Use quotes for multi-word subjects: <code>!truth set &quot;Covenant officer&quot; Walks with a slight limp</code>.</td></tr><tr><td><code>!state strike &lt;subject&gt; &lt;attribute&gt;</code></td><td>Remove a state attribute from the active scene. Example: <code>!state strike scene lighting</code>.</td></tr><tr><td><code>!state set &lt;subject&gt; &lt;attribute&gt;=&lt;value&gt;</code></td><td>Set or replace a state value. Example: <code>!state set scene lighting=stable</code>.</td></tr></tbody></table><h3>Migration Commands (GM only)</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!migrate-entities</code></td><td>One-time housekeeping: migrate ship records from custom journal storage onto native foundry-ironsworn Starship Actor documents. Idempotent — safe to run multiple times. Legacy journals are tagged, not deleted, so you can verify the result before cleanup.</td></tr><tr><td><code>!migrate-entities --cleanup</code></td><td>Delete legacy journals that were migrated more than 7 days ago. Run only after you've confirmed the migrated Actors look right.</td></tr></tbody></table><h3>Oracle Commands</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!oracle yes [odds] [question]</code></td><td>Ask the Oracle yes/no. Odds: <code>small</code> · <code>unlikely</code> · <code>50/50</code> · <code>likely</code> · <code>certain</code> (default 50/50). On a match (both d10 digits equal, including 100) the card flags 'envision an extreme result or twist'.</td></tr><tr><td><code>!pay-the-price [question]</code></td><td>Roll the d100 Pay the Price table directly. Alias: <code>!ptp</code>. The same table is automatically rolled and surfaced as an advisory seed on every move's miss; this command lets the player or GM invoke it manually outside the move pipeline.</td></tr><tr><td><code>!oracle-add [id]</code></td><td>GM-only. Opens a dialog for defining a custom oracle table — one line per entry (<code>min-max Result</code>), persisted on campaignState and re-registered at world ready.</td></tr></tbody></table><h3>Session & Safety Commands</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!flag</code></td><td>Set a Flag — captures content to approach mindfully. Public or GM-private; persisted on <code>campaignState.safetyFlags</code>.</td></tr><tr><td><code>!fate</code></td><td>Change Your Fate — five-option chooser (Reframe / Refocus / Replace / Redirect / Reshape) + optional framing text.</td></tr><tr><td><code>!break</code></td><td>Take a Break — Move on (records a +1-on-next-non-progress-move bonus) or Stop for now (chains into End a Session).</td></tr><tr><td><code>!begin-session</code></td><td>Begin a Session — lists recent flagged content for review and offers an optional Spotlight Vignette roll (+1 momentum to all players).</td></tr><tr><td><code>!incite</code></td><td>Envision an Inciting Incident — rolls an Action + Theme spark and has the narrator compose your campaign's opening event (grounded in your truths/sector/connection/character) plus a suggested starting vow. The card's <strong>⚔ Swear this vow</strong> button creates that vow on your character sheet — with a tension clock attached when the incident has a deadline — and adds the vow's target NPC to your Connections with their backstory. Also on the Session panel as ✦ Envision Inciting Incident.</td></tr><tr><td><code>!end-session</code></td><td>End a Session — captures quest focus / connection focus for next session and awards +1 momentum if either is set.</td></tr></tbody></table><h3>Combat / Connection Commands</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!bond &lt;rank&gt;</code></td><td>Bonded Develop Your Relationship roll: +rank (troublesome=+1 … epic=+5) vs. the challenge dice. Strong hit marks 2 ticks on the bonds legacy track; weak hit grants +2 momentum; miss grants nothing.</td></tr></tbody></table><h3>Clocks</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!clock list</code></td><td>List all clocks with filled/segments. Anyone may invoke.</td></tr><tr><td><code>!clock new &lt;name&gt; &lt;segments&gt; [campaign|tension] [odds]</code></td><td>GM-only. Segments must be 4, 6, 8, or 10. Default type tension; default odds likely (campaign only). Use quotes for multi-word names.</td></tr><tr><td><code>!clock advance &lt;name&gt;</code></td><td>Campaign clocks roll the configured odds via Ask the Oracle; tension clocks fill one segment.</td></tr><tr><td><code>!clock fill &lt;name&gt;</code></td><td>Fill one segment manually.</td></tr><tr><td><code>!clock reset &lt;name&gt;</code></td><td>Reset the clock to 0 filled.</td></tr><tr><td><code>!clock remove &lt;name&gt;</code></td><td>Delete the clock.</td></tr></tbody></table><h3>Vehicle Repair</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!repair</code></td><td>Open the Repair point-spend dialog. Pick situation (facility / field / under fire) × outcome (strong / weak), optionally trade supply for extra points, allocate to the six spend types. +1 integrity / Clear battered / Sacrifice Resources are applied mechanically; companion-health and module spends record on the chat card.</td></tr></tbody></table><h3>Ship Envision &amp; History</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!ship envision [name] [facet]</code></td><td>Roll supplementary ship oracles beyond the seed (captain Role/Goal/First-look/Name, Action+Theme for crew flavour and current agenda, Initial Contact) and post a 2-3 sentence narrator paragraph weaving them in. Facets: <code>captain</code> · <code>crew</code> · <code>agenda</code> · <code>contact</code> · <code>all</code> (default). Target defaults to your command vehicle, or your lone tracked ship. Appended to the ship's Notes as a dated section.</td></tr><tr><td><code>!ship history [name] [beats]</code></td><td>Roll Action+Theme backstory beats (default 3, up to 6) plus a Story Clue, then ask the narrator for a 2-3 paragraph history grounded in the ship's existing identity. Same targeting rules and Notes append as Envision.</td></tr></tbody></table>", format: 1 },
  },
  {
    name: "Toolbar Buttons",
    sort: 300,
    text: { content: "<h2>Toolbar Buttons</h2><p>The Companion's buttons live on a small <strong>floating toolbar</strong> — a draggable bar pinned to the screen, available whether or not a scene/map is loaded. Drag it by the <strong>✦</strong> handle to reposition it; its position is remembered per user. (It is not part of Foundry's scene-controls strip on the left of the canvas.)</p><table><thead><tr><th>Button</th><th>Who</th><th>Description</th></tr></thead><tbody><tr><td><strong>Session</strong></td><td>Everyone</td><td>Open the Session panel: Begin / End Session, Set a Flag, Change Your Fate, Take a Break, and ✦ Envision Inciting Incident (GM). Before Begin Session, plain typed narration does not trigger the move pipeline.</td></tr><tr><td><strong>Progress Tracks</strong></td><td>Everyone</td><td>Open the progress track panel. Create, mark, and complete vows, expeditions, combat tracks, connections, scene challenges, and legacy tracks. Scene-challenge tracks auto-create a paired 4-segment tension clock; combat tracks surface a click-to-cycle in-control / bad-spot toggle. All tracks are stored in the \"Starforged Progress Tracks\" journal.</td></tr><tr><td><strong>Entities</strong></td><td>Everyone</td><td>Open the entity panel. Browse all tracked Connections, Ships, Settlements, Factions, and Planets. Click any entity for its detail view, portrait, and notes — and use <strong>✦ Finalise</strong> to populate a blank ship or NPC card (oracles, flavour, portrait + token).</td></tr><tr><td><strong>Character Chronicle</strong></td><td>Everyone</td><td>Open the character chronicle — a reverse-chronological record of significant story moments, revelations, and relationship developments. Players can add and edit entries freely.</td></tr><tr><td><strong>Clocks</strong></td><td>Everyone</td><td>Open the Clocks panel. Create campaign or tension clocks (4 / 6 / 8 / 10 segments), advance them (campaign clocks roll Ask the Oracle with the configured odds; tension clocks fill one segment), fill manually, reset, or remove. Anyone can view; mutations are GM-only.</td></tr><tr><td><strong>Private Channel</strong></td><td>Everyone*</td><td>Open your private narrator conversation window. Hidden for everyone when the Private Channel setting is off.</td></tr><tr><td><strong>Companion Settings</strong></td><td>GM</td><td>Open the settings panel. Configure Safety, Mischief dial, Narrator, and API keys (About tab).</td></tr><tr><td><strong>Sector Creator</strong></td><td>GM</td><td>Open the Sector Creator wizard — the 11-step guided sector generation (settlements, local connection, sector trouble, map). Same as <code>!sector new</code>.</td></tr><tr><td><strong>World Journal</strong></td><td>GM</td><td>Open the World Journal panel — lore, threats, factions, locations, and the session log, with Confirm and severity controls.</td></tr><tr><td><strong>World Truths</strong></td><td>GM</td><td>Open the World Truths dialog to set or revise the campaign's 14 canonical truths. Same as <code>!truths</code>.</td></tr><tr><td><strong>Custom Oracles</strong></td><td>GM</td><td>Open the Custom Oracles panel. Define new oracle tables (one line per entry, <code>min-max Result</code>), roll on them with a single click, delete them after a confirm. Tables persist on campaign state and re-register at world load.</td></tr></tbody></table><p><em>If the toolbar is ever off-screen (e.g. after a resolution change), drag it back by the ✦ handle — or reset its saved position via the <code>companionToolbarPosition</code> client setting.</em></p>", format: 1 },
  },
  {
    name: "Settings Reference",
    sort: 400,
    text: { content: "<h2>Configure Settings → Starforged Companion</h2><table><thead><tr><th>Setting</th><th>Description</th><th>Scope</th></tr></thead><tbody><tr><td><strong>Claude API Key</strong></td><td>Your Anthropic API key. Stored in your browser only — never sent to Foundry's server. Get one at console.anthropic.com.</td><td>Client (About tab)</td></tr><tr><td><strong>OpenRouter API Key</strong></td><td>Your OpenRouter API key for image generation (entity portraits and sector backgrounds). Get one at openrouter.ai.</td><td>Client (About tab)</td></tr><tr><td><strong>OpenRouter Image Model</strong></td><td>OpenRouter model identifier for image generation. Default: black-forest-labs/flux.2-pro. Change to a cheaper FLUX variant or a different provider's image model on OpenRouter at any time.</td><td>World</td></tr><tr><td><strong>Push-to-Talk</strong></td><td>Enable speech input via the browser's Web Speech API. Chromium browsers only. Adds a microphone button to the chat bar.</td><td>Client</td></tr><tr><td><strong>Speech Input Language</strong></td><td>BCP 47 language tag for speech recognition. Default: en-US.</td><td>Client</td></tr><tr><td><strong>Location Background Art Source</strong></td><td>Where settlement/location background art comes from — Auto, Kirin, Rains, or OpenRouter. Default: Auto.</td><td>World</td></tr><tr><td><strong>Generate Sector Background Art</strong></td><td>Generate a background image for each new sector region. Default: on.</td><td>World</td></tr><tr><td><strong>Generate Sector Narrator Stubs</strong></td><td>Generate short atmospheric narrator stubs for new sectors and settlements. Default: on.</td><td>World</td></tr><tr><td><strong>Generate Sector Entity Portraits</strong></td><td>Auto-finalise each settlement created by the Sector Creator and generate its portrait and matching token image. Default: on. Requires the OpenRouter API key.</td><td>World</td></tr><tr><td><strong>Auto-Seed Starship Details</strong></td><td>Roll starship oracles, install modules, write Notes, and generate art AT CREATION. Default: off — new starships are created blank; populate them with ✦ Finalise in the Entities panel.</td><td>World</td></tr><tr><td><strong>Auto-Seed NPC / Connection Details</strong></td><td>Roll the character oracles into a new NPC card's Characteristics, write a Notes introduction, and generate a portrait/token AT CREATION. Default: off — new NPC cards are created blank; populate them with ✦ Finalise in the Entities panel.</td><td>World</td></tr></tbody></table><h2>Companion Settings Panel (Toolbar → 🛡)</h2><h3>Safety Tab</h3><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>Lines</strong></td><td>Hard content limits. Never crossed under any circumstances. Set by GM, apply to all players.</td></tr><tr><td><strong>Veils</strong></td><td>Soft content limits. Approached carefully or faded to black. Set by GM.</td></tr><tr><td><strong>Private Lines</strong></td><td>Personal hard limits visible only to you and the GM. Set on your own client — never shared with other players.</td></tr></tbody></table><h3>Mischief Tab</h3><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>Lawful</strong></td><td>Literal move interpretation. No misdirection. Asides are plain.</td></tr><tr><td><strong>Balanced</strong></td><td>Default. Occasional organic misreads that create interesting moments. Wry asides when interpretation is ambiguous.</td></tr><tr><td><strong>Chaotic</strong></td><td>Actively seeks unexpected move interpretations. Asides are openly smug about it.</td></tr></tbody></table><h3>Pacing Classifier (Mischief Tab)</h3><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>Enable pacing classifier</strong></td><td>Master switch. When on, a Haiku pre-classifier decides whether each undecorated chat input should run the move pipeline, narrate, or narrate with a move suggestion. When off, every input goes straight to the move interpreter.</td></tr><tr><td><strong>Pacing dials (0–10)</strong></td><td>Per-category prior on how move-leaning each scene type should be. Defaults: Combat 9, Investigation 6, Exploration 6, Social 5, Downtime 1. The active Mischief dial (Lawful / Balanced / Chaotic) selects how strictly the classifier reads player intent; the per-category dials shape the category-level prior on top of that.</td></tr><tr><td><strong>Density window</strong></td><td>Number of recent inputs considered for the recent-move-density signal. Higher = smoother pacing, lower = more reactive. Range 3–10.</td></tr></tbody></table><h3>Fact Continuity (Mischief Tab)</h3><p>See the <strong>Fact Continuity</strong> page for the full feature description.</p><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>Enable fact continuity</strong></td><td>Master switch. When on, every narrator response carries a structured sidecar that the module uses to record binding scene truths and current state. Subsequent narrator calls receive the active-scene ledger as part of their context.</td></tr><tr><td><strong>Inject the active-scene ledger</strong></td><td>When on, narrator system prompts receive a Section 6.5 block listing in-scope truths and state. Disable to keep the bookkeeping pipeline but exclude the ledger from the prompt itself.</td></tr><tr><td><strong>Require a sidecar</strong></td><td>When on, the narrator response is expected to contain a fenced JSON sidecar after every reply. When off, missing sidecars are tolerated silently.</td></tr><tr><td><strong>Max ledger tokens</strong></td><td>Soft cap on the size of the active-scene ledger block in the narrator system prompt. State entries are truncated first when the cap is exceeded; binding truths are never dropped. Default 400.</td></tr><tr><td><strong>Scene frame</strong></td><td>The narrator maintains a snapshot of the current scene — where it is set, who is present, what is happening — updated with every response and shown to it on every call. Keeps the scene's cast and location stable across turns. Default: on.</td></tr><tr><td><strong>Consistency check (experimental)</strong></td><td>After every narration, run a Haiku audit pass that checks the prose against the active-scene ledger. High-confidence contradictions surface on the existing GM-only Narrative Review card with a &quot;Retract the offending fact&quot; button. Adds ~$0.0004 and 200–500ms per narration; off by default.</td></tr><tr><td><strong>Ship Positioning</strong></td><td>Track the command vehicle's position (sector / planet / nearest settlement) and surface it as a <em>SHIP POSITION:</em> line in the narrator's prompt. Updates from <code>!at</code>, non-miss <em>Set a Course</em>, narrator sidecar, and (when enabled) sector-Scene Token drag. Default on.</td></tr><tr><td><strong>Auto-Move Ship on Set a Course</strong></td><td>When on, a strong / weak hit on Set a Course updates the command vehicle's position to the destination named in your narration. Off forces a manual <code>!at</code> after every travel resolution. Default on.</td></tr><tr><td><strong>Sector-Scene Ship Token</strong></td><td>Place a command-vehicle Token on every new sector Scene. Dragging the Token onto a settlement Note pin fires the same Set a Course pipeline a chat-typed move would; the Token only moves on a non-miss. Default on.</td></tr><tr><td><strong>Ship Token Snap Radius (grid cells)</strong></td><td>How close the Token must come to a settlement pin to count as a destination drop. 0 = exact-cell overlap; 2 = forgiving. Default 1.</td></tr></tbody></table><h3>Narrator Tab</h3><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>Narration Enabled</strong></td><td>Master switch for AI narration after moves and paced input. Default: on.</td></tr><tr><td><strong>Narration Model</strong></td><td>Claude model for narration. Sonnet 4.5 (recommended, richer prose) or Haiku 4.5 (faster, more economical).</td></tr><tr><td><strong>Perspective</strong></td><td>Auto (second person \"you\" for solo, third person for multiplayer), or fixed to one.</td></tr><tr><td><strong>Tone</strong></td><td>Wry (default), Grim and grounded, Operatic, Noir, or Matter of fact.</td></tr><tr><td><strong>Length</strong></td><td>Target sentence count per narration. Default: 3.</td></tr><tr><td><strong>Context Cards</strong></td><td>How many recent narrator cards each narration sees as fiction context (its short-term memory horizon). Raise if the narrator forgets recent events. Range 1–10, default 3.</td></tr><tr><td><strong>Rolling Session Summary</strong></td><td>Maintain a compressed &quot;story so far&quot; of the whole session so the narrator keeps the narrative arc of beats older than the Context Cards horizon. Regenerated in the background on a small delay (so it costs only an occasional lightweight summary) and saved to the Session Log at End Session. Default: on.</td></tr><tr><td><strong>Custom Instructions</strong></td><td>Free-text injected into the narrator system prompt. Use for campaign-specific style notes, character voice, setting details.</td></tr><tr><td><strong>Narration Max Tokens</strong></td><td>Hard cap on narration output length, in tokens. Default: 300.</td></tr></tbody></table><h3>Scene Interrogation</h3><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>Scene Interrogation Enabled</strong></td><td>When on, messages starting with <code>@scene</code> ask the narrator to describe the scene without triggering a move.</td></tr><tr><td><strong>Scene Response Length</strong></td><td>Target sentence count for scene responses. Default: 2.</td></tr><tr><td><strong>Scene Context Cards</strong></td><td>Number of recent narration cards included as scene context for each query. Default: 3.</td></tr></tbody></table><h3>Previously On / Recap</h3><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>Auto Recap at Session Start</strong></td><td>When enabled, a campaign recap is automatically posted to chat when a new session begins (gap exceeds threshold). Default: on.</td></tr><tr><td><strong>Session Gap Threshold</strong></td><td>Hours of inactivity before a world load is treated as a new session for auto-recap purposes. Default: 4 hours.</td></tr><tr><td><strong>Recap GM-Only</strong></td><td>When enabled, only the GM can trigger recap generation via !recap commands. Default: on.</td></tr></tbody></table><h3>Character Management</h3><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>Active Character</strong></td><td>The player character whose meters and chronicle drive narration and recaps. In solo play this is detected automatically.</td></tr><tr><td><strong>Auto-Write Chronicle Entries</strong></td><td>When on, a short chronicle entry is written after each narrated turn. Default: on.</td></tr><tr><td><strong>Chronicle Capture Threshold</strong></td><td>Minimum significance for an automatic chronicle entry. Conservative by default — fleeting scene beats are skipped; you can still add smaller notes by hand. One of Everything, Scene and above, Notable and above, Significant and above (default), or Campaign-defining only.</td></tr><tr><td><strong>Chronicle Context Count</strong></td><td>Number of recent chronicle entries fed into narration and recap context. Default: 5.</td></tr><tr><td><strong>Character Context Enabled</strong></td><td>When on, the active character's state (meters, debilities, XP) is injected into the narrator context. Default: on.</td></tr></tbody></table><h3>World Journal</h3><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>World Journal Enabled</strong></td><td>Master switch for the World Journal (lore, threats, factions, locations, and session log). Default: on.</td></tr><tr><td><strong>Private Channel</strong></td><td>Enable the per-player Private Channel window — the ✦ toolbar button for a private narrator conversation. When off, the button is hidden for everyone. Default: on.</td></tr><tr><td><strong>Auto-Detect Entries</strong></td><td>When on, the combined detection pass proposes World Journal entries from narration. Default: on.</td></tr><tr><td><strong>Lore in Context</strong></td><td>Inject confirmed lore into the narrator context. Default: on.</td></tr><tr><td><strong>Threats in Context</strong></td><td>Inject immediate threats into the narrator context. Default: on.</td></tr><tr><td><strong>Faction Landscape in Context</strong></td><td>Inject the faction landscape into the narrator context. Default: on.</td></tr><tr><td><strong>Contradiction Notifications</strong></td><td>Surface a chat card when new narration contradicts a confirmed entry. Default: on.</td></tr><tr><td><strong>Session Log Auto-Write</strong></td><td>Append to the session log as the session progresses. Default: on.</td></tr><tr><td><strong>Lore Capture Threshold</strong></td><td>Minimum significance for an auto-detected Lore entry. Conservative by default — transient scene beats are dropped so Lore holds durable world facts. Same five levels as the Chronicle threshold; default Significant and above.</td></tr><tr><td><strong>Threat Capture Threshold</strong></td><td>Minimum significance for an auto-detected Threat. Conservative by default — scene-level complications are dropped so Threats holds campaign-level dangers. Same five levels; default Significant and above.</td></tr></tbody></table><h3>Audio Narration</h3><p>See the <strong>Audio Narration</strong> page for the full feature description. Your ElevenLabs API key is entered in the About tab (client-scoped).</p><table><thead><tr><th>Setting</th><th>Description</th></tr></thead><tbody><tr><td><strong>Enable Audio (world)</strong></td><td>GM master switch for text-to-speech narration. Default: off.</td></tr><tr><td><strong>Narrator / NPC Voice</strong></td><td>ElevenLabs voice IDs for the narrator and for &lt;npc&gt; segments.</td></tr><tr><td><strong>Model / Speed</strong></td><td>ElevenLabs model and speech rate (0.7–1.5). Default model: eleven_flash_v2_5.</td></tr><tr><td><strong>Audio Cache Max</strong></td><td>Maximum on-disk size of the audio cache. Default: 200 MB.</td></tr><tr><td><strong>Enable Audio on this client</strong></td><td>Per-player opt-in for playback. Default: off.</td></tr><tr><td><strong>Volume / Auto-Play</strong></td><td>Client playback volume (0–1, default 0.8) and whether narration auto-plays (default off; requires a one-time click to prime audio).</td></tr></tbody></table>", format: 1 },
  },
  {
    name: "Narrator — How It Works",
    sort: 500,
    text: { content: "<h2>The Narrator</h2><p>After every accepted move, the narrator generates 2–4 sentences of atmospheric prose describing the mechanical consequence as story. It uses the full campaign context — world truths, active connections, safety configuration, character state — to stay grounded in what has been established.</p><h3>What the narrator does</h3><ul><li>Narrates the mechanical consequence of the move outcome (Strong Hit, Weak Hit, Miss) as vivid prose</li><li>Stays within the established fiction — it illuminates what is already there, not invents new things</li><li>Respects all Lines and Veils unconditionally — safety is a hard ceiling</li><li>Maintains a wry tone by default — knowing, slightly sardonic, aware of consequence without wallowing</li><li>Adapts perspective automatically: second person (\"you\") for solo, third person (character names) for multiplayer</li></ul><h3>What the narrator does not do</h3><ul><li>Roll dice or make mechanical decisions — that is the move system</li><li>Introduce new plot elements not already established — use oracles for that</li><li>Override your interpretation of events — it narrates consequences, you narrate actions</li></ul><h3>Scene interrogation</h3><p>Type <code>@scene [your question]</code> to ask the narrator to describe something without triggering a move. The narrator will describe what is already there, not invent new elements. This is the constraint by design — <code>@scene</code> is a camera, not a writer.</p><h3>Cost</h3><p>Estimated cost per 3-hour session (~20 moves) with prompt caching:</p><ul><li>Haiku interpretation + Haiku narration: ~$0.02/session</li><li>Haiku interpretation + Sonnet narration: ~$0.06/session</li></ul>", format: 1 },
  },
  {
    name: "Pacing",
    sort: 525,
    text: { content: "<h2>Pacing — Not Everything Is a Roll</h2><p>The Starforged rulebook is explicit: not every action is a move. Moves trigger from <em>fiction</em> plus <em>uncertainty</em>, not from every action a character takes. The pacing classifier sits between your chat input and the move interpreter, deciding which of three things should happen.</p><h3>Three outcomes</h3><table><thead><tr><th>Decision</th><th>What happens</th></tr></thead><tbody><tr><td><strong>MOVE</strong></td><td>The existing pipeline runs unchanged — move card, roll, narration.</td></tr><tr><td><strong>NARRATIVE</strong></td><td>The narrator responds in fiction. No roll, no move card, no mechanical state change.</td></tr><tr><td><strong>NARRATIVE_WITH_MOVE_AVAILABLE</strong></td><td>The narrator responds, ending with an italicized line offering a specific move if you want to push, plus a <strong>Roll</strong> button beneath the prose that runs that move on the original input when clicked.</td></tr></tbody></table><h3>The Mischief dial drives interpretation posture</h3><p>The classifier reads the same Mischief dial that governs the move interpreter, and treats it as its own interpretation posture:</p><table><thead><tr><th>Mischief</th><th>Classifier posture</th></tr></thead><tbody><tr><td><strong>Lawful</strong></td><td>Strict. Requires an explicit verbal intent marker (\"I try to\", \"I attempt to\", \"I roll to\", \"I want to\") before classifying as MOVE. Without one, you'll get NARRATIVE_WITH_MOVE_AVAILABLE and a hint.</td></tr><tr><td><strong>Balanced (default)</strong></td><td>Reads commitment from what you depict. Pressing a connection on intent or stakes, taking a real risk, or attempting something with consequence classifies as MOVE. Scene-setting, idle dialogue, and pure reaction stay narrative.</td></tr><tr><td><strong>Chaotic</strong></td><td>Aggressive. Classifies as MOVE whenever the fiction could plausibly support one. Reserves NARRATIVE for unambiguous atmosphere.</td></tr></tbody></table><p>Switch postures from Companion Settings → Mischief — the same control that already governs move interpretation now also tunes the classifier.</p><h3>The dials</h3><p>Five dials (Combat, Investigation, Exploration, Social, Downtime) tell the classifier how move-leaning each scene type should be on a 0–10 scale. 10 = almost every input is a move. 0 = almost never. Defaults: Combat 9, Investigation 6, Exploration 6, Social 5, Downtime 1.</p><h3>Scene override — !pace</h3><p>Shift all dials for the current scene without changing the world defaults:</p><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!pace hot</code></td><td>+3 across all categories — quick, more move-prone.</td></tr><tr><td><code>!pace quiet</code></td><td>-3 across all categories — slow, more narrative.</td></tr><tr><td><code>!pace clear</code></td><td>Remove the override.</td></tr><tr><td><code>!pace status</code></td><td>Show current effective dial values.</td></tr></tbody></table><p>Override clears on <code>!pace clear</code>, the next <code>@scene</code>, or session end.</p><h3>Named characters in paced narration</h3><p>When the classifier decides your input is narrative, the narrator still writes prose — and the narrator may name an NPC, faction, location, or lore beat in passing. Those names are now captured automatically: after the narration card posts, a GM-whispered draft card lists anything new, with the same Accept / Dismiss affordance the move pipeline already uses. Nothing is committed to the campaign record without GM confirmation. This runs on Balanced and Chaotic Mischief dials only; Lawful tables get zero added detection cost and no draft cards from paced narration (strict posture preserves the verbal-marker contract end-to-end).</p><h3>Recovery — when the classifier gets it wrong</h3><p>If the classifier sends something to narration that you wanted to roll, type <code>!roll</code>. The very next undecorated input you type will bypass the classifier and route to the move interpreter.</p><p>The opposite case — classifier picked MOVE when you wanted narrative — has no clean recovery; use <code>@scene</code> for the next input or type <code>\\</code> at the start of a message to bypass the pipeline entirely.</p><h3>Disabling pacing</h3><p>If pacing isn't for your table, turn it off in Companion Settings → Mischief → Pacing Classifier. With it disabled every input routes to the move interpreter as before.</p><h3>Cost</h3><p>Each classification is a small Haiku call with prompt caching. Expect ≈$0.0006 per call, ≈$0.02–0.03 per 3-hour session — below the noise floor of the existing pipeline cost.</p>", format: 1 },
  },
  {
    name: "Fact Continuity",
    sort: 535,
    text: { content: "<h2>Fact Continuity — Binding Scene Truths and Current State</h2><p>Fact continuity keeps the narrator honest about what's already been said in the current scene. As the narrator writes prose, it also emits a small structured sidecar listing the binding truths and the current state it just established. The module reads that sidecar, records the entries in the active scene's ledgers, and feeds them back into subsequent narrator calls — so the next response cannot quietly walk back something the narrator said three turns ago.</p><p><strong>Scene frame.</strong> Alongside truths and state, the narrator maintains a per-scene snapshot — where the scene is set, who is present, and what is happening in one line — refreshed with every response and shown to it on every call. The frame keeps the scene's cast and location stable across turns: characters listed as present stay in the narrator's view even when a quick reply doesn't name them. It is also required to record each named character's whereabouts and physical condition, and anything with a deadline or stakes, as scene facts — so a rescue's ticking clock can't quietly evaporate. The frame resets when a scene ends (<code>!scene end</code> or session close). Toggle under Companion Settings → Fact Continuity → Scene frame.</p><h3>Two ledgers</h3><table><thead><tr><th>Ledger</th><th>What it holds</th><th>Example</th></tr></thead><tbody><tr><td><strong>Truths</strong></td><td>Binding facts. Once asserted, they don't change unless the GM or player explicitly retracts them.</td><td><em>Vance walks with a slight limp · The Covenant officer speaks with a Bleakhold accent · The wind has died outside</em></td></tr><tr><td><strong>State</strong></td><td>What's true right now in the scene. Supersedes on attribute — a new value for the same subject + attribute replaces the old.</td><td><em>scene · lighting: red flicker · cargo bay · door: closed · Vance · posture: sitting at the corner table</em></td></tr></tbody></table><h3>Scope filter</h3><p>The ledger surfaces in the narrator's prompt include only the entries relevant to the turn at hand:</p><ul><li>Scene-scoped entries (subject = <code>scene</code>) are always included.</li><li>Entries about a matched entity (the relevance resolver identified the entity in the player's narration) are included.</li><li>Entries about the current location (set via <code>!at Name</code>) are included.</li><li>Free-text subjects (anything not matched to an entity) are included when the player's narration mentions them by name this turn.</li></ul><p>This keeps a 12-NPC campaign from flooding every prompt with every NPC's posture.</p><h3>Scene lifecycle</h3><p>Scenes start and end explicitly. A new scene is started when:</p><ul><li>The player types <code>@scene …</code> — every <code>@scene</code> begins a fresh moment in the fiction.</li><li>The GM types <code>!scene start</code> manually.</li><li>The first narration of a new session fires, if no scene was previously active.</li></ul><p>A scene ends when:</p><ul><li>The GM types <code>!scene end</code>.</li><li>The world closes (session end).</li><li>A new scene starts and the previous one was never cleanly ended.</li></ul><p>At scene end, the active ledgers are <em>migrated</em> rather than discarded. Entity-scoped truths append to the entity record's generative tier (so they survive across scenes the same way narrator-added detail already does). Scene-scoped and free-text truths archive to the World Journal — Lore as narrator-asserted (unconfirmed) entries, tagged with the scene ID so they can be reviewed in context later. State (which is by definition transient — posture, lighting, weather) is discarded.</p><h3>Correcting a fact</h3><p>Every narrator card carries a <em>Correct a fact</em> button in its footer. Clicking it opens a dialog listing every in-scope truth and state value, with per-row Strike / Replace actions.</p><table><thead><tr><th>Actor</th><th>Strike a truth</th><th>Replace a truth</th><th>Strike / replace state</th></tr></thead><tbody><tr><td>GM</td><td>Any</td><td>Any</td><td>Any</td></tr><tr><td>Player</td><td>Any except GM-asserted</td><td>Any except GM-asserted</td><td>Any</td></tr></tbody></table><p>The dialog is the primary affordance. The <code>!truth</code> and <code>!state</code> chat commands provide keyboard / accessibility parity:</p><ul><li><code>!truth strike &lt;id-prefix&gt;</code> — strike-through; entry kept for audit.</li><li><code>!truth set &lt;subject&gt; &lt;fact&gt;</code> — assert manually, bypassing the narrator. Use quotes for multi-word subjects.</li><li><code>!state strike &lt;subject&gt; &lt;attribute&gt;</code> — remove one state attribute.</li><li><code>!state set &lt;subject&gt; &lt;attribute&gt;=&lt;value&gt;</code> — set or replace.</li></ul><h3>Consistency check (experimental)</h3><p>An optional Haiku audit pass runs after every narration. It compares the prose against the active-scene ledger and flags any contradiction it finds. High-confidence contradictions surface on the existing GM-only <em>◈ Narrative Review</em> card — now augmented with a <strong>Retract the offending fact</strong> button that opens the correction dialog scoped to the relevant entries. Medium and low confidence results are recorded as telemetry but not surfaced.</p><p>The pass adds roughly $0.0004 and 200–500ms per narration. It's off by default; enable it in Companion Settings → Mischief → Fact Continuity.</p><h3>Telemetry</h3><p>If the consistency check is on, every audit pass is logged to a <em>Consistency Check</em> page under the existing <em>Pacing Telemetry</em> journal. Each entry records the scene ID, prose excerpt, contradiction list, elapsed time, and whether anything was routed to the Narrative Review surface — useful for tuning the threshold over real play.</p><h3>Cost</h3><p>Fact continuity adds no extra API calls at the baseline. The sidecar is part of the narrator's existing response. The ledger block adds a few hundred tokens to the narrator system prompt, well within the existing budget. The optional consistency check is the only added per-turn cost.</p><h3>Disabling</h3><p>If the feature isn't useful for your campaign, turn it off in Companion Settings → Mischief → Fact Continuity. The sidecar instruction is suppressed, the ledger block is dropped from the narrator prompt, and the parser short-circuits — the feature is fully decoupled from the rest of the pipeline.</p>", format: 1 },
  },
  {
    name: "Previously On",
    sort: 550,
    text: { content: "<h2>Previously On</h2><p>The recap system keeps everyone oriented across sessions — no need to re-read notes or remember what happened last time.</p><h3>Session Recap</h3><p>Type <code>!recap session</code> to post a card summarising everything that happened during the current session: every narrated move, its outcome, and the first sentence of the narrator's response. No API call is required — the summary is assembled directly from the session's chat history.</p><p>The card shows a count of resolved moves, strong hits, and notes any match rolls.</p><h3>Campaign Recap</h3><p>Type <code>!recap</code> or <code>!recap campaign</code> to post a 3–5 paragraph campaign arc summary generated by Claude. The recap covers how the campaign began, key relationships, vows and their status, significant revelations, and where things stand now.</p><p>The campaign recap is <strong>cached</strong> — it only regenerates when new chronicle entries are added. Subsequent calls that session return immediately from cache at no cost.</p><h3>Auto-recap at session start</h3><p>When the Auto Recap setting is enabled (default), a campaign recap is posted automatically to chat when the module detects a new session beginning — defined as a world load after a gap longer than the Session Gap Threshold (default: 4 hours).</p><p>This orients returning players without anyone needing to ask.</p><h3>Session context injection</h3><p>On the first narration of each session, the campaign recap is injected into the narrator's system prompt as additional context. This gives the narrator full campaign awareness at session start without requiring the entire chronicle in every packet.</p><h3>Commands</h3><table><thead><tr><th>Command</th><th>Effect</th></tr></thead><tbody><tr><td><code>!recap</code></td><td>Post campaign recap (Claude, cached)</td></tr><tr><td><code>!recap campaign</code></td><td>Same as !recap</td></tr><tr><td><code>!recap session</code></td><td>Post current session recap (no API call)</td></tr></tbody></table><p>By default, recap commands are GM-only to prevent API call spam in multiplayer. This can be changed in Companion Settings.</p>", format: 1 },
  },
  {
    name: "Safety System",
    sort: 600,
    text: { content: "<h2>Safety System</h2><p>The safety system ensures all narration respects your content boundaries. Safety configuration is always the first section of every context packet sent to the narrator. It is never omitted, summarised, or overridden by any other setting.</p><h3>X-Card</h3><p>Type <code>!x</code> in chat at any time to activate the X-Card. The current scene is immediately suppressed. The narrator will not continue until the scene is redirected. No explanation required. Any player can use it at any time.</p><p>To clear the X-Card, the GM can run this in the Foundry console:<br><code>const s = game.settings.get('starforged-companion','campaignState'); s.xCardActive=false; await game.settings.set('starforged-companion','campaignState',s);</code></p><h3>Lines and Veils</h3><p><strong>Lines</strong> are hard limits — content matching a Line will never appear regardless of any other setting. Set by GM in the Safety tab of the Companion Settings panel.</p><p><strong>Veils</strong> are soft limits — the narrator will approach this content carefully, redirect, or fade to black.</p><p><strong>Private Lines</strong> are personal hard limits visible only to you and the GM. Set on your own client — never visible to other players.</p><h3>How safety is applied</h3><p>Safety configuration is injected before any creative content in every narrator call. It is described to the narrator as a hard ceiling that overrides all other creative direction including the mischief dial setting.</p>", format: 1 },
  },
  {
    name: "Character Management",
    sort: 650,
    text: { content: "<h2>Character Management</h2><p>The module reads from and writes directly to the foundry-ironsworn Actor document. The character sheet is the single source of truth for all mechanical data.</p><h3>Automatic meter updates</h3><p>After every accepted move, the module automatically updates your character's meters:</p><ul><li><strong>Health</strong> — reduced by Endure Harm outcomes</li><li><strong>Spirit</strong> — reduced by Endure Stress outcomes</li><li><strong>Supply</strong> — reduced by Sacrifice Resources</li><li><strong>Momentum</strong> — adjusted by move outcomes; momentum burn handled automatically</li><li><strong>Debilities</strong> — set when mandatory suffer moves apply impacts</li><li><strong>XP</strong> — awarded when legacy track boxes are filled</li></ul><p><strong>Note:</strong> meter updates require the GM to be the active client. If you are playing as a player character, the GM client processes persistence. See the Troubleshooting page if meters are not updating.</p><h3>Character Chronicle</h3><p>Open the Chronicle via the toolbar (📖 button). The Chronicle records significant story moments — revelations, relationship developments, vow milestones, scars, and legacy events.</p><ul><li>The narrator automatically adds an entry after each narration</li><li>Players can add, edit, and annotate any entry freely</li><li>Pinned entries always appear in the narrator's context</li><li>Recent chronicle entries are included in every narration context packet</li></ul><p>The Chronicle is stored in a Foundry journal named <strong>\"Chronicle — [Character Name]\"</strong>. Do not delete this journal.</p><h3>Starship</h3><p>Ship damage (Battered, Cursed) is tracked on the starship Actor. The module finds your starship automatically — it must be a Foundry Actor of type \"starship\" owned by a player.</p>", format: 1 },
  },
  {
    name: "Progress Tracks",
    sort: 700,
    text: { content: "<h2>Progress Tracks</h2><p>Open the Progress Track panel via the toolbar (🗂 button in Token Controls).</p><h3>Track types</h3><ul><li><strong>Vow</strong> — sworn iron vows. Rank determines progress per mark.</li><li><strong>Expedition</strong> — journeys through the Forge.</li><li><strong>Combat</strong> — active fights where progress is marked by gaining ground.</li><li><strong>Connection</strong> — relationship tracks for NPCs. Linked to Connection entity records.</li><li><strong>Legacy</strong> — Quests, Bonds, and Discoveries legacy tracks for XP.</li></ul><h3>Ranks and progress</h3><table><thead><tr><th>Rank</th><th>Ticks per mark</th></tr></thead><tbody><tr><td>Troublesome</td><td>3 ticks (≈3–4 marks to fill)</td></tr><tr><td>Dangerous</td><td>2 ticks (≈5 marks)</td></tr><tr><td>Formidable</td><td>1 tick (≈10 marks)</td></tr><tr><td>Extreme</td><td>½ tick (≈20 marks)</td></tr><tr><td>Epic</td><td>¼ tick (≈40 marks)</td></tr></table><p><em>Each box = 4 ticks. 10 boxes = 40 ticks for a fully filled track.</em></p><h3>Storage</h3><p>All tracks are stored in a single journal named <strong>\"Starforged Progress Tracks\"</strong>. Do not delete this journal.</p>", format: 1 },
  },
  {
    name: "Entities & Portraits",
    sort: 800,
    text: { content: "<h2>Entities & Portraits</h2><p>Open the Entity Panel via the toolbar (👥 button in Token Controls).</p><h3>Entity types</h3><ul><li><strong>Connections</strong> — NPCs with a relationship track and history log</li><li><strong>Ships</strong> — vessels including your command ship</li><li><strong>Settlements</strong> — outposts, stations, and communities</li><li><strong>Factions</strong> — organisations, orders, and powers</li><li><strong>Planets</strong> — worlds and moons</li><li><strong>Locations</strong> — derelicts, vaults, structures, sites</li><li><strong>Creatures</strong> — named or distinctly described creature types</li></ul><h3>How entities get created</h3><p>Most entities are seeded automatically. When the narrator names a new NPC, ship, faction, or place in their prose, a small Haiku-powered detector spots it and posts a GM-whispered <strong>New Entities Detected</strong> chat card. Each draft has two buttons:</p><ul><li><strong>Confirm</strong> — creates the entity and adds it to the Entities panel.</li><li><strong>Dismiss</strong> — tells the detector to stop suggesting that name (useful for incidental references the narrator made up but you don't want to track).</li></ul><p>The card updates in place as you action each draft, so you can review at your own pace. <em>Make a Connection</em> still auto-creates the connection NPC on a hit without going through the draft card. The Lawful mischief dial skips paced-narration detection entirely; Balanced and Chaotic run it at roughly $0.0006 per call with prompt caching.</p><h3>Portrait lifecycle</h3><p>Each entity can have one AI-generated portrait (FLUX.2 Pro via OpenRouter by default; configurable). The portrait lifecycle is strict by design — it creates visual consistency for the campaign.</p><ol><li><strong>No portrait</strong> — Generate button available once a source description exists in the narration</li><li><strong>Portrait (unlocked)</strong> — One regeneration permitted</li><li><strong>Portrait (locked)</strong> — Permanently locked after regeneration. No further changes.</li></ol><p>Choose your regeneration carefully. The lock is permanent.</p><h3>Storage</h3><p>Each entity is stored in its own Foundry Journal Entry. Do not manually edit these journals — use the Entity Panel instead.</p>", format: 1 },
  },
  {
    name: "World Journal",
    sort: 900,
    text: { content: "<h2>World Journal</h2><p>The World Journal automatically documents things discovered during play. It lives in the Foundry Journals sidebar under a <strong>\"Starforged Companion\"</strong> folder.</p><h3>Journal categories</h3><ul><li><strong>Factions — Intelligence</strong> — what is known about organisations and powers, their goals and attitude</li><li><strong>Locations — Known Sites</strong> — descriptions of visited or named places</li><li><strong>Lore — Discoveries</strong> — revelations about the setting, precursors, AI history, and mysteries</li><li><strong>Threats — Active Dangers</strong> — tracked threats with severity status (looming → active → immediate → resolved)</li><li><strong>Session Log</strong> — automatically written at session end</li></ul><h3>How entries are created</h3><p><strong>Automatic:</strong> After every narration, the module analyses the text and records any world-significant events (faction encounters, location descriptions, lore revelations, new threats).</p><p><strong>Manual:</strong> Use <code>!journal</code> commands to record things directly (see Chat Commands page). This is also how to correct auto-detected entries or add things the module missed.</p><h3>Annotations</h3><p>Any player can annotate any world journal entry. Annotations are marked with your name and date. The module's auto-generated content is fully editable — this is your world record.</p>", format: 1 },
  },
  {
    name: "Audio Narration",
    sort: 950,
    text: { content: "<h2>Audio Narration</h2><p>Narrator prose can be read aloud by ElevenLabs text-to-speech. The text narration remains the canonical output; audio overlays it. The feature is off by default and opt-in per player.</p><h3>Setup</h3><ol><li>Sign up at <strong>elevenlabs.io</strong> and copy your API key.</li><li>In Companion Settings → <strong>About</strong>, paste it into the <em>ElevenLabs API Key</em> field and click <em>Save Keys</em>. The key is stored locally in your browser only.</li><li>In Companion Settings → <strong>Audio</strong>:<ul><li>GMs: tick <em>Enable audio narration for this world</em>, paste in two voice IDs (narrator voice + NPC voice — get the IDs from elevenlabs.io), pick a model, and set the playback speed.</li><li>Players (including the GM playing as a player): tick <em>Enable audio narration on this client</em>, set volume, and choose whether to auto-play.</li></ul></li><li>Click <em>Save Audio Settings</em>.</li></ol><h3>How it works</h3><p>Every narrator card grows a ▶ Play button when you have audio enabled. Click it to hear the narration. The button changes to ⏸ Pause while playing.</p><p>If <em>Auto-play</em> is on, narrator audio starts as soon as the card renders. The browser requires a user gesture before audio can play — the first card after a fresh page load shows a brief \"click anywhere\" overlay; subsequent cards auto-play without the overlay until you reload.</p><h3>NPC voice</h3><p>When the world has audio enabled, the narrator wraps NPC dialogue in <code>&lt;npc&gt;…&lt;/npc&gt;</code> markers. The audio pipeline plays those segments with the secondary voice; everything else uses the narrator voice. The chat card displays clean prose without the markers. Player-character dialogue is NOT tagged — players hear their own characters internally.</p><h3>Caching</h3><p>Generated audio is stored at <code>worlds/{worldId}/audio/</code>, same persistence guarantee as sector backgrounds. Re-narrating the same prose plays instantly. Cache writes are GM-gated; non-GM players generate locally and the GM commits to disk in the background.</p><h3>Cost</h3><p>ElevenLabs charges per character. Flash v2.5 and Turbo v2.5 are 0.5 credits/char (the default Flash is fastest and cheapest); Multilingual v2 and Eleven v3 are 1.0 credit/char (higher quality, slower). At ~400 chars per card and ~60 cards per 2-hour session, the upper bound before cache hits is 24,000–48,000 credits per session. Click <em>⟳ Check ElevenLabs usage</em> in the Audio tab for your live counter; there is no hard cutoff in the module.</p><h3>Failure modes</h3><ul><li><strong>No key</strong> — play button stays hidden.</li><li><strong>Bad key</strong> — first click shows the error and a console hint identifying the key prefix.</li><li><strong>Rate limit / quota</strong> — one-time warning toast; subsequent cards keep trying.</li><li><strong>Network or CORS error</strong> — play button hides for the rest of the session; reload to retry.</li></ul><p>None of these block the chat card from rendering — the prose appears immediately on every card.</p>", format: 1 },
  },
  {
    name: "Troubleshooting",
    sort: 1000,
    text: { content: "<h2>Troubleshooting</h2><h3>Move interpretation failed — API error</h3><p>Check your Claude API Key in Companion Settings → About. It should start with <code>sk-ant-</code>. Make sure there is no trailing whitespace. The key is stored per-browser — if you switch browsers you will need to re-enter it. Check the browser console (F12) for the specific error message.</p><h3>Narration unavailable after move card</h3><p>The narrator API call failed. Check the browser console (F12) for the specific error. Common causes: missing or invalid Claude API key, exhausted Anthropic credit, incorrect model name in Narrator settings.</p><h3>Character sheet not updating after moves</h3><p>Meter changes require the GM client to be active — the module gates world-scoped writes to the GM. If you are playing as a player character, make sure the GM account is connected. If you are the GM and meters still do not update, check the console for errors from <code>actorBridge.js</code> or <code>persistResolution.js</code>.</p><h3>No move card after typing narration</h3><p>The message may have been filtered. Check: you are not logged in as the GM (GM messages are excluded), the message does not start with <code>\\</code>, <code>/</code>, or <code>@</code>, and the message type is not OOC or a roll.</p><h3>Safety settings not applying</h3><p>Safety settings sync to campaign state on the ready hook and on every write. If changes do not appear to apply, reload the world (F5).</p><h3>X-Card will not clear</h3><p>Run this in the Foundry console (F12) as the GM:<br><code>const s = game.settings.get('starforged-companion','campaignState'); s.xCardActive=false; await game.settings.set('starforged-companion','campaignState',s);</code></p><h3>Progress tracks journal is missing</h3><p>The \"Starforged Progress Tracks\" journal was deleted. The module will recreate it automatically on the next track operation. Existing track data cannot be recovered if the journal was deleted.</p><h3>Portrait or sector art generation failed</h3><p>Check your OpenRouter API Key in Companion Settings → About. The key should start with <code>sk-or-v1-</code>. Make sure your OpenRouter account has credit. The default image model is FLUX.2 Pro (a few cents per image); change <strong>OpenRouter Image Model</strong> to a cheaper FLUX variant (e.g. <code>black-forest-labs/flux.2-klein</code>) if needed. Sector art failures also surface as permanent toast notifications describing the exact error.</p><h3>Campaign recap says \"No campaign history available yet\" even though I have played sessions</h3><p>Fixed across three passes. v1.2.4 corrected the read path. v1.2.7 added automatic Chronicle writes after every narration. v1.2.12 closed the silent gating bug that made both halves no-op in practice: the internal field both the writer and the reader checked was never actually populated, so even after dozens of narrations the recap was always reading from an empty list and the auto-write was always picking \"no character\". Both halves now find your player character through the same path the rest of the module uses, so <code>!recap</code> works without you doing anything special. If it still reports empty on v1.2.12 or later, confirm you are running as the GM on at least one narration (the writer is GM-gated, same as meter persistence) and that the Claude API key is configured.</p><h3>Narrator keeps introducing the same NPC over and over</h3><p>Fixed in v1.2.4. Two causes: an NPC stored as <em>\"Dr. Chen\"</em> wasn't matched against a <em>\"Chen\"</em> the narrator wrote, and unconfirmed draft cards weren't tracked, so the detector re-posted the same draft each turn. Update the module — honorifics now collapse during matching and pending drafts now suppress further detection of the same name.</p><h3>Narrator mentions \"records\", \"entities panel\", or \"contradictions\" in prose</h3><p>Fixed in v1.2.4. The narrator's permission prompt contained a sentence about \"capturing for the campaign record\" that the model occasionally paraphrased into fiction. That line is gone and every narration mode now has an explicit \"speak only as the fiction\" rule.</p><h3>Chronicle not recording</h3><p>The Chronicle journal is created automatically on first use. If entries are not appearing, check that the narrator is running successfully (narration card should appear after moves) and that chronicleAutoEntry is enabled in Companion Settings.</p><h3>Running integration tests</h3><p>Install and enable the <strong>Quench</strong> module. Click the flask icon in the Foundry toolbar. Select batches starting with \"STARFORGED:\" and click Run. Tests requiring an API key will be skipped automatically if no key is configured.</p>", format: 1 },
  },
  {
    name: "Changelog",
    sort: 1100,
    text: { content: "<h2>Changelog</h2><h3>v1.7.13</h3><ul><li>Added: <strong>The narrator keeps a running memory of the whole session.</strong> Beats that scrolled past the recent-cards horizon used to fade from the narrator's memory mid-session, leaving only their hard facts. The narrator now keeps a compressed &quot;story so far&quot; of the session — refreshed in the background as you play and saved to the Session Log when you end the session — so the thread holds across a long sitting. Toggle: Companion Settings → Narrator → Rolling session summary.</li><li>Improved: <strong>Recurring NPCs remember what they did; bit-part characters stay disposable.</strong> An established NPC's meaningful actions and developments are now recorded on their card during ordinary roleplay and conversation, not just after dice rolls, so characters stay consistent across a session — and only consequential beats are kept, not idle motion. The narrator also now leans toward leaving one-off background figures generic and unnamed ('a guard', 'the comms officer'), reserving names for characters meant to recur.</li><li>Fixed: <strong>Both characters appear in the Begin a Session vignette.</strong> With two PCs the opening galley scene could omit one; it now builds its cast from your full player-character roster, so everyone shows up (present in the galley, or named in the banter). (v1.7.12 playtest finding B.)</li><li>Fixed: <strong>Player-sworn vows queue the NPC for the GM.</strong> When a non-GM player swears a vow naming a new character, that character is now queued as a connection draft for the GM to confirm in one click, instead of a dead \"ask your GM\" note. (v1.7.12 playtest finding C.)</li><li>Fixed: <strong>An NPC's title is kept as their role.</strong> A name like \"Administrator Lyssa Chen\" no longer gets a random contradictory role — a recognised title in the name (Administrator, Captain, Doctor, Governor, …) becomes the role. (v1.7.12 playtest finding D.)</li><li>Fixed: <strong>Sector maps open centred on the map.</strong> A generated sector map could load shifted off into the black space past the edge; it now centres correctly on the map. (v1.7.12 playtest finding A.)</li><li>Fixed: <strong>Your co-op partner's character is no longer proposed as a new NPC.</strong> Player characters are now treated as established before entity detection runs, so the New Entities Detected card stops offering to create your fellow players as Connections. (v1.7.12 playtest finding F.)</li><li>Fixed: <strong>No \"Roll Compel\" against a fellow player character.</strong> When your message addresses another player character, the narrator no longer suggests Compel or the relationship moves against them — inter-player tension stays roleplay. (v1.7.12 playtest finding G.)</li><li>Improved: <strong>Clearer Push-to-Talk and audio feedback for players.</strong> Push-to-Talk is now labelled as a per-player, per-device setting, and clicking Play on a narrator card that can't produce audio now tells you why instead of doing nothing. (v1.7.12 playtest findings E and H.)</li><li>Fixed: <strong>NPC portraits match the character's pronouns.</strong> A character rolled as she/her could still come back from the art generator looking masculine; the portrait prompt now restates the established gender where the image model weighs it most, so the picture and the card agree. (v1.7.12 playtest finding I.)</li><li>Fixed: <strong>Session vignettes use the right pronouns.</strong> The opening galley and closing NPC vignettes now hand the narrator each featured character's pronouns — including the absent crewmates the banter is about — so a she/her character is no longer written as \"he\" in the scene. (v1.7.12 playtest finding R.)</li><li>Fixed: <strong>The Roll button matches the move the narrator suggested.</strong> When a narrator card closed with a hint like \"this could be a Gather Information\", the button beneath it sometimes said (and rolled) a different move. The button now reads the move from the narrator's own closing line, so the label, the roll, and the prose agree. (v1.7.12 playtest finding J.)</li><li>Fixed: <strong>The narrator knows whether your ship is docked or underway.</strong> The ship's position line now reads \"docked at Bleakhold Station\" or \"in transit to Tartarus\" instead of the ambiguous \"near …\" — so the narrator can't invent an underway framing when you're docked, or write the ship as stationary when you've set a course. (v1.7.12 playtest finding L.)</li><li>Fixed: <strong>Characters introduced mid-scene are anchored on first mention.</strong> When the narrator names a character or faction who has no entity card — a new contact, a bystander, a criminal cartel, a referenced-but-never-carded name — it now records their identity, role, agenda, and relationship to you in fact-continuity on first mention, so they can't be described differently each turn. (v1.7.12 playtest finding O.)</li><li>Improved: <strong>Fewer throwaway characters.</strong> When a scene needs someone to fill a role — a dockmaster, a pilot, a contact — the narrator now reuses an existing character if one fits and only invents a new one (placed in your current sector) when none can, so you stop collecting one-off names that never return or contradict each other.</li><li>Improved: <strong>Every kind of narration carries the same world context.</strong> Oracle-result narration, the campaign recap, and the Begin and End session vignettes now see the same sector, settlement, and cast context that move narration does, so they stop drifting from your established places and people — and the recap no longer occasionally tacks on a stray line of internal bookkeeping.</li><li>Improved: <strong>The narrator builds on your sector.</strong> When envisioning an inciting incident (and on every narrator turn), the narrator now sees each settlement's Authority and Trouble and your sector's existing NPCs and their roles — so it won't appoint an \"Administrator\" of a settlement your sheet calls lawless, and prefers drawing drama from the people and troubles already in your sector instead of always inventing a new NPC.</li><li>Fixed: <strong>Your campaign's opening premise stays canon.</strong> The inciting incident (who's involved, where, the timeframe, the stakes, the deadline) used to fade from the narrator's memory within a session, so details drifted — a death \"three cycles ago\" could become \"three weeks ago\" — and the recap baked in the wrong version. The opening premise is now recorded as canon the moment you envision it and fed into every narrator turn for the whole campaign, so it can't age out or be overwritten.</li><li>Fixed: <strong>Multiplayer no longer freezes after an asset-effect prompt.</strong> When a move's outcome offered an optional asset effect to resolve (e.g. \"you may take +1 momentum\", or \"choose one\"), that prompt held the table's \"one move at a time\" lock until the GM answered it — so a missed or unanswered pop-up blocked everyone else's narration with \"a move is already being resolved\" and could only be cleared by reloading. The lock now lifts as soon as the move is resolved and narrated, before the optional prompt, so play keeps flowing.</li></ul><h3>v1.7.12</h3><ul><li>Added: <strong>Asset effects apply themselves.</strong> When a move's outcome triggers an asset effect (e.g. +1 momentum on a strong hit, suffer -1 supply, mark progress on a hit), the module applies it for you — optional or 'choose one' effects ask first. A summary card shows what was applied. Effects are read from the asset's text using the GM's Claude key (only the GM needs a key); a GM setting turns it off.</li><li>Fixed: <strong>Quickstart characters use the Starforged sheet.</strong> A character made by ✦ Playtest Quickstart opened with the classic Ironsworn sheet; it now uses the Starforged one, and existing characters and NPC cards are repaired automatically at world load.</li><li>Fixed: <strong>NPC Characteristics no longer show raw HTML.</strong> The seeded oracle details now read as plain text in the Characteristics box.</li><li>Fixed: <strong>Place your ship on the map and it counts.</strong> Dragging your command vehicle onto the sector map now records your position and enables drag-to-travel — previously only an auto-placed token worked. Quickstart also drops the ship on the map for you.</li><li>Fixed: <strong>The sector map camera no longer gets stuck zoomed in.</strong> Sector scenes now have room to pan and a captured \"whole map\" home view, so you can always zoom back out to the overview.</li><li>Added: <strong>NPCs get pronouns.</strong> New NPC cards establish a pronoun set that guides their portrait, the narrator's prose, and their narration voice — so art, text, and audio agree.</li><li>Added: <strong>Gendered NPC voices.</strong> Optional feminine / masculine / neutral NPC voices in Companion Settings → Audio; an NPC's narration picks the voice matching their pronouns (falls back to the default NPC voice).</li><li>Added: <strong>✦ Improve to Strong Hit.</strong> Assets like Fugitive that let you improve a result after the roll now show a button on the move card — it upgrades the outcome, re-narrates, and advances the asset's clock as the cost.</li></ul><h3>v1.7.11</h3><ul><li>Added: <strong>✦ Playtest Quickstart.</strong> A hotbar macro (created automatically for the GM) that sets up a fresh world in one click: rolls all 14 World Truths, generates a random sector, creates a character with the standard stat array and two random Path assets, and creates your command-vehicle starship with two matching Modules. Run it on a fresh world for a clean random slate every time — and turn off sector art/stubs/portraits in Companion Settings for the fastest reset loop.</li><li>Fixed: <strong>Hyphenated surnames behave.</strong> Rolled names could occasionally come out as long hyphen chains or include the literal text \"Roll again (paired name)\"; paired surnames are now always exactly two parts.</li><li>Added: <strong>First Session walkthrough.</strong> A new help page lays out the exact order to stand up a new campaign by hand — API keys, safety, World Truths, character, starship, Sector Creator, finalising entities, Begin Session, Envision Inciting Incident, swear your first vow, first move. (For a one-click version, see ✦ Playtest Quickstart above.)</li><li>Fixed: <strong>The Toolbar Buttons help page now describes the floating toolbar.</strong> It previously described the old scene-controls strip (removed in v1.7.5) and was missing several buttons; it now lists the full button set with who can see each.</li><li>Fixed: <strong>NPC cards open with the Starforged character sheet.</strong> Sector-created NPCs were opening with the classic Ironsworn sheet (Bonds, Banes, Burdens) — which also hid their generated portrait and introduction, because that sheet's Notes tab shows a different field. New NPC cards use the Starforged sheet, and existing ones are repaired automatically at world load (player characters and deliberately re-sheeted cards are untouched).</li><li>Fixed: <strong>Renaming a ship or NPC now renames it everywhere.</strong> Renaming an Actor in the sidebar previously left the old name in the Entities panel and in the narrator's context until a Finalise refreshed it. Renames now apply immediately, and older drift is reconciled at world load.</li><li>Fixed: <strong>Each settlement gets its own star.</strong> Sector maps previously showed the same stellar object on every settlement; each settlement now rolls its own.</li><li>Fixed: <strong>\"Where am I?\" answers from your ship's actual position.</strong> The narrator previously improvised your location from the campaign premise. Now the ship token on the sector map establishes your position: placing or moving it records where you are (dropping it on a settlement pin still offers Set a Course; parking nearby is a free position note), the Inciting Incident records your starting location, <code>!at</code> finds your settlements again, and if no position has been established yet the narrator says so instead of guessing.</li></ul><h3>v1.7.10</h3><ul><li>Added: <strong>The narrator always knows who's speaking (multiplayer).</strong> Select your token (or set your character on your user) and every narration, move, and <code>@scene</code> question is attributed to your PC: the narrator's prompt names the speaker and, with two or more player characters, carries a party roster marking whose turn it is — so the narrator stops mixing up who did what. Selecting a non-character token (your ship, an NPC) is handled safely: attribution falls back to your assigned character.</li></ul><h3>v1.7.9</h3><ul><li>Improved: <strong>Risky travel now offers a roll.</strong> The pacing classifier waved time-critical, hazardous travel through as plain narration (the journey cost nothing). It now recognises movement under stakes — an established hazard or a deadline — and offers Set a Course, Undertake an Expedition, or Face Danger; it also sees the live scene snapshot, so it knows the stakes even when your message doesn't restate them. Category handling is sharper too: working the nav computer mid-crisis is exploration, not downtime.</li><li>Improved: <strong>The map follows the fiction.</strong> When your ship travels in the story — a Set a Course or Finish an Expedition hit, the narrator moving you, or <code>!at</code> — the ship's token on the sector map now moves to the destination automatically (it previously only moved when you dragged it yourself). The narrator is also now required to record ship movement, and destination names like \"Lyra's orbital graveyard\" correctly match the settlement Lyra.</li><li>Added: <strong>⚔ Swear this vow.</strong> The Inciting Incident card's suggested vow is now a button: one click creates the vow on your character sheet, attaches a tension clock when the incident carries a deadline (the narrator proposes the clock and its size), and adds the vow's target NPC to your Connections — carrying the rich opening backstory, filed in the sector's NPC folder, with the usual portrait generation. Already-established names are never duplicated, and a second click can't create a second vow.</li><li>Improved: <strong>The narrator keeps the thread.</strong> A narrator-memory overhaul addressing story drift: the opening Inciting Incident and <code>@scene</code> answers now feed the narrator's working memory (and session recaps); the narrator is required to record each named character's whereabouts and condition, and what is at stake, as binding scene facts; and it maintains a live <strong>scene frame</strong> — where the scene is set, who is present, what is happening — that it sees on every call. Characters in the scene keep their entity records in view even on turns that don't name them.</li><li>Added: <strong>Entity cards on every narration.</strong> Free-form (paced) narration and <code>@scene</code> questions now inject matched entity records into the narrator's context, the way move narration always has.</li><li>Added: <strong>Context cards setting.</strong> Choose how many recent narrator cards feed each narration (1–10, default 3) — raise it if the narrator forgets recent events.</li><li>Added: <strong>Scene frame setting.</strong> The drift-prevention snapshot above can be toggled in Companion Settings → Fact Continuity. Default: on.</li></ul><h3>v1.7.8</h3><ul><li>Added: <strong>Envision an Inciting Incident.</strong> A new way to launch a campaign: the <strong>✦ Envision Inciting Incident</strong> button on the Session panel (and the <code>!incite</code> chat command) rolls an Action + Theme oracle spark and asks the narrator to write your campaign's opening dramatic event — grounded in your world truths, starting sector, local connection, and character — and proposes a starting vow you can swear. Without an AI key it posts the oracle spark on its own as a prompt to envision the incident yourself.</li></ul><h3>v1.7.7</h3><ul><li>Fixed: <strong>NPCs no longer receive player-only effects.</strong> Because NPC cards are character actors, an NPC was picking up things meant only for player characters — most visibly the \"+1 momentum to all players\" when you Began a Session. NPC cards are now kept out of all player-character logic.</li><li>Fixed: <strong>Settlements stay in their sector folder after a reload.</strong> A reload could move a correctly-filed settlement into the \"Unsorted\" folder; it now stays put.</li><li>Changed: <strong>New ships and NPC cards are created blank — populate them with ✦ Finalise.</strong> Ships and NPCs no longer auto-fill at creation, so you can name and set them up first. When you're ready, open the Entities panel and click <strong>✦ Finalise</strong> to roll their oracles, write their Notes / Characteristics, and generate a portrait + token. (This also fixes the NPC portrait not showing at full size in Notes.) The old auto-fill-at-creation behaviour is available via the Auto-Seed Starship / NPC settings, now off by default.</li></ul><h3>v1.7.6</h3><ul><li>Fixed: <strong>The Actors sidebar no longer fills up with empty duplicate sector folders.</strong> Every time you loaded your world, a fresh empty copy of each sector's folder was being created. Folders are now reused correctly, and any empty duplicates already in your world are tidied up automatically on load.</li><li>Added: <strong>NPCs and connections are now character cards, sorted by sector.</strong> The Companion creates <strong>PCs</strong> and <strong>Starships</strong> folders for you, and gives each sector its own <strong>NPCs</strong> folder. The connection from the sector wizard — and any NPC the narrator introduces — now gets a full character card in that sector's NPCs folder instead of a plain journal entry. Your existing connections are converted to cards automatically.</li><li>Added: <strong>New NPC cards fill themselves in.</strong> A new NPC card rolls the character oracles (first look, initial disposition, role, goal) into its Characteristics, writes a short introduction in its Notes tab, and — if you've set an image API key — generates a portrait used for both the card and its token. Turn it off with the new \"Auto-Seed NPC / Connection Details\" setting.</li></ul><h3>v1.7.5</h3><ul><li>Fixed: <strong>The Companion buttons work again — including when no map is open.</strong> The Session, Progress Tracks, Entities, Chronicle, Clocks, and other Companion buttons used to live in Foundry's scene-controls bar (the icons down the left of the map). That bar only works while a scene/map is loaded, so if you play without a map open — common for theater-of-the-mind — clicking the buttons did nothing. The launcher is now a small <strong>floating toolbar</strong> that's always on screen whether or not a map is loaded. Drag it by the ✦ handle to reposition it; it remembers where you put it. Every button opens the same panel it always did, and GM-only tools still only appear for the GM.</li></ul><h3>v1.7.4</h3><ul><li>Fixed: <strong>Attempted to fix the unresponsive Companion buttons.</strong> This release tried to repair the buttons in the scene-controls bar, but they stayed unresponsive whenever no map was open. See v1.7.5 for the working fix.</li></ul><h3>v1.7.2</h3><ul><li>Fixed: <strong>Suffer moves actually fire now — the character sheet finally matches the narrator.</strong> For months, when the narrator described \"you pay a cost\", \"you take harm\", or \"fuel reserves tick down\" your sheet often didn't change at all. Now Endure Harm, Endure Stress, Lose Momentum, Sacrifice Resources, Withstand Damage, and Companion Takes a Hit all actually apply when a move's outcome routes through them. Meters drop, debilities mark when a meter hits 0, and chat cards describe what just changed.</li><li>Added: <strong>A new \"Resolve consequence\" dialog appears when the rulebook asks you to choose.</strong> On weak hits that say \"choose: Sacrifice Resources -1 OR Lose Momentum -2\", on Set a Course's \"one suffer -2 OR two suffers -1 OR complication at destination\", on Endure Harm's \"trade momentum for health\", and other rulebook-mandated choice points — the dialog pops up and you pick. The narrator waits for your choice before posting their card so the prose lands grounded in what you actually took. There's a GM-override button for AFK players and a cancel button if you need to bail out.</li><li>Added: <strong>Pay the Price now writes to your sheet too.</strong> Rolling \"You are harmed\" actually fires Endure Harm. Rolling \"You waste resources\" fires Sacrifice Resources. Rolling \"Your vehicle suffers damage\" fires Withstand Damage. Narrative entries (like \"A trusted ally acts against you\") still hand off to the narrator and GM as before.</li><li>Added: <strong>Set a Course confirms what happened.</strong> When the course resolves to a non-miss, a chat card names the destination (\"Token moved to Hearth. Course held cleanly (+1 momentum).\") so you can see at a glance that the position update fired. Before this, the token-move pipeline was invisible.</li><li>Fixed: <strong>Settlements created by the Sector Creator now get portraits and matching tokens automatically.</strong> Before, every sector spawned a fistful of settlements with the default silhouette — you had to walk the Entity Panel and click ✦ Finalise on each one to get art. Now \"Create Sector\" finalises each settlement at the end of the pipeline, so both the sheet portrait and the canvas token render with the rolled identity. You can opt out under Module Settings → \"Generate Sector Entity Portraits\" if you'd rather not spend on art at that step.</li><li>Fixed: <strong>New starships now get Modules that fit their rolled identity.</strong> Before, a ship rolled as a stealthy hunter bristling with weapons providing medical aid could land with Engine Upgrade and Stealth Tech installed — nothing that matched the prose. The auto-seed step now picks up to three Modules from the rolled type, first look, and mission (so the hunter above arrives with Heavy Cannons, Missile Array, and Medbay), pulled from the Starforged Assets compendium so each module keeps its full ability list. If a ship already has Modules installed, the seeder leaves them alone — nothing is ever overwritten.</li><li>Added: <strong>Envision and History on demand for ships.</strong> The Entities panel detail view for a ship now offers two new buttons next to ✦ Finalise: <em>✦ Envision</em> rolls the supplementary oracles the auto-seed skips (your captain's role, goal, first-look, and given name; Action+Theme pairs for crew flavour and current agenda; an Initial Contact line) and asks the narrator for a short paragraph weaving them in. <em>📜 History</em> rolls a fresh round of Action+Theme backstory beats plus a Story Clue and asks the narrator for a short backstory grounded in your ship's identity. Both also work as chat commands: <code>!ship envision [name] [captain|crew|agenda|contact|all]</code> and <code>!ship history [name] [beats]</code> — either resolves to your command vehicle if you don't name one, or to your lone tracked ship. Each result posts a card to chat AND appends a dated section to the ship's Notes, so subsequent narrator calls pick up the new detail. Falls back gracefully when no Claude API key is configured — you still get the raw oracle rolls.</li></ul><h3>v1.7.1</h3><ul><li>Fixed: <strong>The Starforged Companion sidebar group is fully reachable again.</strong> After the v1.7.0 reorg gave the Companion its own toolbar group, clicking that group icon (or any other group icon while the Companion group was active) silently aborted because of an internal Foundry error during the group transition. Switching groups now works in both directions, and every Companion button (Session, Entities, Chronicle, Private Channel, …) opens its panel as expected.</li><li>Fixed: <strong>Generated names and rolls no longer show \"Roll twice\" or \"Roll again\" as literal text.</strong> Some character and ship oracle rows are instructions to roll again, not final results — connections were ending up named things like <em>Amelia Roll again (paired name)</em> and ship types could read <em>Action + Theme</em>. Rolls now follow those instructions automatically: paired-surname rows produce hyphenated surnames; \"Roll twice\" rows give two combined results; \"Action + Theme\" rows roll Action and Theme together. Existing entries with leaked names will need to be renamed by hand.</li><li>Fixed: <strong>New sectors now organise their entities under the sector's own folder.</strong> Settlements, planets, and locations created by the Sector Creator were all landing in a generic <em>Sectors / Unsorted</em> folder in the Actor sidebar. They now correctly land under <em>Sectors / &lt;Sector Name&gt;</em>. Existing entities already in Unsorted can be dragged to the right folder by hand.</li><li>Fixed: <strong>The \"New Entities Detected\" card no longer prompts about things the game already knows about.</strong> If the narrator mentions your ship by name, or the sector you're currently in, the draft card used to ask whether to add them as new entities every time. The detector now also checks live Actors (starships, settlements, planets, locations) and the sector list before asking, so already-known things stay quiet.</li><li>Fixed: <strong>Sector-rolled details now actually appear on the settlement, planet, and connection sheets.</strong> Several fields were rolled at sector-creation time but vanished between the roll and the sheet: the settlement's <em>Type of settlement</em> dropdown stayed blank, the connection's rolled <em>Goal</em> never made it to the journal page, and the oracle-rolled Population / Authority / Projects / Trouble / Stellar object were nowhere on the settlement sheet body. Each settlement sheet now carries both the atmospheric narrator description AND a compact facts line of the rolled details; the connection sheet now shows its Goal as a labelled line; the settlement and planet sheets now correctly fill in their system dropdowns. Stellar object is now rolled once per sector and shared across all its settlements.</li></ul><h3>v1.7.0</h3><ul><li>Added: <strong>Private Channel.</strong> A new <em>✦ Private Channel</em> button in the Starforged Companion tool group opens a private side window where you can talk with the narrator one-on-one — reflect in character, think through a decision, or ask something quietly. Nothing reaches the main chat unless you choose to <em>Publish</em> a reflection (it then appears as a card in your character's voice). Each player's conversation is saved to their own journal — the GM can read it, other players can't — one page per session. Turn the whole feature off worldwide with the <em>Private Channel</em> setting.</li></ul><h3>v1.6.2</h3><ul><li>Added: <strong>Auto-capture now keeps the small stuff out.</strong> The World Journal (Lore and Threats) and your character Chronicle used to record nearly every passing scene detail — a sensor blip, a description of scorch marks, a moment-to-moment observation — as its own permanent entry. Each channel now rates how durable a beat is and skips fleeting scene texture, so these records fill with facts worth remembering next session instead of minutiae. You can still promote anything you want by hand.</li><li>Added: <strong>Three capture-threshold settings.</strong> <em>Lore Capture Threshold</em>, <em>Threat Capture Threshold</em>, and <em>Chronicle Capture Threshold</em> each choose how much is recorded on that channel — from <em>Everything</em> through <em>Significant and above</em> (the default) to <em>Campaign-defining only</em>. Lower a channel's threshold if you want more captured; raise it for less.</li><li>Added: <strong>A running Session Log.</strong> Fleeting scene beats now append to one running <em>Session Log</em> page per session — a live, scene-by-scene log — instead of each becoming its own permanent Lore or Threat entry. The End-Session summary still lands on that same page as a wrap-up section. Lore and Threats are reserved for the facts that endure.</li><li>Fixed: <strong>Sector settlements now show their description.</strong> Settlements created by the Sector Creator were rendering with an empty sheet body — the generated atmospheric blurb was being written to the wrong place after an internal storage change. They now carry their description on the settlement sheet.</li><li>Added: <strong>Finalise an entity on demand.</strong> Ships, settlements, planets, and points of interest now show a <em>✦ Finalise</em> button in the Entities panel that writes a short, grounded narrator description (and generates a portrait if art is enabled). Once finalised it becomes <em>↻ Regenerate flavour</em>, so you can refresh the description by hand — your finalised text is never overwritten automatically.</li></ul><h3>v1.6.1</h3><ul><li>Updated: the in-game Settings Reference now documents the Audio Narration, Character Management, World Journal, and sector/art settings.</li><li>Updated: refreshed the command reference and the project documentation so every chat command is listed.</li></ul><h3>v1.6.0</h3><ul><li>Added: <strong>Session Panel</strong> — new ▶ Session button on the left toolbar opens a single panel covering all five session moves (Begin, End, Set a Flag, Change Your Fate, Take a Break). A status badge at the top shows whether the session is currently active.</li><li>Added: <strong>Begin Session is now the gate for narration.</strong> Before you press Begin Session, plain typed messages in chat don't trigger the narrator or move pipeline — perfect for setting up before play. Card buttons (X-Card, Confirm draft, Refresh recap) and explicit narration commands (<code>@scene</code>, <code>!oracle yes</code>, <code>!pay-the-price</code>, <code>!lore</code>) all continue to work pre-session. End Session flips the gate back off.</li><li>Added: <strong>Begin Session opens with a galley vignette.</strong> A short atmospheric scene of the active PCs (your GM counts as a player too) hanging out together in the ship's galley, joking absurdly about what the missing crewmates are off doing. Silent skip if your Claude key isn't configured.</li><li>Added: <strong>End Session closes with an NPC vignette.</strong> A short slice-of-life scene featuring a currently-important NPC — a bonded connection, a recurring threat, a faction figurehead — doing something trivial and mundane. The cosmic threat eating a sandwich; the bonded ally watering houseplants.</li></ul><h3>v1.5.5</h3><ul><li>Added: <strong>New <code>!pay-the-price</code> chat command (alias <code>!ptp</code>).</strong> Rolls the d100 Pay the Price table directly so the player or GM can invoke it without going through a move miss. Add an optional question after the command (e.g. <code>!ptp what breaks?</code>) and the chat card echoes it back alongside the d100 result.</li><li>Added: <strong>Oracle chat commands now auto-narrate.</strong> When you roll <code>!oracle yes</code> or <code>!pay-the-price</code>, the raw d100 card is followed by a second card written by your narrator — 2-3 sentences weaving the oracle's answer into the current scene in your configured tone. Silent skip if your Claude API key isn't configured (the raw card still lands as before).</li></ul><h3>v1.5.4</h3><ul><li>Fixed: <strong>Asking the narrator about your character now works.</strong> Questions like <em>@scene who am I</em> used to come back with only your health / spirit / supply / momentum. The narrator now also knows your stats, your paths and their abilities, your background vow and other vows, your connections, any marked impacts, and your character's Biography / Notes — in every narration, not just after a move.</li><li>Fixed: <strong>The narrator now knows your ship.</strong> If your starship carries the Command Vehicle asset it's recognised as your command vehicle automatically — and if you have only one starship, that one is used. The narrator now knows your ship's name, type, and condition (and its location once it's established), so asking \"what is the name of my ship\" gives a real answer instead of an invented one.</li><li>Fixed: <strong>Generated ship and location art now appears on the actor.</strong> AI-generated portraits used to sit only in the Art Cache journal; they're now attached to the actor's portrait and token as well.</li><li>Added: <strong>Narrated starship introductions.</strong> When a new starship is auto-filled from the oracles, its Notes now open with a short atmospheric introduction written in your narrator's voice, followed by a compact summary of the rolled type, first look, and mission. (Requires a Claude API key; falls back to a plain list otherwise.)</li><li>Fixed: <strong>Narrator audio no longer overlaps.</strong> When several narrator cards were spoken in quick succession their audio could pile up and play over itself in a chaotic jumble. Only one narration now plays at a time — starting a new one stops the previous.</li><li>Added: <strong>Stop button on narrator cards.</strong> A ⏹ Stop control sits next to ▶ Play so you can cut off a narration that is running too long. It stops whatever audio is currently playing, even if it started from a different card.</li><li>Fixed: <strong>Sector trouble now reaches the narrator.</strong> The rolled <em>SECTOR TROUBLE</em> entry from sector creation is now recorded as an active threat in your World Journal, so the narrator references it in subsequent narrations. Previously the trouble was only shown in the sector preview and never reached the narrator's context.</li><li>Fixed: <strong>Chat commands no longer match similar-looking typos.</strong> <code>!recap</code>, <code>!sector</code>, and <code>@scene</code> now require a space or end-of-line after the keyword, so look-alikes like <code>!recapify</code>, <code>!sectoral</code>, or <code>@scenery</code> are correctly treated as plain narration instead of being silently re-routed to the wrong handler.</li><li>Fixed: <strong>Truths the narrator captures about a draft entity migrate when you confirm it.</strong> Previously, free-text truths emitted in the narrator's sidecar that referenced an as-yet-unconfirmed name stayed text-bound forever. Confirming the draft now rewrites matching truth entries to point at the new entity record.</li></ul><h3>v1.5.3</h3><ul><li>Fixed: <strong>Adding gear to a ship no longer triggers the narrator.</strong> Adding an asset or module to a starship posts a short italic system note (for example <em>Added Grappler</em>). That note was being mistaken for something your character said — the narrator would reply to it, and with audio narration on it was even read aloud. System notes like these are now ignored by the narrator, along with whispers and dice rolls, so only your actual narration drives the fiction.</li></ul><h3>v1.5.2</h3><ul><li>Fixed: <strong>Chat-card buttons that did nothing on Foundry v13 now work.</strong> The recap card's <em>↻ Refresh</em>, the audio narration <em>▶ Play</em>, the narrative-with-move-suggestion <em>Roll</em> button, the entity-draft <em>Confirm</em> / <em>Dismiss</em> pair, the move-result <em>🔥 Burn Momentum</em>, the per-card <em>Correct a fact</em>, and the setup-card <em>Set World Truths</em> were all silently dead on v13. Foundry deprecated the hook the module was using to wire those buttons in favour of a new one; late v13 builds stopped firing the old name altogether. All seven buttons now wire through both hook names so they work on every supported Foundry version.</li><li>Fixed: <strong>Quench tests no longer leave entities behind in your world.</strong> Running the test suite used to deposit settlement Actors (\"Glimmer\", \"Selena\"), draft connections, pending-lore entries with story-line names (\"Drifter Movement Investigation\"), sector folders, art-cache journals, and threat entries into whatever world you ran them against. Most were unlabelled, so you couldn't tell test residue apart from real campaign content. Each Quench batch now snapshots every world collection before it runs and reaps anything it added when it finishes — Actors, Items, Journals, pages on long-lived journals, Folders, Scenes, Macros, Playlists, Rolltables, Cards, and Messages. A test world stays clean across runs.</li></ul><h3>v1.4.2</h3><ul><li>Added: <strong>Audio narration</strong> — narrator prose can be read aloud by ElevenLabs text-to-speech, with a distinct voice for NPC dialogue. Off by default. Enable it under Companion Settings → Audio (world-level: voices, model, speed) and on each player's client (per-player: enable, volume, autoplay). Paste your ElevenLabs key into the About tab alongside your Claude and OpenRouter keys — it's stored locally per browser, never on Foundry's server. Each narrator card grows a ▶ Play button when you have audio enabled; click to hear that beat. Generated audio is cached at the world level (worlds/{worldId}/audio/…) so re-rolled identical text plays instantly. NPC speech wrapped in <code>&lt;npc&gt;…&lt;/npc&gt;</code> by the narrator gets the secondary voice; the chat card always shows clean prose. Failure (no key, bad key, rate limit, CORS) never blocks chat — the play button just stays hidden or surfaces an error. See the new <em>Audio Narration</em> help page for the full description.</li><li>Fixed: <strong>Narrator-card audio actually plays.</strong> The initial audio implementation shipped two compounding defects that turned every click of the ▶ Play button into an error. (1) The playback wrapper attached an event listener for an event Foundry v13's audio class doesn't recognise, which threw immediately and flipped the button to <em>Unavailable</em> before any audio was requested. (2) On The Forge, uploaded MP3s land in your Forge Assets Library (not on the world's disk), but the cache was storing the wrong path and trying to play back from a location that didn't exist — so every play 404'd and the cache never hit, forcing a fresh ElevenLabs call on every click. Both halves are fixed; cached prose now plays back instantly on Forge.</li><li>Fixed: Running the Quench integration tests no longer leaves narrator cards, momentum-change cards, draft-entity cards, or narrative-review cards behind in chat after the run. Every Quench batch now snapshots the chat log before it starts and sweeps any messages it added — directly or via foundry-ironsworn side effects like momentum-delta cards — when it finishes, so chat looks the same as it did before the run.</li><li>Fixed: The Entities panel's <em>Set as current location</em> button now correctly clears the current location on a second click. Earlier builds got the first click right but the toggle-back step raced against the first click's settle, so the second click sometimes re-set the same location instead of clearing it. (Quench-only — affects the test suite, not normal play.)</li><li>Added: <strong>Stat substitution on asset abilities.</strong> When an enabled ability lets you roll a different stat than the move's listed one (e.g. Empath's <em>\"roll +heart in place of the listed stat\"</em>), the Confirm Move Interpretation dialog now shows a <em>Stat substitution</em> radio row with the offered alternative next to <em>Keep listed</em>. Pick the substitute, accept the dialog, and the move rolls against the swapped stat — no manual re-roll. Numeric +N adds are unaffected; both can stack on the same ability.</li><li>Added: <strong>Ship positioning.</strong> The narrator now knows where the player's command vehicle is. A persistent position record on the ship — sector / nearest planet / nearest settlement, with a free-text fallback — is surfaced as a <em>SHIP POSITION:</em> line in the narrator's prompt and updates from four triggers: <code>!at &lt;name&gt;</code>; a non-miss <em>Set a Course</em> resolution (destination read from the player's narration); a narrator sidecar entry of shape <code>{\"subject\":\"ship\",\"attribute\":\"position\",\"value\":\"&lt;name&gt;\"}</code>; and dragging a command-vehicle Token onto a settlement Note pin in the sector Scene. The Token affordance opens a confirmation dialog and only moves the Token on a non-miss outcome — misses leave the Token in place. Four new world settings gate the feature (master toggle, auto-move on Set a Course, sector-Scene Token affordance, snap radius).</li></ul><h3>v1.4.1</h3><ul><li>Changed: Sector entity Actors (settlements / planets / points-of-interest) are now filed directly under <code>Sectors / &lt;Sector Name&gt;</code> in the Actors sidebar, with no per-type subfolder. Previously every sector had a redundant <em>Settlements</em> / <em>Planets</em> / <em>Locations</em> folder inside it, which made the sidebar's next-level-down read as \"Settlements\" for every sector instead of the sector name. The first GM world-load after this update reparents every existing entity into the new layout and deletes the empty old subfolders. Entities whose sector record can no longer be resolved go into a shared <em>Sectors / Unsorted</em> folder. The same migration is also wired into <code>!migrate-entities</code> so it can be re-run on demand.</li><li>Fixed: Momentum on the character sheet now actually decreases when a move says \"take −1 momentum\". On previous builds the change was silently swallowed for any character whose current momentum was at or below the momentum-reset value (+2 for an unimpacted PC), because the bridge was incorrectly clamping momentum to <em>momentum reset</em> as a floor. Per the play kit, momentum ranges from −6 to +10 during regular play — the reset value is the target that <em>burn momentum</em> sets it to, not a lower bound. The clamp now uses the correct −6 floor; the reset value is still honoured by the burn path.</li><li>Fixed: Confirming or dismissing an entity draft card no longer throws a console error if the card has been deleted in the meantime. The follow-up message update now skips silently when the card is gone, instead of surfacing a \"ChatMessage does not exist\" error and a follow-on Foundry notification crash.</li></ul><h3>v1.4.0</h3><ul><li>Added: <strong>Background Vow</strong> is now surfaced in the narrator's character context. The first vow item on the character is treated as the founding quest (play-kit convention), with other active vows listed beneath it.</li><li>Added: <strong>Take Decisive Action bad-spot downgrade</strong> — when the single active combat track is in a bad spot, a strong hit without a match downgrades to a weak hit and a weak hit downgrades to a miss, per play kit p. 5. Strong-with-match is exempt.</li><li>Added: <strong>Scene Challenge tracks auto-create a paired tension clock.</strong> Creating a scene_challenge track in the panel also creates a 4-segment tension clock with the same name; the panel surfaces the clock state in the track footer.</li><li>Added: <strong>Vehicle Repair point-spend dialog</strong> via <code>!repair</code> — pick situation × outcome, allocate the points across the six spend types, and the dialog applies the mechanical effects (+1 integrity, clear battered, supply trade) directly. Companion-health and module spends are recorded only.</li><li>Added: <strong>Clocks panel</strong> and <strong>Custom Oracles panel</strong> in the toolbar — full UI for both features that previously shipped as chat-command-only.</li></ul><li>Fixed: Momentum reset and impact counting now match the play kit. Earlier builds returned 0 / −1 / −2 for 0 / 1 / 2+ impacts (should be +2 / +1 / 0), and only counted four of the ten impacts toward max momentum — battered, cursed, doomed, tormented, indebted, permanently harmed, and traumatized had no effect on momentum bounds. Both bugs are fixed; the bridge now prefers the vendor system's computed momentumMax / momentumReset getters when present and falls back to a corrected local formula.</li><li>Fixed: Strike and Clash now correctly mark progress twice on strong / weak hits per the play kit. Earlier builds set progressMarked to 0 for all combat outcomes, so the auto-mark path never fired. The field has been redefined as a count of mark-progress operations and the persistence layer multiplies by the track's rank ticks when applying.</li><li>Fixed: Endure Harm and Endure Stress now use the higher of the two stat options ('roll +health or +iron, whichever is higher' / 'roll +spirit or +heart, whichever is higher'). The interpreter's pick is overridden with the higher value at resolution time.</li><li>Added: All eight d100 tables from the play kit are now wired in and auto-roll alongside the relevant move: Pay the Price (any miss for the 18 listed moves), Spotlight Vignette (Begin a Session), Make a Discovery and Confront Chaos (Explore a Waypoint match outcomes plus their own moves), Take Decisive Action weak-hit cost, Mortal Wound (Endure Harm miss), Desolation (Endure Stress miss), and Vehicle Damage (Withstand Damage miss). Results appear on the move card via the existing oracle-seed surface so the narrator picks them up.</li><li>Added: <strong>Ask the Oracle yes/no with odds</strong> via the new <code>!oracle yes [odds] [question]</code> chat command. Odds map to the play-kit thresholds: small ≤ 10, unlikely ≤ 25, 50/50 ≤ 50, likely ≤ 75, certain ≤ 90. On a match (both d10 digits equal, including 100), the card flags 'envision an extreme result or twist'.</li><li>Added: <strong>!bond &lt;rank&gt;</strong> for the bonded-path Develop Your Relationship — rolls +rank (troublesome=+1 … epic=+5) against the challenge dice; strong hit marks 2 ticks on the bonds legacy track, weak hit grants +2 momentum, miss grants nothing.</li><li>Added: <strong>Set a Flag</strong>, <strong>Change Your Fate</strong>, and <strong>Take a Break</strong> dialogs via <code>!flag</code>, <code>!fate</code>, and <code>!break</code> chat commands. Set a Flag captures content to approach mindfully (public or GM-private); Change Your Fate offers the five play-kit options (Reframe / Refocus / Replace / Redirect / Reshape); Take a Break offers Move on (+1 on next non-progress move) or Stop for now (chains into End a Session).</li><li>Added: <strong>Begin a Session</strong> and <strong>End a Session</strong> dialogs via <code>!begin-session</code> and <code>!end-session</code>. Begin a Session lists recent flagged content for review and offers an optional Spotlight Vignette roll (+1 momentum to all players). End a Session captures quest focus / connection focus for next session and awards +1 momentum if either is set.</li><li>Added: <strong>Clocks</strong> management via <code>!clock new | advance | fill | reset | remove | list</code>. Campaign clocks roll their configured odds via Ask the Oracle on <code>!clock advance</code>; tension clocks fill one segment. Multi-word names supported via quotes.</li><li>Added: <strong>Custom oracle tables</strong> via <code>!oracle-add [id]</code> (GM-only). Opens a dialog with a textarea; one line per entry in the form <code>min-max Result</code> or <code>n Result</code>. Tables persist on campaignState.customOracles and re-register at world ready.</li><li>Added: <strong>Combat positioning</strong> (in control / in a bad spot) is now tracked on combat-type progress tracks. Combat moves set the position automatically per the play kit; the Progress Tracks panel surfaces it with a click-to-cycle toggle.</li><li>Added: <strong>Scene Challenge</strong> as a creatable track type in the Progress Tracks panel.</li></ul><h3>v1.3.1</h3><ul><li>Fixed: Move-result chat cards now show your real stat value instead of 0. Every action move was resolving as <em>action die + 0 + adds</em> regardless of your character sheet — visible as <em>+iron (0)</em> on the result card. The pipeline now looks up the named stat (edge / heart / iron / shadow / wits, the player meters, the vehicle integrity meter, and now also companion health from your Companion asset) off the speaking player's character sheet between interpretation and dice resolution. For Companion Takes A Hit with multiple Companion assets, the highest-track companion is used and a notification names which one — re-narrate if you meant a different companion. Solo play was as affected as multiplayer; the bug was easier to spot when two players started comparing their rolls.</li><li>Fixed: Multiplayer — the move-interpretation pipeline now runs on a single client (the GM) instead of every connected client. Previously, in a 2-player session, both clients ran the pipeline independently, which produced duplicate narration cards (one real, one fallback when the player had no API key), red permission-error toasts on the player's screen, and duplicate entity-draft cards. Players still type narration as before; only the GM's client now does the work.</li><li>Fixed: Multiplayer — when a player types narration, the pipeline now resolves THEIR character (via Foundry's User-to-Actor binding, then ownership lookup, then campaign fallback). Previously every narration in a multi-PC session described whichever PC happened to be first in the campaign state regardless of who was typing, and the workaround was to keep selecting your token before each message.</li><li>Fixed: Multiplayer — narration that names another player's character is no longer flagged as a new potential connection. The new-entity check now cross-references the world's player-character actors before proposing a new connection draft.</li><li>Fixed: Push-to-talk button now appears on Foundry v13 chat sidebar when speech input is enabled. The chat-controls container was renamed in v13 (from id <code>#chat-controls</code> to class <code>.chat-controls</code>); the button injection now tries a chain of selectors and emits a console warning if none match, so any further Foundry restructuring is visible rather than silent.</li><li>Fixed: Sector Creator passage drawings now appear, and clicking a settlement or planet pin reliably opens the linked sheet. The earlier fix unblocked the settlement icons but the passage drawings still failed silently against tighter v13 schema validation (the line drawings need every text/fill field set even when they're invisible), and the click handler relied on a Foundry hook that v13 no longer fires reliably. The scene builder now sets every required Drawing field defensively and wraps each batch in its own error path so one batch failing can't take the other down. Pin clicks now work through both the legacy hook and a direct override of Foundry's built-in note double-click — whichever Foundry fires first wins. Settlement and planet pins open the linked Actor sheet on double-click; stellar-object pins remain decorative.</li><li>Fixed: Fallback narrator cards (posted when the Claude API call fails) now carry the same flag shape as success-path narrator cards — sessionId, sessionNumber, narrationText, moveId, outcome, matchedEntityIds, and timestamp. The recap reader and the live-Foundry integration tests both rely on those flags being present on every narrator card; the fallback was missing them and silently broke the recap chain in API-failure scenarios.</li><li>Fixed: Claude API calls now defensively trim whitespace and trailing newlines from the API key before sending. A common copy-paste mistake — a trailing newline from the source application — used to produce an opaque 401 from Anthropic. The console now also prints a one-line hint when a 401 is received, calling out the key prefix and the contrast between Anthropic (<code>sk-ant-</code>) and OpenRouter (<code>sk-or-v1-</code>) key formats. A one-time format warning runs at module load if the stored Claude key obviously isn't an Anthropic key.</li><li>Added: The Confirm Move Interpretation dialog now scans your character's enabled assets — paths, companions, deeds, support vehicles — and your command vehicle's modules for abilities that apply to the chosen move. Matches surface as opt-in checkboxes with a brief summary; checked items contribute their <code>+N</code> add to the roll. Detection runs structured-first (ironsworn's <code>@Compendium[…]{MoveName}</code> links inside ability text are unambiguous; if any are found, no API call is made), with a Haiku fallback only when no structured match exists — typical implicit phrasing like \"When you make moves to attack…\" is caught by the fallback. Numeric adds are extracted from the ability text via regex (<em>\"add +1\"</em> / <em>\"+2 add\"</em>); abilities that surface without a clean numeric add still appear in the list with their text so you can decide what to do.</li><li>Fixed: Weak-hit and strong-hit moves that say <em>\"Take +N momentum\"</em> now actually update the momentum on your character sheet. Earlier builds tried to look up the active character via a campaign-state field that nothing in the module ever wrote — so in solo-GM mode the lookup quietly returned no character and every meter change, debility change, and progress mark from a move silently no-op'd. Same shape of defect as the v1.2.12 recap fix. Persistence now falls back to the first player-owned character (or the first character actor in solo-GM mode where <em>hasPlayerOwner</em> is uniformly false), matching the pattern the recap pipeline and the assembler use.</li><li>Added: <strong>🔥 Burn Momentum</strong> button on move-result chat cards. Whenever burning your current momentum would actually improve the outcome (miss → hit, weak → strong), the move card now grows a button showing the preview — for example <em>\"🔥 Burn Momentum (8 → Strong Hit)\"</em>. Click it to upgrade: the action score is replaced with your current momentum, your momentum resets to its impact-adjusted value, the new outcome's consequences apply (including reversing any meter changes from the original outcome), and the narrator is re-run on the upgraded outcome. The button hides itself for progress moves (rules: cannot burn there), already-strong hits, and cases where burning wouldn't change the result.</li><li>Fixed: Clicking the <strong>Roll &lt;move&gt;</strong> suggestion button at the end of a narrative-with-move-suggestion card now actually runs the move the classifier nominated. The button previously re-posted your original input with the pacing classifier turned off, and the move interpreter would re-classify from scratch — usually picking a different move because the original input read differently without pacing context (an input that the classifier called <em>make_a_connection</em> based on the surrounding scene might re-classify as <em>gather_information</em> when read in isolation). The suggested move ID now travels with the re-posted message and the pipeline honours it directly, skipping the LLM interpretation entirely.</li><li>Fixed: Auto-created Connections are now registered on the active character as ironsworn <em>progress</em> Items with subtype <em>bond</em> — they appear on the character sheet's Connections tab with their own progress track instead of only living in the entity panel. The same wiring applies whether the connection came from a successful Make a Connection roll or from clicking <strong>Confirm</strong> on a detector draft card. Vows added through the Progress Tracks panel are mirrored the same way (subtype <em>vow</em>, lands in the Progress tab).</li><li>Fixed: New non-command Starship Actors now arrive with a system starship token as their portrait/token art instead of the generic Foundry actor silhouette. The icon is picked deterministically from the foundry-ironsworn <code>Starforged-Starship-Token-NN.webp</code> set, so the same ship always renders with the same token. Command vehicles continue to defer to the art pipeline / the STARSHIP asset.</li><li>Fixed: The orphan <code>Art: ship &lt;id&gt; &lt;id&gt;</code> journal entries the art pipeline writes are now filed under a dedicated <em>Starforged Companion — Art Cache</em> folder so they no longer appear in the Starforged Entities folder mixed in with real entity records. The pages now also render the generated portrait when opened, rather than showing an empty <em>Asset Data</em> heading — the image was always stored in a page flag but never surfaced in the visible page body.</li><li>Fixed: The narrator now sees the character's <strong>Paths &amp; Assets</strong> in its system-prompt context. Earlier builds read stats, meters, debilities, and XP from the character actor but skipped the actor's asset Items entirely, so a character with the Firebrand and Scoundrel paths might as well not have had them — the narrator couldn't reference the path abilities in scene suggestions or in narration. The enabled abilities of each asset are now included in the CHARACTER STATE section of the prompt.</li><li>Fixed: When the module creates an ironsworn Item on a character (the new bond / vow registration above), the create call passes <code>suppressLog: true</code> so the system's <em>preCreateItem</em> chat-alert hook does not emit an <em>Added &lt;name&gt;</em> emote message in chat. Manual additions from the character sheet are unaffected — those remain controlled by the system's <strong>Log Changes</strong> setting.</li></ul><h3>v1.3.0</h3><ul><li>Added: <strong>Fact Continuity</strong> — the narrator now records binding scene truths and current state as it writes, and feeds those entries back into subsequent narrator calls. The next response cannot quietly contradict something said three turns ago. See the new <em>Fact Continuity</em> page for the full feature description, including the two ledgers, the scope filter, the scene lifecycle, the correction affordance, and the optional consistency-check audit pass.</li><li>Added: <code>!scene start</code> and <code>!scene end</code> chat commands (GM only). Every <code>@scene</code> also starts a fresh scene automatically; the closeWorld hook flushes the active scene at session end.</li><li>Added: Per-narrator-card <strong>Correct a fact</strong> button. Opens a DialogV2 listing every in-scope truth and state value, with per-row Strike / Replace actions. GM-asserted truths are protected from player strikes; everything else is correctable by either actor.</li><li>Added: <code>!truth strike</code>, <code>!truth set</code>, <code>!state strike</code>, <code>!state set</code> chat commands as keyboard / accessibility parity with the correction dialog.</li><li>Added: Optional <strong>consistency check</strong> — a Haiku audit pass after every narration that flags prose contradicting the active-scene ledger. High-confidence findings extend the existing GM-only Narrative Review card with a <em>Retract the offending fact</em> button. Off by default; enable in Companion Settings → Mischief → Fact Continuity.</li><li>Added: Five new world settings — Enable fact continuity, Inject the active-scene ledger, Require a sidecar, Max ledger tokens, Consistency check (experimental). Default values match the scope document.</li></ul><h3>v1.2.13</h3><ul><li>Added: When you create a new starship from the Actors sidebar, the module now auto-rolls the Starforged starship oracle tables (type, first look, mission for your active sector's region) and writes the results to the Notes tab of the starship sheet. A portrait is also generated silently if your OpenRouter API key is set. You can keep typing the name and the module won't overwrite it; if you typed anything into the Notes field before clicking Create, the auto-seed sees that and skips. New world setting <strong>Auto-Seed Starship Details</strong> in Companion Settings (default on) — turn it off to keep new starships blank.</li><li>Fixed: Auto-created NPCs from a successful <em>Make a Connection</em> roll now arrive with real detail instead of a near-empty journal entry. The oracle tables for role, goal, first look, and given name were already rolling during move resolution — but the rolls only fed the narrator prompt; the connection's journal record received only a name and the detector's surface description. Role, motivation, and the first-look details are now persisted on the connection itself, so reopening the entity panel after the roll shows a usable character sketch rather than a blank one. A portrait is also generated silently at creation time, gated on your OpenRouter API key being configured (no notification, no portrait if the key is missing). None of this new detail enters the chat — the narrator card and the detection draft card stay as-is; the seeded structure lives only on the entity record.</li><li>Fixed: When the detector proposes a connection or ship from another discovery move and you click <strong>Confirm</strong> on the chat-card row, the create path now rolls fresh oracle entries (role / goal / first look / given name for connections; type / first look / name for ships) and applies the same enrichment, including the silent portrait generation. Other entity types (settlement, faction, planet, location, creature) keep their existing minimal create path.</li><li>Internal: Two new Quench batches — <em>STARFORGED: Connection Seed Enrichment</em> and <em>STARFORGED: Starship Seed Hook</em> — exercise the new paths against real journals and actors so they can't silently regress.</li></ul><h3>v1.2.12</h3><ul><li>Fixed: <code>!recap</code> now actually works. v1.2.4 corrected the read path, v1.2.7 added automatic chronicle writes — but both halves silently no-op'd in practice because the internal field they both read to identify your player character was never populated. Auto-write picked \"no character\" and skipped; the recap reader saw an empty list and posted \"No campaign history available yet\". Both halves now look up your player character through the same path the rest of the module uses, so chronicle entries get written after every narration and <code>!recap</code> returns a real summary. The empty-state card now means \"you genuinely haven't played any narrated turns yet\" rather than \"silently broken\".</li><li>Internal: New Quench batch <em>STARFORGED: Recap End-to-End</em> exercises the writer and reader against a real Actor and real Chronicle journal with the bug condition forced, so this can't silently regress again.</li></ul><h3>v1.2.11</h3><ul><li>Fixed: Pacing classifier now reliably picks MOVE at high dials. On earlier builds, cranking every dial to 9 still routed clear character actions (\"I reach into the system\", \"I grab the device\") to <em>narrative with a move suggestion</em> instead of running the move. The classifier's instruction set has been retuned: the 9–10 rule is now described behaviourally (\"classify as MOVE whenever the player describes a character action — reaching, grabbing, hacking, pressing\"), a tie-break rule forces MOVE when a candidate move is identified at dial 9–10, and the cautious \"outcome not in doubt\" default is restricted to dial 0–5 so it can't override a high dial. High dials feel high again.</li><li>Added: <strong>Roll</strong> button on narrative-with-move-suggestion cards. When the classifier ends a paced narration with an italicized hint like <em>\"Pressing further here would be a Compel\"</em>, the card now also offers a one-click button to run that move on the original input. Cheaper than retyping; reads cleanly as a player-driven escalation rather than a re-roll. The button disables itself after use so the same card can't fire twice.</li></ul><h3>v1.2.10</h3><ul><li>Changed: Sector-record journal overviews are now self-updating. Rename a settlement Actor in the sidebar (or change its location-class / population / authority) and the sector page's settlement list re-renders automatically, debounced ~500 ms — no more manually regenerating the sector page to keep names in sync. Meter ticks, debility flips, and other non-displayed field changes are ignored so the rewrite only fires when the overview would actually change.</li><li>Changed: <code>!migrate-entities</code> now also rewrites the existing sector-record overview pages to use Foundry document links (<code>@UUID[Actor.…]</code>) and deletes the per-settlement embedded pages those overviews used to carry. The narrator stub on the overview is preserved by surgical replacement. Idempotent — re-running on a world that's already migrated is a no-op.</li></ul><h3>v1.2.9</h3><ul><li>Changed: Settlements, planets, and locations-of-interest are now native foundry-ironsworn <em>location</em> Actors (with the system's <code>subtype</code> field distinguishing the three). They live under <em>Actors / Sectors / &lt;Sector Name&gt; / Settlements</em> (or <em>Planets</em> or <em>Locations</em>), so the per-sector hierarchy is visible in the sidebar and the Actors are drag-droppable onto scenes as tokens. The sector-record JournalEntry's settlement list now uses Foundry document links to each Actor, so renaming a settlement in the sidebar updates the sector page automatically (debounced live-refresh hook ships separately). Existing campaigns: run <code>!migrate-entities</code> to convert legacy settlement/planet/location journals — same idempotent behaviour and 7-day grace window as the ship migration.</li><li>Removed: The pre-migration <em>Starforged Sectors</em> JournalEntry flag store. Nothing read it post-migration; <code>!migrate-entities</code> deletes the orphan journal automatically.</li><li>Removed: The per-settlement embedded JournalEntryPages inside each sector-record journal. The Actor is the single source of truth for settlement detail; the sector journal keeps only the overview page with document links to its settlement Actors.</li></ul><h3>v1.2.8</h3><ul><li>Changed: Ships are now native foundry-ironsworn Starship Actors instead of custom journal entries. Battered and Cursed render on the system sheet with the system's own widgets; you can drop a starship Actor onto a Scene as a token; the sidebar Actors tab groups them under a new <em>Starships</em> folder. Existing campaigns can run <code>!migrate-entities</code> (GM only) to convert legacy ship journals — the command is idempotent and tags the legacy journals so you can verify before removing them with <code>!migrate-entities --cleanup</code> after at least 7 days.</li><li>Internal: New entity-registry layer hides the journal-vs-actor split from the entity panel, art pipeline, and assembler. Adding the migration's later phases (planet / settlement / location → Actor) won't require touching those call sites.</li></ul><h3>v1.2.7</h3><ul><li>Added: Automatic Chronicle entries. After each narration — both move resolutions and paced narration — the module now writes a one-sentence narrative beat to your character's Chronicle (\"revelation\", \"relationship\", \"scar\", \"discovery\", or \"moment\"). Earlier builds left the Chronicle entirely manual, which meant <code>!recap</code> always reported \"No campaign history available yet\" no matter how long you played — there was nothing in the Chronicle to summarise. Auto-entry is gated on the existing <strong>Chronicle Auto-Entry</strong> setting (default on); turn it off if you prefer to curate by hand. GM-only on the writer side; player clients no-op silently while the GM client picks the entries up.</li><li>Fixed: The pacing dials now actually decide whether a move is invoked. On earlier builds the Mischief dial was rendered inside the same classifier call and outranked the pacing dials — so with the default Balanced Mischief posture you could crank every pacing dial to 9 and pressing a connection for information would still route to \"narrative with a move suggestion\" instead of running the move. The classifier now only decides <em>whether</em> a move is invoked, driven entirely by the pacing dial value; the Mischief dial still decides <em>which</em> move and how strictly to interpret it, downstream. High pacing dials feel high again. The post-narration entity-detection pass that previously skipped on Lawful as a token-saving optimisation now runs regardless of Mischief — detection isn't a \"should this be a move?\" decision.</li><li>Fixed: The Entities panel now actually shows your tracked entities. Previously it always read \"No entities tracked yet\" no matter how many connections, ships, settlements, factions, planets, locations, or creatures you had — the panel was looking for entity data in the wrong place inside Foundry's journal. Bug was present from the day the panel shipped; everyone running the module has been affected.</li><li>Fixed: The \"New Entities Detected\" chat card now has working <strong>Confirm</strong> and <strong>Dismiss</strong> buttons next to each draft, replacing the old \"Open the Entities panel to confirm or dismiss\" hint that pointed nowhere. Confirm creates the entity and adds it to your Entities panel; Dismiss tells the detector to stop suggesting that name. Buttons are GM-only. Previously, only Make a Connection automatically created NPCs — drafts of any other type (ships, settlements, planets, etc.) had no way to become real entities at all.</li><li>Fixed: The <strong>↻ Refresh</strong> button on the Campaign Recap chat card now works. Earlier builds rendered the button for the GM but never wired it up — clicks did nothing. The button now forces a fresh recap, bypassing the cache (the same effect as running <code>!recap</code> after writing new chronicle entries).</li><li>Fixed: The narrator no longer invents new settlement names for places that already exist in the active sector. Paced narration and scene queries used to have zero sector context, so the narrator could decide your character was in some never-before-seen station even when the sector had an obvious hub. The narrator now sees an \"Active Sector\" block listing established settlement names with an explicit instruction to reuse them, and the detector now does cross-type dedup so a name that's already a Settlement won't be re-proposed as a Location (or vice versa).</li></ul><h3>v1.2.6</h3><ul><li>Added: The in-game help journal is now also published as a public website on GitHub Pages, rebuilt automatically on every push to <code>main</code> and on every release. You can link directly to a help page from outside Foundry — same content as the in-game journal, no module install required. Source of truth remains the in-game help; the website is a generated mirror.</li></ul><h3>v1.2.5</h3><ul><li>Fixed: The narrator no longer occasionally writes prose about \"HTML\", \"records\", \"contradictions\", or other module mechanics as if they were happening in the fiction. The root cause: a handful of cards the module posts to chat (the empty-state recap card, the empty <code>!sector list</code> card) were missing the internal flag that marks them as module-generated. Without that flag, the chat hook treated their HTML body as if the player had typed it, and the narrator's next response would riff on the markup it just received. Every module card now carries a flag, and the filter has an umbrella guard so any future card type is automatically excluded.</li><li>Fixed: Belt-and-braces — the narrator now strips HTML tags and entities from player input before composing its prompt, so even if a card somehow slips past the filter the narrator sees plain prose.</li></ul><h3>v1.2.4</h3><ul><li>Fixed: <code>!recap</code> (and <code>!recap campaign</code>) now actually reads your character chronicle. Earlier builds always showed the empty-state card — <em>\"No campaign history available yet. Play some sessions first!\"</em> — even after dozens of chronicle entries had been written, because the recap helpers were looking up your character ID in the wrong place. The recap now aggregates entries across every player character in the campaign, sorted chronologically.</li><li>Fixed: The narrator stops re-introducing the same NPC every paced-narrative turn. Two things were broken: stored entity names with honorifics (\"Dr. Chen\") didn't match the bare form the narrator extracted from prose (\"Chen\"), so the detector treated them as different people; and unconfirmed draft cards weren't tracked, so each subsequent narration that mentioned the same NPC re-posted the same draft. Honorifics are now stripped during comparison, and pending drafts are now respected — once an NPC is sitting in a draft card you haven't actioned yet, the detector won't post the same draft again.</li><li>Fixed: The narrator no longer leaks module mechanics into its prose. Earlier builds occasionally wrote sentences like <em>\"she's now in your records\"</em> or referenced contradictions and entities panels as if they were in-fiction. The narrator prompt had a line — \"Any entity you name will be captured for the campaign record\" — that was meant as implementation guidance and is now removed, replaced with an explicit \"speak only as the fiction\" rule applied to every narration mode.</li><li>Fixed: The narrator's character context (your character's name and current meter values) was being read from the wrong place, so paced narration often had no idea who the player character was. The narrator now reads directly from your foundry-ironsworn Actor.</li></ul><h3>v1.2.3</h3><ul><li>Added: The narrator now captures named NPCs, factions, locations, and lore beats that appear in pure-narrative turns. Previously, when the pacing classifier decided your input should stay as narration (no roll), any character or place the narrator named in their prose vanished from the campaign record. The narrator now runs the same entity-detection pass it already used for moves, just after the chat card posts, and surfaces anything it found as a GM-whispered draft card with Accept / Dismiss buttons — the same review surface the move pipeline uses for non–Make-a-Connection discoveries. Make a Connection's auto-create behaviour on a hit is unchanged. Detection only runs on Balanced and Chaotic Mischief dials; Lawful tables (strict posture) pay zero added cost and see no draft cards from paced narration.</li></ul><h3>v1.2.2</h3><ul><li>Changed: The pacing classifier now follows the Mischief dial. On <strong>Lawful</strong>, it still waits for explicit phrases like \"I try to\" or \"I attempt to\" before treating an input as a move. On <strong>Balanced</strong> (default), it reads commitment from what you depict — pressing a connection on intent or stakes counts as a move; pure scene-setting and reaction stays narrative. On <strong>Chaotic</strong>, it leans aggressively toward rolling whenever the fiction could plausibly support it. Most opening-scene play that used to fall into narrative-with-suggestion will now fire a real move on Balanced.</li><li>Changed: Default Social and Exploration pacing dials nudged up — Social 3→5, Exploration 5→6. If you already tuned your dials, your values are preserved. Combat, Investigation, and Downtime defaults are unchanged.</li></ul><h3>v1.2.1</h3><ul><li>Fixed: The narrator no longer pads atmospheric narration with in-prose suggestions like <em>\"you could make her a connection\"</em> or <em>\"you could push through\"</em>. Earlier builds inherited a move-resolution role description on every narration call (including paced narration and scene queries, where no move outcome exists) and lacked an explicit \"depict, do not offer\" rule — so the model leaned into commenting on possibilities instead of committing to them. The role description is now matched to the call site, and an anti-suggestion rule applies to every narration mode. The optional closing italic move-hint at the end of a paced-narrative turn is preserved as the one allowed exception.</li></ul><h3>v1.2.0</h3><ul><li>Fixed: After a successful <em>Make a Connection</em> roll, the auto-created NPC journal entry now reliably appears before the move is considered finished. The connection write was previously fire-and-forget; anything that read the connection list right after the narration card could miss it. Also surfaced by the v1.2.0 Forge tests as a failing assertion in the Connection Pipeline batch.</li><li>Fixed: Regenerating an entity portrait now keeps the original portrait record around with a \"superseded\" flag instead of silently overwriting it. The storage layer used to name both the original and the regenerated portrait the same way, so the second write replaced the first; the locked portrait you see in chat is unchanged, but the history behind it is preserved correctly. Surfaced by the v1.2.0 Forge tests as a failing assertion in the Portrait Generation batch.</li></ul><h3>v1.1.2</h3><ul><li>Internal: Integration test coverage for the narrator-creates-a-connection-NPC flow has been expanded. The full chain — narrator call, connection auto-creation, entity card storage, portrait generation, and regenerate-and-lock — is now exercised end-to-end both deterministically (with stubbed API responses) and against the real Anthropic and OpenRouter APIs when keys are configured. No visible behaviour changes.</li></ul><h3>v1.1.1</h3><ul><li>Fixed: Sector Creator narrator stubs now generate noticeably faster. The sector prologue and per-settlement stub calls used to run one after another; they now run together. A fresh sector that previously took the full length of three sequential Claude calls now finishes in roughly the length of one, and the related integration test no longer trips its time limit on The Forge.</li></ul><h3>v1.1.0</h3><ul><li>Added: <strong>Pacing classifier</strong> — a small Haiku pre-classifier sits between your chat input and the move pipeline. It decides whether each undecorated input should trigger a move, be handled as pure narration, or end with an inline move suggestion. Per-category dials (Combat, Investigation, Exploration, Social, Downtime) tune the feel. Master switch in Companion Settings → Mischief → Pacing Classifier.</li><li>Added: <code>!pace hot</code> / <code>!pace quiet</code> / <code>!pace clear</code> / <code>!pace status</code> — GM commands to shift pacing for the current scene without changing world defaults.</li><li>Added: <code>!roll</code> — force the next undecorated input through the move pipeline, bypassing the classifier. Use as a false-negative recovery.</li><li>Added: Pacing telemetry journal — every classifier decision is recorded under <em>Pacing Telemetry</em> in the Starforged Companion folder so you can tune dials from real play data.</li></ul><h3>v1.0.1</h3><ul><li>Fixed: <code>!lore</code> now works after the World Truths dialog has been re-run or its journal has been deleted and recreated. The reader used to give up with \"World Truths content could not be read\" if the stored journal id no longer pointed at an existing document; it now looks up the \"Setting Truths\" journal by name as a fallback, picks the entry with real truth content if duplicates exist, and repairs the stored id so subsequent calls are fast again.</li><li>Fixed: Sector creation no longer scatters settlement, connection, and planet journal entries across the root of the Journals tab. New entity records now file under a dedicated <strong>\"Starforged Entities\"</strong> folder. Existing top-level entries from previous sectors are left alone — drag them into the new folder or delete them as you prefer.</li><li>Fixed: Running the Quench integration tests no longer leaves a <code>QUENCH PROBE …</code> entry in the Companion Settings → Safety → Lines list. The probe is now restored cleanly when the test finishes.</li><li>Added: In-game help now documents the previously-missing chat commands — <code>!truths</code>, <code>!lore</code>, <code>!at Name</code> / <code>!at</code>, <code>!sector list</code> / <code>!sector new</code> / <code>!sector Name</code>, and <code>!sfc encounter Name</code>.</li></ul><h3>v0.6.6</h3><ul><li>Removed: Local Claude API proxy. All external API calls now go directly from the Foundry renderer — Claude via Anthropic's client-side opt-in, image generation via OpenRouter (which supports browser CORS natively). No more <code>npm run proxy</code>, no setup difference between desktop and The Forge.</li><li>Changed: Art generation unified on OpenRouter. The DALL-E (OpenAI) backend has been removed since it required the local proxy. Default image model is FLUX.2 Pro via OpenRouter, configurable to any OpenRouter image model.</li><li>Removed: Settings <code>artApiKey</code>, <code>artBackend</code>, and <code>claudeProxyUrl</code> are no longer registered. Existing stored values are harmless; the OpenAI key input is gone from the About pane.</li></ul><h3>v0.6.5</h3><ul><li>Fixed: Claude API calls now succeed on The Forge. Previously the module routed Forge traffic through a non-existent <code>ForgeAPI.call(\"proxy\", ...)</code> verb; it now uses Anthropic's documented client-side opt-in instead. Desktop continued to use the local proxy in this release as a make-before-break step.</li><li>Added: OpenRouter image-generation backend — selectable in module settings, default on The Forge. Supports FLUX.2 Pro (default) and other OpenRouter image models. Requires a separate OpenRouter API key, configurable in Companion Settings → About.</li><li>Changed: Art Generation Backend setting now offers OpenRouter (Forge-friendly) or DALL-E (desktop only); the Replicate / fal.ai entries that were never wired up have been removed.</li></ul><h3>v0.6.4</h3><ul><li>Fixed: Confirm Move Interpretation dialog accept and reject paths are more responsive and no longer hang under fast back-to-back use. The dialog now waits for its window to be fully drawn before reporting the prompt as ready.</li><li>Fixed: Sector Creator now reliably saves a freshly generated sector. Previously the active sector and its settlement / connection links could be lost if multiple internal saves landed in quick succession; the wizard now performs a single save once the sector and all its entities have been built.</li></ul><h3>v0.6.3</h3><ul><li>Fixed: Confirm Move Interpretation dialog now resolves Accept reliably. After the first move in a session a stale dialog id could let the gate flag persist, so a later Accept click silently did nothing and the move stalled.</li><li>Fixed: <code>!journal lore \"title\" confirmed — text</code> now consistently writes the entry before any follow-up read sees it.</li></ul><h3>v0.6.2</h3><ul><li>Fixed: Removing a progress track from the Progress Tracks panel now reliably persists the deletion. The handler previously raced with the journal write, leaving the track in storage even after the confirm dialog was accepted.</li><li>Changed: <code>@scene</code> queries now always post a card. When scene queries are disabled, the X-Card is active, no API key is configured, or the API call fails, the chat now shows a brief reason instead of silently doing nothing.</li></ul><h3>v0.6.1</h3><ul><li>Fixed: Chat command examples corrected to use the <code>!</code> prefix — <code>!x</code>, <code>!recap</code>, <code>!journal</code> — matching the actual command prefix used in Foundry v13</li><li>Fixed: Help journal now updates automatically when the module updates its content — a world reload is all that is required</li></ul><h3>v0.6.0</h3><ul><li>Added: <code>!recap session</code> posts a session summary of all narrated moves — no API call required</li><li>Added: <code>!recap</code> generates a 3–5 paragraph campaign arc summary via Claude</li><li>Added: Campaign recap is cached and only regenerates when new chronicle entries are added</li><li>Added: Auto-recap posts to chat automatically at the start of each new session</li><li>Added: Campaign recap injected into the narrator context on the first narration of each session</li><li>Added: Three new settings — Auto Recap, Session Gap Threshold, Recap GM-Only</li></ul><h3>v0.5.0</h3><ul><li>Added: Scene interrogation — type <code>@scene your question</code> to ask the narrator about the current scene without triggering a move</li><li>Added: Scene card appears in chat with question visible and narrator response below</li><li>Added: Scene queries are suppressed when the X-Card is active</li><li>Added: Three new settings: Scene Interrogation Enabled, Scene Response Length, Scene Context Cards</li></ul><h3>v0.4.0</h3><ul><li>Added: Session ID management — each world load generates or restores a session ID; resumes existing session if last session was less than 4 hours ago</li><li>Added: Session number increments automatically on each new session</li><li>Added: Narrator cards now carry session ID, session number, move ID, outcome, and narration text as flags — enables upcoming recap and scene interrogation features</li><li>Added: About tab in Companion Settings shows current session number, session ID, and session start time</li></ul><h3>v0.3.0</h3><ul><li>Added: Character management — move resolutions now automatically update health, spirit, momentum, debilities, and XP on the foundry-ironsworn character sheet</li><li>Added: Character Chronicle — reverse-chronological story record with player annotations; accessible via the 📖 toolbar button</li><li>Added: Starship damage tracking — Battered and Cursed impacts applied to the starship Actor automatically</li><li>Added: Momentum burn handled automatically when action die matches current momentum</li></ul><h3>v0.2.0</h3><ul><li>Added: Direct Claude narration replacing Loremaster — works from any player account, no GM dependency</li><li>Added: Narrator tab in Companion Settings — configure model (Haiku/Sonnet), perspective, tone, length, and custom instructions</li><li>Added: Auto perspective — second person for solo campaigns, third person for multiplayer</li><li>Added: Default tone: wry (knowing, slightly sardonic)</li><li>Added: Prompt caching for narration system prompt — significantly reduces per-session cost</li><li>Fixed: ChatMessage type \"other\" removed (not valid in Foundry v13)</li><li>Fixed: message.author deprecation warning in v13</li><li>Fixed: Meter persistence now correctly gated to GM account</li></ul><h3>v0.1.x — Deployment hardening</h3><ul><li>Fixed: CORS — local Node.js proxy added; The Forge server-side proxy supported</li><li>Fixed: Foundry v13 compatibility (ApplicationV2, DOM API, string literal message types)</li><li>Fixed: World truths, progress tracks, and X-Card suppression now working correctly</li><li>Fixed: Safety configuration now correctly reaches the narrator context packet</li><li>Fixed: Mischief dial \"lawful\" value now recognised correctly</li><li>Fixed: CI release — module.json version and URLs updated before zip build</li></ul><h3>v0.1.0 — Initial release</h3><ul><li>Move interpretation via Claude Haiku with prompt caching</li><li>Move confirmation dialog, dice resolution, mischief dial</li><li>Safety system (Lines, Veils, Private Lines, X-Card)</li><li>Progress tracks, entity panel, portrait generation (DALL-E 3)</li><li>World Truths generator, oracle tables, push-to-talk speech input</li><li>Foundry v13 compatibility</li></ul>", format: 1 },
  },
];

export async function ensureHelpJournal() {
  if (!game.user.isGM) return;

  const pages = PAGES.map(p => ({
    name: p.name,
    type: "text",
    sort: p.sort,
    text: { content: p.text.content, format: 1 },
  }));

  const existing = game.journal.getName(JOURNAL_NAME);

  if (existing) {
    const storedVersion = existing.getFlag("starforged-companion", "contentVersion");
    if (storedVersion === CONTENT_VERSION) return;
    const pageIds = existing.pages.map(p => p.id);
    await existing.deleteEmbeddedDocuments("JournalEntryPage", pageIds);
    await existing.createEmbeddedDocuments("JournalEntryPage", pages);
    await existing.setFlag("starforged-companion", "contentVersion", CONTENT_VERSION);
    return;
  }

  const journal = await JournalEntry.create({
    name: JOURNAL_NAME,
    ownership: { default: CONST.DOCUMENT_OWNERSHIP_LEVELS.OBSERVER },
  });
  await journal.createEmbeddedDocuments("JournalEntryPage", pages);
  await journal.setFlag("starforged-companion", "contentVersion", CONTENT_VERSION);
}
