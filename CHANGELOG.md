# Changelog

All notable changes to Starforged Companion are documented here.

---

## [Unreleased]

- Added: **Pacing classifier** — a small Haiku call between chat input and the move pipeline. Decides whether each undecorated input should run the move pipeline, narrate without rolling, or narrate with an inline move suggestion. Per-category dials (Combat 9, Investigation 6, Exploration 5, Social 3, Downtime 1) tune the feel. Master switch in Companion Settings → Mischief → Pacing Classifier; module-level enable defaults to `true`. New module: `src/pacing/{classifier,router,telemetry}.js`. New schema field: `CampaignStateSchema.pacing = { sceneOverride, forceNextAsMove }`. The classifier routes through `src/api-proxy.js` (the same Anthropic-direct path used by the move interpreter and narrator); the system prompt is cached per Anthropic's `prompt-caching-2024-07-31` beta. Approximate per-call cost ~$0.0006 with cache hits.
- Added: **`!pace` chat command** — `!pace hot` shifts every dial +3 for the current scene, `!pace quiet` shifts -3, `!pace clear` removes the override, `!pace status` shows current effective dials. GM-only (writes world-scoped state). Override clears on `!pace clear`, the next `@scene` (the in-memory recent-density buffer also resets there), or session end.
- Added: **`!roll` chat command** — false-negative recovery. Sets `campaignState.pacing.forceNextAsMove`; the next undecorated input routes straight to the move interpreter, bypassing the classifier and clearing the flag. GM-only.
- Added: **Narrator-only response card** (`narratePacedInput` in `src/narration/narrator.js`, `buildPacedNarrativeUserMessage` in `src/narration/narratorPrompt.js`). Used by the pacing router's NARRATIVE branches. Posts an `sf-narration-card--paced` card with `pacedNarrative: true` flag. When the classifier returned `NARRATIVE_WITH_MOVE_AVAILABLE`, the narrator is instructed to end with one italicized line nominating the suggested move; an additional `sf-narration-card--with-suggestion` class is applied so themes can style it.
- Added: **Pacing telemetry** — every classifier decision is logged to a `Pacing Telemetry` journal under the existing "Starforged Companion" folder. One page per session; entries cap at 200 per page. The page also renders an HTML table for human review. Read API: `readPacingTelemetry(sessionNumber)` in `src/pacing/telemetry.js`.
- Added: Pacing UI section in Companion Settings → Mischief tab. Sits below the existing Lawful/Balanced/Chaotic radio — five number inputs (dials), the density-window field, an enable checkbox, and a scene-override status readout. Save button is GM-only.
- Added: Eight new world-scoped settings — `pacing.enabled`, `pacing.densityWindow`, and `pacing.dial.{combat,investigation,exploration,social,downtime}`. Defaults match the scope document.
- Added: Quench integration batch `starforged-companion.pacing` covering dial math + scene-override clamping, the in-memory density buffer (record/reset/scene-tag transitions), registered setting defaults, the `!pace hot|clear` chat path (round-trips through `campaignState.pacing.sceneOverride`), the `!roll` path (sets `forceNextAsMove`), and the `pacing.enabled = false` short-circuit. Wired into `Hooks.on("quenchReady")` alongside the existing 24 batches.
- Added: Unit suite `tests/unit/pacing.test.js` — 41 tests over `effectiveDial`, `buildClassifierContext`, `classifyInput` parsing + fallback paths, the router's MOVE/NARRATIVE/NARRATIVE_WITH_MOVE_AVAILABLE dispatch, density-buffer behaviour, `applyPaceCommand` subcommands, `markForceNextAsMove`, and `readPacingConfig`'s settings + campaignState merge. apiPost, narratePacedInput, and logPacingDecision are mocked at the module boundary so the suite stays hermetic.
- Changed: `isPlayerNarration` now also excludes `paceCommandCard` and `rollCommandCard` flag holders so the pacing-confirmation chat cards do not loop back through the pipeline. The pacing classifier is wired into `registerChatHook` after every existing `!`/`@scene` intercept and before the existing `pendingMove` lock; the lock is only claimed on the MOVE branch (NARRATIVE paths don't write campaignState and don't need it). The in-memory density buffer is reset on every `@scene` query and on `Hooks.once("ready")`.
- Changed: Help compendium updated (CONTENT_VERSION → 0.1.61). Chat Commands page gains a "Pacing Commands (GM only)" subsection. Settings Reference gains a "Pacing Classifier (Mischief Tab)" table. A new dedicated "Pacing" page (sort 525) explains the three decisions, the dials, `!pace`/`!roll`, and disabling. Changelog page gains a v1.1.0 entry.
- Fixed: `!lore` no longer fails with "World Truths content could not be read" when `campaignState.worldTruthsJournalId` is stale. The previous reader in `callLoreRecapNarrator` (`src/truths/generator.js`) trusted the stored id and, when it pointed at a deleted/recreated journal, fell through to the structured fallback — which is silently rejected by the `!formatted.includes("undefined")` guard if any of the 14 categories serialised an `undefined`. The reader now also resolves the journal by name (system i18n `IRONSWORN.JOURNALENTRYPAGES.TypeTruth` or the module's own `World Truths`), prefers a candidate whose first page has ≥2 `<h2>` elements (matching the `createJournalEntryPage` validity check), and repairs `worldTruthsJournalId` once a real entry is found (GM-gated, single `game.settings.set`). `writeLoreRecapToJournal` gets the same name-based fallback so the recap card and its "Story So Far" page write back to the journal the user is actually editing. `CONTENT_VERSION` in `src/help/helpJournal.js` bumped to `0.1.60`; a matching bullet is appended to the v1.0.1 Changelog page. Diagnosed against the user's live state where `worldTruthsJournalId` was `V6lLqy3UFSUUE07G` and `game.journal` contained two `Setting Truths` entries with different ids.
- Fixed: Settlement, connection, planet, location, faction, creature, and ship journal entries no longer appear at the root of the Foundry Journals tab. Every entity creator in `src/entities/` now passes `folder: await getOrCreateEntitiesFolder()` to `JournalEntry.create`, funnelling new entries into a dedicated "Starforged Entities" folder. The new helper is `src/entities/folder.js` — it looks up or creates the folder, caches the id, and self-heals if the folder is deleted between calls. The "Sectors" folder used by `sectorGenerator.js` for sector wrapper records is untouched. Already-leaked entries from earlier sector creations are left where they are (no migration, no destructive moves); they can be dragged into the new folder or deleted by hand.
- Fixed: Quench's `Settings Panel → addLine writes through to globalSafetyLines` test no longer leaves a `QUENCH PROBE <timestamp>` entry in the GM's Safety panel after a run. Foundry's `game.settings.get()` returns object-valued settings by reference, so the previous `before = game.settings.get("globalSafetyLines")` captured the live array; the `addLine` handler then mutated that same array via `lines.push(probe)`, and the `finally` restore wrote the mutated (probe-containing) array back. The snapshot is now deep-cloned via `JSON.parse(JSON.stringify(...))`. `withTempSetting` is hardened identically so the same class of leak cannot recur for any future object-valued setting that's written through a handler which mutates its argument in place.
- Added: In-game help documents the previously-missing chat commands. `src/help/helpJournal.js` Chat Commands page now lists `!truths`, `!lore`, `!at <name>` / `!at`, `!sector list` / `!sector new` / `!sector <name>`, and `!sfc encounter <name>` with a brief description of each effect. `CONTENT_VERSION` bumped to `0.1.59` so the help journal refreshes on next world load; a matching v0.6.7 block is appended to the Changelog page.
- Fixed: Quench runs no longer show stacked yellow toast warnings for tests that deliberately exercise fallback paths. A new `withSilencedNotifications(fn)` helper in `src/integration/quench.js` stubs `ui.notifications.{info,warn,error}` for the duration of the wrapped block and restores them on exit (even on throw). Applied to the seven tests that intentionally trigger user-facing toasts in production code: `!at` with an unknown location, `!sfc encounter Bogfaller` chat dispatch, `interrogateScene` direct call, `rollProgress` DOM click (silences the "move already being resolved" info toast that can leak from prior tests), `generateLoreRecap` live API path, `generateSectorBackground` no-key path, and both `spawnEncounter` live tests. Production toast behaviour is untouched — only the test runs are quiet.
- Removed: Local Claude API proxy. The `proxy/` directory, `proxy/claude-proxy.mjs`, `proxy/start.sh`, and the `npm run proxy` script have been deleted. All external API calls now go directly from the Foundry renderer — Anthropic via `anthropic-dangerous-direct-browser-access: true` (handled centrally in `src/api-proxy.js`), image generation via OpenRouter (which supports browser CORS natively). The same code runs on Foundry desktop and on The Forge with no setup difference. The `claudeProxyUrl` setting and the `isLocalProxyReachable` / `proxyModeDescription` helpers are removed; the GM ready-hook no longer warns about a non-running proxy.
- Removed: DALL-E (OpenAI) image-generation backend. It required the local proxy to bypass browser CORS, and now that the proxy is gone there is no path that supports it. Image generation unifies on OpenRouter (FLUX.2 Pro by default, configurable). The `artBackend` setting, the `artApiKey` setting and its About-pane field, the `migrateArtBackend` migration, and all DALL-E call paths in `src/art/generator.js` and `src/sectors/sectorArt.js` are removed. The `src/api-proxy.js` helper is now Anthropic-only.
- Changed: Documentation reflects the Phase 2 architecture. README "Before every session (desktop)" section is gone; "Requirements" no longer lists Node.js; first-time setup is two API keys (Claude + OpenRouter). `docs/decisions.md` consolidates the CORS-strategy section to "direct browser fetch for everything". `docs/known-issues.md` marks CORS-001 fully resolved across both phases. `CLAUDE.md` updates the architecture constraints and project-context blocks to remove the proxy-routing rule and replace it with the direct-fetch convention. The in-game help journal (v0.1.58) drops the "Before Every Session (Desktop)" page, the OpenAI Art API Key row, the DALL-E troubleshooting section, and the Claude Proxy URL row.
- Fixed: OpenRouter requests now succeed when the caller-supplied `X-Title` string contains characters outside ISO-8859-1. The default titles used by the sector art and entity portrait callers contained an em-dash (`—`, U+2014); the browser's Fetch implementation rejects any `RequestInit.headers` value with non-Latin-1 code points, so the request never reached OpenRouter and the failure surfaced (correctly, after the previous diagnostic pass) as *"OpenRouter network error: Failed to execute 'fetch' on 'Window': Failed to read the 'headers' property from 'RequestInit': String contains non ISO-8859-1 code point."* The `X-Title` and `HTTP-Referer` values are now sanitized to the Latin-1 range before being attached to the request.
- Changed: Sector-art failures are now surfaced to the GM as visible notifications instead of silent `null` returns. Each failure mode (missing OpenRouter key, missing OpenAI key, image-API error, parser couldn't read response, upload failed) emits a permanent `ui.notifications` toast describing what went wrong, with the underlying error message included where applicable. The previously-silent `.catch(() => null)` wrapper around `generateSectorBackground` in `sectorPanel.js` now logs and surfaces the rejection too. A trace line at the start of `generateSectorBackground` records the resolved backend and key-presence flags so the active configuration is visible in the console without reproduction.
- Added: GM ready-hook art-status log + warning. After the `artBackend` migration runs, `src/index.js` prints a one-line summary of the active backend, key-presence flags, model id, and `sectorArtEnabled` flag. If the backend is `openrouter` and no OpenRouter key is set while sector art is enabled, the GM also gets a permanent UI notification explaining how to fix it. This makes the most common "no art appeared" cause (forgot to paste the key after the backend migration) visible without needing F12.
- Added: `artBackend` migration runs once on the GM client at world load. Two cases handled: (1) stale values `"replicate"` or `"fal"` (choices that were never wired up and are now removed) migrate to the environment default — `openrouter` on The Forge, `dalle` on desktop. (2) Worlds where `artBackend` is `"dalle"` and the client is running on The Forge migrate to `"openrouter"`, since DALL-E cannot reach OpenAI from a browser. Both cases post a notification explaining the change. Idempotent — once `artBackend` is `"openrouter"` (Forge) or `"dalle"` (desktop), subsequent loads do nothing.
- Changed: `src/art/openRouterImage.js` response parser hardened to handle more shapes — `message.images[0].image_url.url`, `.url`, `.b64_json`, parts inside `message.content` (with `image_url`, `inlineData`, `b64_json`, or `image` fields), and a fallback for markdown-image strings. When the model returns an `https://` URL instead of inline base64 (FLUX is documented to do this; URLs expire after ten minutes), the helper now fetches the URL and converts to base64 inline so callers see a uniform shape regardless of provider. On parse failure the helper logs a 1500-character truncated snapshot of the message body so the actual response shape is diagnosable from the browser console without a network capture.
- Fixed: Claude API calls now work on The Forge. The previous Forge transport relied on `ForgeAPI.call("proxy", ...)`, an endpoint that does not exist on Forge VTT — `ForgeAPI.call` simply appends the verb to `https://forge-vtt.com/api/<verb>` (no `/api/proxy` is routed server-side), it requires a Forge `Access-Key` the module never provisioned, and it returns the raw JSON body, not the assumed `{ success, response }` envelope. The fix routes Anthropic traffic on The Forge through a direct browser `fetch` with the `anthropic-dangerous-direct-browser-access: true` header (Anthropic's documented client-side opt-in). Desktop continues to use the local Node proxy unchanged in this Phase 1 "make-before-break" step; once Forge is verified in production, Phase 2 will migrate desktop to the same direct path and retire `proxy/claude-proxy.mjs`. The same approach is used by the `loremaster-foundry` module in production. The previously failing Quench batches `STARFORGED: Narrator → narrateResolution — live API call`, `STARFORGED: Full Pipeline → interpretMove returns a valid interpretation`, and `STARFORGED: Sector Creator → generateNarratorStubs → returns a sector stub string when API key is configured` will pass on Forge once a valid Claude API key is configured.
- Added: OpenRouter image-generation backend selectable as `artBackend = "openrouter"`, defaulting to that on The Forge (where DALL-E cannot reach the OpenAI API due to browser CORS) and to `"dalle"` on desktop. The new backend posts to `openrouter.ai/api/v1/chat/completions` with `modalities: ["image"]`, decoding the inline base64 result and persisting it the same way as DALL-E b64_json output. The default model is `black-forest-labs/flux.2-pro`, configurable via the new `openRouterImageModel` world setting. A new `openRouterApiKey` client setting holds the user's OpenRouter key (entered in Companion Settings → About). `src/art/openRouterImage.js` is the shared helper; `src/art/generator.js` and `src/sectors/sectorArt.js` dispatch on `artBackend` so existing DALL-E desktop flows are untouched. `src/api-proxy.js` no longer carries a Forge branch — direct fetch handles Anthropic on Forge, OpenRouter handles images on Forge, and the desktop local proxy still handles Anthropic and DALL-E for desktop users.
- Changed: `Art Generation Backend` choices are now `OpenRouter` and `DALL-E` only — the Replicate and fal.ai entries that were registered but never implemented have been removed to avoid suggesting non-functional options. The Quench `Sector Commands → generateSectorBackground — skips without API key` test now clears both `artApiKey` and `openRouterApiKey` so the no-key assertion holds whichever backend is active.
- Fixed: Move confirmation dialog accept and reject paths no longer hang under integration test harnesses (and are correspondingly more responsive in normal play). `MoveConfirmDialog.prompt()` now awaits its inner render before observing the resolver promise and exposes the in-flight instance via the `MoveConfirmDialog.pending` accessor, so callers do not have to scan `foundry.applications.instances` (which is a Map in v13 and incompatible with `Object.values()`). The same pattern is applied to `ClarificationDialog`, which previously still used the singleton-id shape that caused the original move-confirm regression.
- Fixed: Sector Creator now reliably persists a newly generated sector to `campaignState`. The previous flow issued three sequential `game.settings.set("campaignState", state)` calls (one per `createSettlement`/`createConnection` and a final one in `storeSector`); on Foundry v13 the sequential writes against the same in-memory reference can race and lose late-stage mutations such as `sectors.push()` and `activeSectorId`. `createSettlement` and `createConnection` now accept a `{ persist: false }` option, which `createEntityJournals` uses so that `storeSector` performs a single batched write at the end. The `Starforged Sectors` journal flag, the active sector id, and per-sector `settlementIds` / `connectionId` now all land in one persisted snapshot.
- Fixed: Move confirmation dialog accept button now reliably resolves with `true` even after a previous dialog has been opened in the same world session. The dialog used a singleton element id, which let `ApplicationV2` reuse a prior closed instance whose internal `#decided` flag was already set; the next click on Accept then short-circuited and the prompt promise hung. Each prompt now creates an instance with a unique id so the resolution gate starts fresh.
- Fixed: `!journal lore "title" confirmed — text` written through the chat path now reliably commits before the confirmation card returns. The createChatMessage hook now exposes its in-flight `!journal` work via `getLastJournalCommandPromise()` so callers (and integration tests) can await the full `JournalEntry.create → createEmbeddedDocuments → setFlag` chain instead of racing two `setTimeout(0)` ticks against a cold journal.
- Fixed: Removing a progress track from the Progress Tracks panel now actually persists. The remove handler — and every other `ProgressTrackApp` action handler — now records its in-flight promise on `this._lastAction` (the same convention used by the chronicle panel), so integration tests that dispatch a real DOM click can wait for the full async chain (load → confirm → filter → setFlag) to settle before re-reading the journal. The previous handlers ran fire-and-forget, causing the deletion to race with subsequent reads.
- Fixed: `@scene` queries now always post a chat card. Previously the function returned `null` silently when scene queries were disabled, the X-Card was active, no API key was configured, the API call threw, or the API returned an empty response — leaving the user with no feedback. A new fallback card now explains the reason in chat for each of those paths, matching the pattern already used for narration on a move resolution.
- Fixed: Quench Safety Extras `!x via chat hook — full path` test no longer fails its precondition due to leaked `xCardActive=true` from earlier batches. The Chat Command Routing `!x` test's `finally` block previously called `game.settings.set("campaignState", stateBefore)` after `clearXCard()`; because Foundry returns `campaignState` by reference, `stateBefore` could itself carry a dirty `xCardActive=true` and the restore would undo `clearXCard()`. The redundant restore is removed. The Assembler X-Card test now wraps `suppressScene`/`assembleContextPacket` in `try/finally` so `clearXCard()` runs even if assembly throws. The Safety Extras `!x` test now defensively clears the X-Card flag in a `before` hook and a per-test `finally`.
- Fixed: Quench Sector Creator `createSectorScene` notes/drawings count assertions updated to match current production behaviour. The drawings test now expects one drawing per `mapData.passages` entry (the previous hardcoded `1` predates FIX 2 in `sceneBuilder.js` which began rendering `toEdge:true` passages). The notes test now computes the expected count from each settlement's `planet`/`stellar` presence (the previous hardcoded `2` predates FIX 3/FIX 4 in `sceneBuilder.js` which added planet and stellar marker notes). The test sector is now created in a `before` hook so subsequent assertions can read it.
- Fixed: Quench `chronicle` batch togglePin test no longer skips when the prior addAnnotation test fails. The `before` hook now seeds a chronicle entry directly via `addChronicleEntry` and pushes its id into `seededIds`, so togglePin's precondition is satisfied independently of the addAnnotation DOM-click test outcome.
- Fixed: Quench integration tests no longer leak journal entries into the live world. The `entityWorldJournal` batch's `beforeEach` created a `"The Covenant"` faction journal that was unprefixed and untracked, leaking five entries per run; the seed faction is now prefixed `"QUENCH TEST — The Covenant"`, the new entry is registered with the cleanup tracker, and cleanup runs `afterEach` so per-test entries are removed before the next iteration recreates them. The chronicle batch's `after` hook now also deletes the per-actor `Chronicle — …` JournalEntry created by the addAnnotation test before deleting the test actor.
- Fixed: `!x` (X-Card) now flips `xCardActive` reliably regardless of how the chat message is created. The previous handler ran only on the typed-input `chatMessage` hook; messages created programmatically (or relayed in ways that bypass the typed input) reached `createChatMessage` without ever activating the X-Card. The `createChatMessage` hook now also recognises `!x` and calls `suppressScene()` so the flag flips on the GM client whichever path delivered the command.
- Fixed: Move confirmation dialog accept button no longer races with the dialog's close path. Resolution is now gated by a single `#decided` flag so that an explicit accept cannot be overridden by a default reject from `close()`.
- Fixed: In-game Help & Reference journal now shows correct `!` command prefixes (`!x`, `!recap`, `!journal`) — content was showing stale `/` prefixes from before the v13 command-prefix change
- Fixed: `ensureHelpJournal` now detects content version changes and updates existing journals automatically — existing worlds receive corrected content on next world reload, no manual steps required
- Removed: `packs/help.json` deleted — was a dead file with no module.json declaration and no build pipeline; `src/help/helpJournal.js` is the sole source of help content

- Added: Quench integration test coverage extended to chat command routing, move pipeline confirmation/persistence, mischief dial, progress track and entity panel actions, character chronicle, settings panel, world truths, sector commands, encounter spawn, session lifecycle, safety extras, toolbar registration, and clarification edge cases. Adds 14 new batches alongside the existing 10, exercising user-facing surfaces previously untested in a live Foundry world.
- Changed: World Journal panel reduced to a compact action surface (~320×400). Tabs removed; the panel now shows only Pending Lore (Confirm/Dismiss), Contradictions (Review/Override/Dismiss when flagged), Active Threats (severity dropdown), and a footer of links that open the Lore, Threats, Factions, and Session Log JournalEntries in Foundry's native viewer for reading.
- Added: System asset integration — the companion now reuses foundry-ironsworn art, compendium content, and localisation. Centralised path constants in `src/system/ironswornAssets.js` (planet, stellar, starship, location, asset, oracle, stat icons); deterministic starship token picker; location-background resolver with three art sets (Kirin/Rains/Root) and a new `locationArtSource` world setting.
- Added: Localisation wrapper (`src/system/i18n.js`) maps internal stat/meter/debility/move slugs to `IRONSWORN.*` keys, with English fallbacks for missing translations.
- Added: Canonical compendium lookup (`src/system/ironswornPacks.js`) — null-safe helpers for moves, oracles, encounter actors, and setting truths, with per-session document caching.
- Added: Move interpreter optionally injects the canonical move description from `starforged-moves` as a `<canonical_move>` block when an `expectedMoveSlug` is supplied.
- Added: `!sfc encounter <name>` chat command spawns canonical encounters from `foe-actors-sf` — drops a token on the active scene for GMs, posts a stat-summary chat card otherwise.
- Added: Narrator system prompt now includes a `<campaign_truths>` digest built from the foundry-ironsworn `starforged-truths` pack when `campaignState.canonicalTruthSlugs` is configured.
- Fixed: Silent-failure audit — production code paths that previously swallowed errors with empty `try/catch` now log via `console.error`/`console.warn` or rethrow. Settings-write functions across `sectors`, `entities/*`, `art/storage`, and `truths` now propagate persistence failures instead of no-opping. Actor-bridge reads (`getPlayerActors`, `getActor`) no longer mask Foundry errors with empty defaults that previously matched empty test fixtures by accident.
- Added: Test-suite console-error guard — `tests/setup.js` now spies on `console.error`/`console.warn` and fails any test that emits an unexpected error log. Tests that exercise legitimate error-handling paths can opt in via the new `expectConsoleError(/pattern/)` and `silenceConsoleErrors()` helpers.
- Added: ESLint rule banning empty catch blocks across the module (`no-empty` with `allowEmptyCatch: false`, plus a `no-restricted-syntax` rule that catches comment-only catch bodies).
- Fixed: `tests/unit ` directory renamed to `tests/unit` (trailing space removed) — caused permission-prompt friction during automated tooling.
- Fixed: Sector scene passage lines now actually render between settlements — v13 `BaseDrawing` joint validation rejects polygon shapes whose `shape.width`/`shape.height` are zero, so passage drawings were silently failing despite valid stroke properties; `makePassageLine` now sets non-zero bounding box dimensions and the previous error-suppressing try/catch has been removed so future regressions surface immediately
- Added: Sector-created settlements and connections are now flagged `canonicalLocked` so future narrator-driven entity discovery will not overwrite them
- Fixed: Sector creator now mirrors each settlement's narrator stub onto the settlement entity record's description, keeping the canonical entity in sync with the sector journal page
- Added: DALL-E 3 sector background art — each sector gets a generated 1792×1024 landscape image matching its region (Terminus, Outlands, Expanse, Void), with visual modifiers for notable troubles (energy storms, supernova, spatial rifts)
- Added: Foundry Scene created on sector finalization — background image, Journal Note pins per settlement, Drawing lines for passages; scene is created but not auto-activated
- Added: Narrator journal stubs — atmospheric one-paragraph descriptions for sector and each settlement generated via Claude Haiku, stored as annotatable journal pages in a sector record JournalEntry
- Added: `sectorArtEnabled` and `sectorNarratorStubsEnabled` world settings to control optional generation steps
- Fixed: Quench narrator integration tests now pass `campaignState` in the correct argument position so the `sessionId` flag on narrator chat cards is verified against a real session ID
- Changed: Claude API Key and Art Generation API Key are no longer shown in the standard Configure Settings dialog. They are now configured in Companion Settings → About tab and are only visible to the GM.
- Fixed: All module commands changed to `!` prefix (`!x`, `!recap`) — Foundry v13 rejects unrecognised `/command` patterns before `createChatMessage` fires
- Fixed: Confirmation dialogs now use `DialogV2` instead of the deprecated `Dialog` (entityPanel, progressTracks)
- Fixed: Chronicle panel rebuilt to correct ApplicationV2 patterns — window title, position, `DEFAULT_OPTIONS`, HTMLElement rendering, and `render({ force: true })` calls
- Fixed: Momentum recalculation now fires correctly when another client applies a debility change (`system.debility` path corrected in `updateActor` hook)
- Fixed: Suffer-move auto-debility logic now reads correct ironsworn schema paths (`system.health`, `system.spirit`, `system.supply`, `system.debility`)
- Fixed: Safety sync no longer attempts a world-scoped settings write from non-GM clients (silently failed before; now correctly skipped)
- Fixed: Removed dead `getApiUrl()` function from move interpreter; API routing already used the canonical Anthropic URL via `api-proxy.js`

## [0.6.0] — Previously On

- Added: `/recap session` posts a session recap card summarising all narrated moves in the current session — no API call required
- Added: `/recap` or `/recap campaign` generates a 3–5 paragraph campaign arc summary via Claude and posts it as a styled chat card
- Added: Campaign recap is cached; regenerates only when new chronicle entries are added
- Added: Campaign recap is automatically posted to chat when a new session begins (configurable; enabled by default)
- Added: Campaign recap text is injected into the narrator's context on the first narration of each session
- Added: Three new settings in Companion Settings: Auto Recap at Session Start, Session Gap Threshold, and Recap GM-Only

## [0.5.0] — Scene Interrogation

- Added: `@scene` prefix routes free-form questions to the narrator without triggering a move or rolling dice
- Added: Scene card posts to chat with the player's question visible and narrator response below, styled distinctly from move narration cards
- Added: Scene queries are suppressed when the X-Card is active
- Added: Recent narration cards from the current session are included as context so the narrator stays grounded in the immediate scene
- Added: Three new settings in Companion Settings: Scene Interrogation Enabled, Scene Response Length (sentences), Scene Context Cards (context window)

## [0.4.0] — Foundations

- Added: Session ID management — each world load generates or restores a session ID (resumes if last session was < 4 hours ago)
- Added: Session number increments automatically on each new session
- Added: `lastSessionTimestamp` recorded on world close for session boundary detection
- Added: Narrator cards now carry `sessionId`, `sessionNumber`, `moveId`, `outcome`, `narrationText`, and `timestamp` flags for use by upcoming recap and scene interrogation features
- Added: About tab in Companion Settings shows current session number, session ID (truncated), and session start time

## [0.3.0] — Character Management

- Added: Character management — move resolutions now automatically update health, spirit, momentum, debilities, and XP on the foundry-ironsworn character sheet
- Added: Character Chronicle — reverse-chronological story record with player annotations; accessible via the 📖 toolbar button
- Added: Starship damage tracking — Battered and Cursed impacts applied to the starship Actor automatically
- Added: Momentum burn handled automatically when action die matches current momentum

## [0.2.0] — Narrator

- Added: Direct Claude narration replacing Loremaster — works from any player account, no GM dependency
- Added: Narrator tab in Companion Settings — configure model (Haiku/Sonnet), perspective, tone, length, and custom instructions
- Added: Auto perspective — second person for solo campaigns, third person for multiplayer
- Added: Default tone: wry (knowing, slightly sardonic)
- Added: Prompt caching for narration system prompt — significantly reduces per-session cost
- Fixed: ChatMessage type "other" removed (not valid in Foundry v13)
- Fixed: message.author deprecation warning in v13
- Fixed: Meter persistence now correctly gated to GM account

## [0.1.x] — Deployment hardening

- Fixed: CORS — local Node.js proxy added; The Forge server-side proxy supported
- Fixed: Foundry v13 compatibility (ApplicationV2, DOM API, string literal message types)
- Fixed: World truths, progress tracks, and X-Card suppression now working correctly
- Fixed: Safety configuration now correctly reaches the narrator context packet
- Fixed: Mischief dial "lawful" value now recognised correctly
- Fixed: CI release — module.json version and URLs updated before zip build

## [0.1.0] — Initial release

- Move interpretation via Claude Haiku with prompt caching
- Move confirmation dialog, dice resolution, mischief dial
- Safety system: Lines, Veils, Private Lines, X-Card
- Progress tracks panel (vows, expeditions, connections, combat, scene challenges)
- Entity management with AI portrait generation (DALL-E 3)
- World Truths oracle tables (all 14 Starforged categories)
- Oracle integration
