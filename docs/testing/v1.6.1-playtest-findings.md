# v1.6.1 Playtest Findings

A running log of observations from a fresh-world v1.6.1 playtest, kept for later
diagnosis. **Recording only — do not action the open items from this file without
explicit direction.** Items marked _Fixed_ / _Resolved_ note where/how.

Status key: 🔴 open (to diagnose) · 🟡 needs decision · 🟢 fixed/resolved.

---

## Audit pass — 2026-06-01

A status sweep against current `main` (latest tag v1.7.1) reconciled this doc
with shipped code. The original observations are preserved verbatim; statuses
are updated in the per-finding headings and a **Fix:** line is appended where
applicable. Headline:

- **🟢 Now closed (20 of 22) + themes T2 + T3:** F1, F2, F3, F4 (live re-test
  pending), F5, F7, F8, F9a, F11, F12 ⚠️ (the HIGH mechanical bug), F13a,
  F13b, F14, F15, F16, F17, F18, F19, F20, F21, F22 — plus theme **T2**
  (granularity + empty bodies) and **T3** (description-into-page-body),
  both closed via the v1.6.2 salience-gate sweep + the page-body invariant
  in `upsertPage` with regression coverage at
  `tests/unit/worldJournal.test.js:242-291`.
- **🟡 Partially closed (2 findings + T1):** F6 and F9b (placeholder
  ship name leaking into prose — same root cause; `autoSeedStarship`
  fires on the `createActor` hook before any rename, so the narrator
  receives `name: "Ship"`. T1 Finalise is a workaround, not a fix of
  the auto-seed case); and theme **T1** itself (Finalise button
  shipped, full "entity created & assigned" gate did not).
- **🔴 Genuinely still open:** none from the original 22 findings.

**Self-correction (same-day, 2026-06-01):** two corrections to the first
pass of this audit.

1. The first pass called F13a and F14 "genuinely still open." Both were
   already shipped. F14 was closed on 2026-05-31 (commit `7345a32`,
   `claimAutoplayOnce(message.id)` in `src/audio/index.js:42-66/290`) and
   F13a is closed by `supersedeOriginalNarration` /
   `buildSupersededNarrationContent` in
   `src/moves/burnMomentum.js:316-346` (called from `handleBurnClick:223`).
   Both have explicit test coverage (`tests/unit/audio.test.js:642-667`,
   `tests/unit/burnMomentum.test.js:132-188`) and both name the playtest
   finding in inline comments.

2. The first pass left theme T3 (description-into-page-body) marked
   "needs verification" for lore / threats / faction / location confirm
   paths. T3 is in fact fully closed — `upsertPage`
   (`src/world/worldJournal.js:882-908`) writes `data` into both the
   page flag *and* the page body on both create and update branches,
   with an inline comment at L887-888 naming F15/F17/F19/F21 directly.
   `renderPageBody` (L920-943) carries a per-type switch for lore
   (`data.text`), threat (`data.summary`), faction
   (`data.knownGoal` + attitude), and location (`data.description`).
   A dedicated `describe("page body content (T3)", …)` block at
   `tests/unit/worldJournal.test.js:242-291` pins one assertion per
   type plus an update-branch test.

The lesson on both: grep the codebase for the finding id before writing
a "corrected diagnosis" or carrying forward an "uncertain" caveat —
both 7345a32 and `supersedeOriginalNarration` would have surfaced
immediately, and the `T3` test block names every finding it covers in
its docstring.

---

## F1 — Session toolbar button didn't render on Foundry v13 build 350 · 🟢 resolved (external)

**Environment:** The Forge, Foundry VTT v13 build 350.

**Observed:** The ▶ Session button (`sfSession`) did not appear in the Token
Controls toolbar, even though the other Companion buttons (🛡 Companion Settings,
Progress Tracks, Entities, Clocks) did. Updating Foundry from build **350 → 351**
made it appear.

**Module side:** No defect found. The registration matches the canonical two-hook
v13 pattern (`getSceneControlButtons` registers metadata, `renderSceneControls`
binds the click) in `src/index.js`, and `sfSession` has **no `visible` gate**, so
it should show for all users. The v1.6.1 commits only touched comment text in
`src/index.js`, never the toolbar code.

**For later:** if it recurs on a specific v13 build, investigate whether the
first-registered tool or a tool lacking an explicit `order` is dropped by that
build's scene-controls renderer.

---

## F2 — Docs labeled the Session button "🎮" instead of "▶" · 🟢 fixed (this branch)

**Observed:** v1.6.1 docs described the Session toolbar button as "🎮", but the
actual icon is `fas fa-play-circle` (▶). The shipped help journal already uses ▶.

**Fix:** corrected 🎮 → ▶ in `README.md`, `docs/scope-index.md`,
`docs/session/session-scope.md`, and the `CHANGELOG.md` [1.6.0] entry.

---

## F3 — Sector creation: settlement Actors created but sheets/icons not populated · 🟢 fixed (v1.6.2 / v1.7.1 / v1.7.2)

**Environment:** fresh world, `!sector new` → sector "Onyx Verge".

**Observed:**
- Four settlement Actors were created — **Argosy, Nyx, Sirius, Welkin** — under
  Actors → **Sectors → Unsorted**, using the generic foundry-ironsworn location
  sheet.
- Those Actor sheets are **empty**: Region, Location type, Type of settlement, and
  the roll tables (Population, First look, Initial contact, Authority, Settlement
  projects, Settlement trouble) are all unfilled. **No portrait icon** is set
  (generic hooded silhouette).
- Meanwhile the journal **"Onyx Verge — Sector Record"** captures everything well:
  Region (Terminus — the settled core), Trouble (settlements/factions on the brink
  of war), a narrative blurb, and a Settlements list with full data —
  Nyx (Deep Space, Pop: Hundreds, Authority: Unyielding),
  Welkin (Orbital, Pop: Tens of thousands, Authority: None/lawless),
  Argosy (Orbital, Pop: Thousands, Authority: Corrupt),
  Sirius (Planetside, Pop: Thousands, Authority: Corrupt) — plus "4 passages charted".
- Reporter notes the flow "only partially uses the Starforged UI / continues to run
  duplicates" — settlement data appears to live in two representations (the
  unsorted settlement Actors **and** the Sector Record journal under
  Sectors → Onyx Verge) without the Actor sheets being filled.

**Desired outcome (reporter):** the settlement **Actor cards should be filled out**
(sheet fields populated) and **icons set**, *in addition to* the journal snippet.

**Likely-related (for diagnosis, not yet investigated):**
- `docs/entities/entity-actor-migration-scope.md` (📋 planned) — makes
  settlement/location entities **native Actors as the source of truth**, collapses
  the settlement-data duplication across storage layers, and adds hierarchical
  Actor folders. This finding looks like the gap that scope is meant to close.
- `docs/sectors/sector-creator-enhanced-scope.md` and `src/sectors/sectorGenerator.js`,
  `src/sectors/sectorOverview.js` — how settlement Actors vs the Sector Record are
  produced.
- `docs/foundations/system-asset-integration-scope.md` + `src/system/ironswornAssets.js`
  — the icon resolvers (`statIcon` / `assetIcon` / location icons). The scope-index
  "Visual UI polish" next-step notes these resolvers shipped but were never wired
  into the panels/cards.
- `src/entities/settlement.js`, `src/entities/registry.js`, `src/entities/folder.js`
  — settlement record CRUD, host-document dispatch, and the "Unsorted" vs per-sector
  folder placement.

**Fix (post-audit 2026-06-01):** closed by a three-PR sweep across the failure
modes the original finding described. (a) `applyStubsToSettlementEntities`
(v1.6.2) now routes narrator stubs through `updateSettlement` so the Actor's
`system.description` and module flag both receive the prose — the empty-sheet
half. (b) The sector-rolled-data sweep (v1.7.1) wires the rolled oracles onto
the foundry-ironsworn fields: `normaliseSettlementKlass` / `normalisePlanetKlass`
map titlecase wizard strings onto the schema enum (`planetside` / `orbital` /
`deep space`); `composeSettlementDescription` surfaces Population / Authority /
Projects / Trouble / First-look / Initial-contact / Stellar object on the
sheet; Connection Goal is a real schema field. (c) `finalizeSectorEntities`
(v1.7.2, [Unreleased]) runs the portrait pipeline against each sector-spawned
settlement so `actor.img` and `prototypeToken.texture.src` are populated.

---

## F4 — Sector Record narrative passage truncated mid-sentence · 🟢 likely fixed · live re-test pending

**Observed:** the "Onyx Verge — Sector Record" narrative blurb ends mid-sentence
with no closing punctuation:

> "…and where the only thing spreading faster than suspicion is the grim certainty
> that someone's going to light the fuse. The real question, as you chart your
> course through the cold black between them"

**Likely-related (for diagnosis, not yet investigated):**
- The sector narrator-stub generation (`sectorNarratorStubsEnabled` setting). A
  `max_tokens` cap that's too low for the blurb, or a trim/parse step dropping the
  tail, would produce this. Code: `src/sectors/sectorGenerator.js` /
  `src/sectors/sectorOverview.js` and the narrator call they use
  (`src/narration/narrator.js`).

**Fix (post-audit 2026-06-01):** `sectorGenerator.js:836` (`callStubApi`) now
calls `trimToLastSentence(text.trim(), stopReason === "max_tokens")` when the
model stops on `max_tokens`, lopping the truncated trailing fragment so the
blurb ends on a complete sentence rather than mid-clause. Hypothesised root
cause matches; **needs a live re-test against a fresh `!sector new` to confirm
closed** — the trim depends on whether `stop_reason` is reliably surfaced and
on whether earlier sentences are long enough to still read well.

---

## F5 — Ship portrait lands, but the sheet icon is small; mirror it into Notes · 🟢 fixed

**Environment:** fresh world, starship Actor "Kobayashi IV".

**Observed:** the ship portrait now renders correctly (top-left of the starship
sheet) — a clear improvement over earlier sessions. But the sheet icon is small.
Request: also embed the generated art **in the Notes tab** (larger) if the sheet
allows it.

**Related:** `src/art/*` (portrait generation/storage), `src/entities/ship.js`
(Notes content authoring), and the foundry-ironsworn starship sheet's Notes field.

**Fix (post-audit 2026-06-01):** closed. `withPortraitInNotes(notesHtml, imgPath)`
(`src/art/generator.js:254-264`) prepends a marker-wrapped `<img>` block at the
top of the starship's Notes HTML — wider than the sheet's small header icon —
and is idempotent (regeneration replaces the prior art block rather than
stacking). `attachPortraitToActor` (L286-292) wires it in for ships only:
when generating the portrait it also writes `system.notes = withPortraitInNotes(notes, path)`
in the same single document update that sets `actor.img` and
`prototypeToken.texture.src`. Inline comment at L286-289 names F5 directly.
Test coverage in `tests/unit/shipPortraitNotes.test.js` (3 tests: prepend
preserves prose, idempotent regeneration replaces the block, clearing
the path strips the block).

---

## F6 — Ship flavor generated before the ship was named (placeholder "Ship") · 🟡 mitigated

**Observed:** the Notes prose refers to the **placeholder** name — *"The crew calls
it 'Ship' with the same resigned irony they reserve for ration packets labeled
'food'"* — even though the ship is named **Kobayashi IV**. The auto-generation
fired before the name was assigned.

**Related:** starship auto-seed / Notes-stub timing (`autoSeedStarship` setting,
`src/entities/ship.js`, and the narrator stub call). Generation appears to run at
creation, before naming.

**Fix (post-audit 2026-06-01):** mitigated, not fundamentally fixed. The T1
Finalise lifecycle (v1.6.2) ships an explicit **✦ Finalise** button on the
Entity Panel for ship / settlement / planet / location actors — generates a
2-4 sentence description grounded **only** in the entity's stored fields and
triggers a first-time portrait. The recommended workflow is now: create →
name → assign assets → click Finalise. The auto-seed-at-creation path
(`autoSeedStarship`) is unchanged, so a player who relies on auto-seed will
still see this. A full "created & assigned" gate (T1) remains the candidate
direction.

---

## F7 — Ship flavor implies assets/modules that aren't on the sheet · 🟢 fixed (v1.7.2)

**Observed:** the Notes prose and seed line — *"Carrier — Launches fighters ·
Oversized engines · Roll twice"* plus prose about "engines grotesquely outsized…
launching waves of fighters" — describe a **carrier with fighters and oversized
engines**, but the Actor's actual assets are **STARSHIP (Command Vehicle)** +
**RESEARCH LAB (Module)**. The generated flavor isn't grounded in the real asset
loadout; it leaned on the oracle "Roll twice" seeds (Carrier / Launches fighters /
Oversized engines), which then contradict the equipped modules.

**Related:** starship oracle seeding (`src/oracles/tables/starships.js`,
`autoSeedStarship`) and the flavor generator not reading the Actor's actual Item
assets before writing prose.

**Fix (post-audit 2026-06-01):** closed in v1.7.2 ([Unreleased]).
`installModulesForRolledIdentity` (`src/entities/ship.js`) uses a deterministic
keyword-based mapping in `src/entities/starshipModules.js` to score each of the
15 canonical Starforged Modules against the rolled type / first-look / mission
text and embeds the top 3 as Item documents on the starship Actor (via the
`starforgedassets` compendium through a new `getCanonicalAsset(slug)` export
on `src/system/ironswornPacks.js`). Wired into the tail of `seedStarshipActor`
(idempotent — skips when any Module-category asset is already present). The
Notes prose still leans on the rolled oracle seeds, but the sheet now actually
carries the assets the prose describes, so they're no longer a contradiction.

---

## Theme T1 — an explicit "entity created & assigned" lifecycle gate · 🟡 candidate direction

Across F3 / F4 / F6 / F7 a pattern emerges: the module **generates names, flavor,
art, and journal/Notes stubs eagerly at creation time**, before the entity is
named, assigned, and carrying its real details (assets, type, sheet fields). The
result is placeholder-name references ("Ship") and invented details (fighters,
oversized engines, off-sector settlements) that don't match the finished entity.

**Reporter's framing:** *"I'm starting to feel like we need to have a process for
declaring the ship 'created' and assigned."*

**Candidate direction (not yet actioned):** an explicit "entity finalized" step —
analogous to the Session **Begin** gate — so generation (flavor / art / Notes /
stub) fires **after** naming + assignment and is **grounded in the actual sheet
data**. Relates to `entity-actor-migration-scope.md` (native Actors as the source
of truth) and the generate-once/locked portrait lifecycle in `../decisions.md`.

**Confirmed behavior (not a bug):** a starship carrying the **Command Vehicle**
asset shows an **INTEGRITY** meter (5 → 0) on its sheet; modules (e.g. Research
Lab) appear as their own asset cards.

---

## F8 — Actor folder tree is designed + helpers exist, but actors land in "Unsorted" · 🟢 fixed (v1.7.1)

**Reporter:** *"I feel like we had a folder structure picked out for characters,
ships, and NPCs, but since it doesn't pre-populate, the actors just get jumbled."*

**Confirmed — the structure was designed and the helpers already exist:**
- Design: `entity-actor-migration-scope.md` §3.4 — a typed Actor folder tree
  `Starships/` · `NPCs/` · `PCs/` · `Sectors / <Sector Name> /` (the per-type
  `Settlements` / `Locations` / `Planets` sub-tier was later flattened away).
- Code: `src/entities/folder.js` already exports
  `getOrCreateActorFolder("Starships"|"NPCs"|"PCs"|"Sectors")` and
  `getOrCreateSectorActorFolder(sectorId, campaignState)`.

**Observed symptom:** the settlement Actors from `!sector new` landed under
**Sectors / Unsorted**, not **Sectors / Onyx Verge** (see F3 screenshot).

**Hypothesis (to verify):** `getOrCreateSectorActorFolder` falls back to
`Sectors / Unsorted` when the `sectorId` isn't found in `campaignState`
(`src/entities/folder.js:142`). During `!sector new` the settlement Actors appear
to be created **before** the sector record is persisted/resolvable in
`campaignState`, so the lookup misses and everything drops into Unsorted — a
creation-ordering issue in the same family as **T1**.

**Status of the full tree:** end-to-end population + reparenting
(`flattenSectorActorFolders`, the `!migrate-entities` regroup) lives in the
**Entity → Actor Migration** scope, which is 📋 PLANNED — so today only the
on-demand helpers run and the Unsorted fallback dominates.

**For diagnosis:** `src/entities/folder.js:142` (Unsorted fallback),
`src/sectors/sectorGenerator.js` (campaignState persist order vs settlement Actor
creation), `src/entities/settlement.js` / `src/entities/ship.js` (which folder
each creator requests).

**Fix (post-audit 2026-06-01):** closed in v1.7.1. The hypothesis was correct
— ordering bug between `createEntityJournals` and `storeSector` in
`src/sectors/sectorGenerator.js`. `createEntityJournals` now pre-registers a
minimal `{id, name}` sector stub on `campaignState.sectors[]` before any
entity is created (idempotent so re-runs don't duplicate); `storeSector`
replaces the stub in place rather than appending. Three new unit tests in
`tests/unit/sectorGenerator.test.js` pin the pre-registration, idempotency,
and in-place replacement. **Existing sectors already in Unsorted are not
migrated** — those Actors need to be dragged by hand or a follow-up cleanup
pass run.

---

## F9 — Galley vignette: `<npc>` voice markup leaks into the card; ship still "Ship" · 🟢 (F9a) fixed · 🟡 (F9b) mitigated

**Environment:** fresh world, **solo** play. Begin Session → "Opening — Ship's
Galley" card.

**Positives (first pass):** strong — named crew NPCs (Oakes the medic; Chen and
Rourke as the absent crewmates), good wry color, and solo mode handled well (the
lone PC at the bolted-down table, banter about the missing crew). The Spotlight
Vignette "+1 momentum" play-kit card posted above it as expected.

**F9a — `<npc>…</npc>` markup renders literally in the card.** The card shows raw
tags, e.g. `<npc>"That'll be Chen again,"</npc>`. These are the module's **audio
voice-split markers** (narrator voice vs NPC voice) — *not* the narrator avoiding
names (it named everyone). They should be stripped/styled before display.
- `stripMarkup(prose)` exists for exactly this — `src/audio/segments.js:12`,
  docstring "Remove `<npc>…</npc>` markers from prose for chat display" — but is
  currently wired only into the **audio** path (`src/audio/index.js:224`).
- The vignette cards post `escapeHtml(text)` **without** stripping:
  `src/session/galleyVignette.js:143` and `src/session/endSessionVignette.js:169`.
- To verify: whether the **main** narrator card also leaks `<npc>` or strips it
  elsewhere — i.e. is this vignette-specific or a broader display gap?

**F9b — the ship is referred to as "Ship", not its real name.** Reinforces F6 /
theme **T1**: the ship doesn't appear tied to the PC, so the vignette narrator has
no real name to use and defaults to the placeholder. Reporter's read: *"might be
because it hasn't tied the ship to me, or … it is just not making it up."* Likely
addressed by the same "entity created & assigned" work (T1) that ties the command
vehicle to the player.

**Update (see F10):** the main `@scene` narrator *does* use the real name
"Kobayashi IV" and the active sector — so the ship→PC tie exists for main
narration; the **vignette** path is the specific gap (its context assembly and/or
Begin-Session timing), not a missing link.

**Fix (post-audit 2026-06-01):** F9a closed — `galleyVignette.js:144` now wraps
text in `stripMarkup(text)` before posting, stripping `<npc>…</npc>` markers
that were only intended for the audio-segment splitter. F9b mitigated by the
same T1 Finalise lifecycle as F6 — if the player finalises the ship after
naming, the galley vignette context picks up the real name.

---

## F10 — @scene correctly IDs ship + location; confirms the narrator reads ship Notes · 🟢 positive / 🔴 confirms F7

**Environment:** fresh world, solo. `@scene where am I located currently`.

**Response:** *"You're aboard the **Kobayashi IV**, your carrier drifting in the
black between stars of the **Onyx Verge**. The bridge hums with the steady thrum
of those **oversized engines**, running hot even at station-keeping…"*

**Positive:** the main `@scene` narrator path correctly resolves the **real ship
name** and the **active sector** — the assembler surfaces ship + location context
here. (Refines F9b above: the tie exists for main narration.)

**Confirms F7 — and raises its priority:** "those oversized engines" shows the
narrator is **reading the ship's Notes text** (good context integration), but it
means the **F7 ungrounded flavor propagates into narration**. The invented
"oversized engines / carrier" detail isn't cosmetic to the sheet — it becomes part
of the narrator's working truth. Grounding the ship flavor in the actual assets
(F7 / T1) therefore matters downstream, not just on the sheet.

---

## F11 — Two-voice NPC audio plays on top of itself (segments overlap) · 🟢 fixed

**Environment:** narrator card with an NPC line (Vance), audio narration enabled,
two voices configured.

**Positive:** first successful **two-voice split** — the narrator voice and a
distinct NPC voice were both produced, so the `<npc>` audio markup did its job on
the synthesis side.

**Bug:** the two audio segments **played simultaneously** (overlapping) rather than
one after the other. Playback is *designed* as a sequential queue —
`src/audio/playback.js` docstring: "the queue plays in order; each `end` advances
to the next" — so the `end` → advance chaining isn't holding here; both segments
start at once.

**Hypothesis (to verify):** playback *is* a sequential `while`-loop that awaits each
segment (`src/audio/playback.js:199–206`, `_playFromCurrent` → `_playOneSound`), so
the overlap implies one of: (a) `_playOneSound` resolves on `play()`-start rather
than on the segment's `end` event, advancing the loop early; (b) the `end` event
isn't firing on this Forge/v13 build (same `Sound` event-listener family as
**AUDIO-001** in `known-issues.md`); or (c) two playback sessions started
concurrently (e.g. autoplay + a manual click, or a double-bound play button in
`src/audio/index.js`). Code: `src/audio/playback.js` + `src/audio/index.js`.

**Relation:** same `<npc>` markup as **F9a** — F9a is its *display* side (tags
leaking into the card), F11 is the *audio* side (segment ordering).

**Fix (post-audit 2026-06-01):** closed. `_playOneSound` in
`src/audio/playback.js:241-264` now resolves only on the Sound's `'end'` or
`'stop'` event, not on the `play()` promise. The inline comment at L246-250
names F11 directly as the motivating bug ("Foundry v13 Sound.play() resolves
at playback START, so finishing on it would advance to the next segment
immediately and play it on top of this one"). A fall-back path covers
implementations that never emit `'end'`, so the queue can't deadlock.

---

## F12 — Burn momentum turned a Weak Hit into a Miss · 🟢 fixed · ⚠️ was HIGH (mechanical / play-affecting)

**This is the first mechanical bug** (it changes a roll outcome), not a
cosmetic/timing one.

**Environment:** Face Danger. Original roll **Action 5 + 2 = 7** vs **Challenge
[6, 7]** → **Weak Hit** (7 beats 6, ties 7). Player clicked **Burn momentum** →
card flipped to **"Burned → Miss" / "Pay the Price."**

**Reporter's key clue:** *"the action score and challenge score were the same as the
original roll"* — the card still showed **5 + 2 = 7** vs **6, 7**, but labeled it a
**Miss**. 7 vs [6,7] is a **Weak Hit**, so the displayed numbers don't justify a Miss.

**Why this is wrong:** per the play kit (and `src/moves/burnMomentum.js:6–12`),
burning momentum can **only improve or hold** an outcome — never worsen it — and the
card is supposed to **only offer the burn when it helps**.

**Code facts (corrected after reading the source):**
- Real functions: `buildBurnState` (`burnMomentum.js:63`), `canBurnMomentum` /
  `applyMomentumBurn` (`src/moves/resolver.js:360` / `:369`). *(An earlier draft of
  this entry named `computeBurnOutcome` / `shouldOfferBurn` / `resolveBurnContext` —
  those do not exist; corrected here.)*
- The click handler burns the **stored** `burn.momentum` (`handleBurnClick`,
  `burnMomentum.js:187`) — the same `postMomentum` computed at render
  (`buildBurnState:67–69`). So render and click use the **same** momentum; the bug is
  **not** a render-vs-click mismatch.
- The eligibility gate `canBurnMomentum` (`resolver.js:360–367`) is just
  `return momentum > Math.min(c1, c2)` (plus not-progress / momentum>0 / not-already-
  strong-hit). **It compares momentum only to the lower challenge die — it never
  compares the *burned* outcome to the *current* outcome.** `applyMomentumBurn`
  (`:369–374`) is the standard `> both → strong / > one → weak / else miss`.

**Inconsistency to repro:** with the **displayed** dice **[6, 7]**, an *offered* burn
requires `momentum > 6` (≥ 7), and `applyMomentumBurn(7,[6,7])` → **weak_hit**, never
**miss** — yet the card shows **Miss**. So either the momentum/dice actually used at
burn differ from what the card displays, or the "Miss" label/consequence is applied
downstream incorrectly (`buildBurnedCardContent` / `mapConsequences`). **Needs a live
repro capturing the actor's real momentum value** to pin exactly.

**Defects:**
1. **Mechanical (HIGH):** `canBurnMomentum` lacks an explicit *"burned outcome ≥
   current outcome"* check (it only asks whether momentum beats the lower die), and
   `handleBurnClick` doesn't re-assert improvement before applying. A burn that
   doesn't strictly improve should be neither offered nor applied — add the guard in
   **both** `buildBurnState` and `handleBurnClick`.
2. **Display:** the move-result card shows the **original** action score (5+2=7) next
   to the burned label, so the numbers contradict the outcome. After a burn the card
   should surface the **momentum value** used as the score.

**Code:** `src/moves/resolver.js` (`canBurnMomentum` / `applyMomentumBurn`),
`src/moves/burnMomentum.js` (`buildBurnState` / `handleBurnClick` /
`buildBurnedCardContent`), `src/index.js` (burn render at `:899` / `:2248`),
`src/character/actorBridge.js` (momentum read).

**Fix (post-audit 2026-06-01):** closed. The headline mechanical defect is
fixed in two places, exactly as the playtest entry prescribed:
- **Render-time gate** — `canBurnMomentum` (`src/moves/resolver.js:127-136`)
  now computes the burned outcome and returns
  `OUTCOME_RANK[burnedOutcome] > OUTCOME_RANK[currentOutcome]`. A bare
  `!==` would also offer a burn that makes the outcome worse; the rank
  comparison enforces strict improvement. The inline comment at L133-135
  names the F12 scenario directly.
- **Click-time defense-in-depth** — `handleBurnClick`
  (`src/moves/burnMomentum.js:198-201`) re-asserts
  `OUTCOME_RANK[burnResult.outcome] > OUTCOME_RANK[burn.originalOutcome]`
  before touching meters or the card, bailing with a warn if a momentum
  change between render and click would make the offered burn worsen the
  outcome.

Display defect (the burned card showing the original action score rather
than momentum) was not visibly addressed in the same pass — re-test in live
play to confirm the card's score readout matches the burned outcome.

---

## F13 — Burn posts a 2nd narrator card; both audios overlap · 🟢 (F13a) fixed · 🟢 (F13b) fixed

**Environment:** continued from F12 — clicking **Burn Momentum** (this time the burn
*upgraded* the outcome). "−1 momentum (now 2)" meter card above.

**Observed:** two **Narrator** prose cards appeared for the one move —
- Card 1 (original outcome): *"…rows of sleek unmarked cases… your crowbar slips…
  a proximity sensor chirps twice… the cargo bay's external airlock just registered
  movement."* (the weak-hit complication)
- Card 2 (burned/upgraded outcome): *"…the real cargo—not medical supplies at all,
  but military-grade munitions… The manifest was a lie…"* (the cleaner result)

…and **both cards' audio played over each other** ("again" — same overlap class as
**F11**, now across two cards).

**F13a — duplicate, now-contradictory narration.** `renarrate()`
(`src/moves/burnMomentum.js:238–281`) always **posts a fresh narration card** for the
upgraded outcome via `narrate(synthResolution, …)`, while the **original** narration
card is left in place. So the superseded prose lingers — and it describes events the
burn undid (the airlock/proximity complication the upgrade removed). (The move-
*result* card is correctly rewritten in place by `message.update` +
`buildBurnedCardContent`; it's the separate *narration* card that duplicates.)
Consider superseding / striking the original narration card instead of appending a
second.

**F13b — cross-card audio overlap.** Both narration cards triggered playback at once
— the **two-concurrent-sessions** variant hypothesised in **F11(c)**. Strongly
suggests the audio layer needs a **global "stop any playing narration before starting
another"** singleton, which would also cover F11's within-card case. Code:
`src/audio/index.js` (togglePlayback / autoplay) + `src/audio/playback.js`.

**Relation:** F13 sits at the intersection of **F12** (the burn re-narration path)
and **F11** (audio overlap) — a burn fix should resolve the duplicate card *and* the
overlapping audio together.

**Fix (post-audit 2026-06-01, corrected same-day):** both halves closed.
- **F13b** (cross-card audio overlap) closed by the v1.5.2 single-active-
  playback guard in `src/audio/playback.js` — starting any session stops
  whichever session was previously active ("latest wins, no overlap"), so
  the second narration card's audio replaces the first rather than
  overlapping it.
- **F13a** (duplicate, contradictory narration card) closed by a link-
  then-supersede flow in `src/moves/burnMomentum.js`. The burn flag
  carries `resolutionId: resolution._id` (L92, inline comment names F13a)
  so the original narrator card can be located. `handleBurnClick` (L223)
  calls `supersedeOriginalNarration(burn)` before renarrating;
  `supersedeOriginalNarration` (L316-330) finds the card by
  `resolutionId`, sets `flags.burnSuperseded = true`, and rewrites its
  content via `buildSupersededNarrationContent` (L337-346) — wraps
  `.sf-narration-prose` in `<s>` with `opacity:0.55;text-decoration:line-through`
  and appends an italic *"Superseded — momentum was burned to upgrade
  this outcome. See the updated narration below."* note. Idempotent
  (skips when `sf-narration-superseded` is already in the class). The
  superseded original stays in the chat history (preserving the record)
  but reads visually as struck-through with the new narration below.
  Test coverage: `tests/unit/burnMomentum.test.js:132-188` —
  content-build assertions, the supersede flow against fake messages,
  null/missing/already-superseded edge cases.

(The original audit pass on 2026-06-01 mis-flagged F13a as still open
because it didn't search the burnMomentum source for the finding id.
The shipped fix was in place at the time of the audit.)

---

## F14 — "Gather Information" move button plays audio instead of rolling · 🟢 fixed

**Environment:** a NARRATIVE_WITH_MOVE_AVAILABLE narrator card ending *"…that would be
a Gather Information."* with the roll button + ▶ Play / ■ Stop / Correct a fact; audio
enabled.

**Observed:** clicking the move button **played the audio again** (on top of the first
playthrough) instead of rolling — "like it interpreted it as a play button."

**Code facts:** the roll button is `data-action="sf-paced-roll"` (`src/index.js:2724`);
audio is `data-action="audioPlayToggle"` / `audioStop` (`src/audio/index.js:215/249`).
Likely culprit — `src/audio/index.js:366` installs a **global capture-phase document
click listener** (`document.addEventListener("click", handler, true)`); a document-wide
capture handler can fire on the `sf-paced-roll` click and (re)start playback. Verify
whether that handler is scoped to the play button or fires on any click
(gesture-priming gone too broad).

**Relation:** another audio double-trigger — same family as **F11 / F13b**. One
"single playback at a time + correctly-scoped handlers" fix likely clears F11, F13b,
and F14 together.

**Fix (post-audit 2026-06-01, corrected same-day):** closed on 2026-05-31
(commit `7345a32`, *"fix(audio): autoplay at most once per card (F14)"*).
**Actual mechanism (different from this entry's two earlier hypotheses):**
a narrator card re-renders on every message update — clicking its inline
`sf-paced-roll` button (the "Roll Gather Information" button) updates the
message, which re-runs `onNarratorCardRendered`. With `audio.autoplay`
on and no guard, the autoplay branch re-fired on every re-render and
played the audio again. The roll *did* fire too, but the audio drowned
it out perceptually and the user read the situation as "button plays
audio instead of rolling."

Fix wires a once-guard:
- `_autoplayedCardIds` Set + `claimAutoplayOnce(cardId)` helper
  (`src/audio/index.js:42-66`) — first call for a given message id
  returns `true` and records it; subsequent calls return `false`.
- The autoplay branch (`src/audio/index.js:290`) now gates on
  `claimAutoplayOnce(message.id)`, so re-renders skip autoplay
  entirely. Click-to-play is unaffected.
- Inline comments at L42-46 and L288-289 name F14 directly.
- 4 regression tests in `tests/unit/audio.test.js:642-667`: first-vs-
  re-render, cross-card independence, null/undefined id, reset helper.

(Both earlier hypotheses on this entry were wrong. The first guess —
a global capture-phase click handler re-starting playback — was
plausible but didn't exist. The 2026-06-01 audit's "gesture-overlay
listener unlocks queued autoplay on any click" was also wrong: the
gesture overlay is one-shot per session via `_gestureOverlayShown`,
so it can't explain a per-card replay. The real fix is the once-guard
on the autoplay branch itself, and it was already shipped when the
audit ran. Lesson: grep the codebase for the finding id before
writing a corrected diagnosis.)

---

## F15 — World Journal Threats: sparse / empty-bodied entries + promotion criteria · 🟢 fixed

**Environment:** World Journal — Threats. Entries: "Settlements or factions are on the
brink o…" (the sector trouble), "Cargo Anomaly", "External Airlock Intrusion".

**Observed:** entries are **incredibly sparse** — the selected "External Airlock
Intrusion" page has a **title but an empty body** (no description / severity / summary).
The detector creates threat entries with no content.

**Design question (reporter):** *"maybe some of these should rise to the level of 'world
threats'"* — the detector promotes **scene-level complications** (cargo anomaly, airlock
intrusion from the current encounter) into WJ Threats. Need clearer criteria for
campaign-level **world threats** vs transient scene beats.

**Code:** `src/world/worldJournal.js` (threat entry creation / body), the threat
severity/looming logic, `src/entities/entityExtractor.js` (detection → threats routing),
`docs/entities/world-journal-scope-v2.md`.

**Fix (post-audit 2026-06-01, tightened same-day):** closed across all three
concerns the reporter raised.
- **Over-capture / promotion criteria** (v1.6.2 salience gate). A five-tier
  salience rating (`trivial` < `scene` < `notable` < `significant` <
  `defining`) is added to the detection prompt; `threatSalienceThreshold`
  (default `significant`) gates what reaches the durable Threats record.
  Below-floor items are routed to the running Session Log via
  `appendSessionLogBeat({ kind: "threat", title, text })`
  (`src/entities/entityExtractor.js:629-631`), preserving their text rather
  than dropping them. The salience floor *is* the "world threat vs scene
  beat" criterion the reporter asked for. Severity escalation of already-
  recorded threats (looming → active → resolved) is handled separately by
  `applyStateTransition` (`src/world/worldJournal.js:418-426`,
  `updateThreatSeverity`), wired into the detection pass at
  `routeWorldJournalResults:648-650`.
- **Empty body** closed via T3 (see Theme T3 below). `recordThreat`
  builds `data.summary = entry?.summary ?? existing?.summary ?? ""`
  (`worldJournal.js:242`); `upsertPage` writes the page body via
  `renderPageBody(FLAG_KEYS.threats, data)` (L933-934), which renders
  `Severity: …` + `<p>${data.summary}</p>`. The dedicated regression
  test `tests/unit/worldJournal.test.js:259` ("threat page body
  carries severity + summary") pins it.

---

## F16 — Foundry's "Starforged tools" group hides the Companion tools (no way back) · 🟢 fixed (v1.7.0 + v1.7.1)

**Environment:** far-left scene controls. Clicking the (foundry-ironsworn system)
**"Starforged tools"** control group makes the **Starforged Companion** buttons
disappear, and they're "not recoverable."

**Cause:** the Companion injects its buttons into the **`tokens`** control group
(`controls.tokens.tools.*` in `getSceneControlButtons`, `src/index.js`), not its own
top-level group. Selecting any other group (the system's "Starforged tools", journal,
etc.) deactivates the tokens group and hides our tools, with no labeled way back.

**Reporter's fix (agreed direction):** give the Companion its **own top-level
scene-control group** (e.g. `controls.starforgedCompanion = { name, title, icon,
tools }`) so the buttons live in a dedicated, always-reachable category instead of
riding inside `tokens`. Code: `src/index.js` getSceneControlButtons / renderSceneControls.

**Fix (post-audit 2026-06-01):** closed in v1.7.0 (Companion top-level
control group with its own `name` / `title` / `icon` / `tools`) and refined
in v1.7.1 — the group needed `activeTool: "clocks"` (a non-GM-gated,
no-op-onChange tool) because v13's `SceneControls.activate()` →
`#preActivate` → `#onToolChange` → `#onChange` chain dereferences
`group.tools[group.activeTool].onChange` regardless of whether any tool has
a meaningful active state. Without the pin, group transitions in either
direction threw `Cannot read properties of undefined (reading 'onChange')`.
A new Quench assertion in the `toolbar` batch pins that `group.activeTool`
resolves to a real, non-throwing, non-GM-gated tool so a future schema
change can't silently reintroduce the bug.

---

## F17 — World Journal Lore: over-captures scene minutiae + empty bodies · 🟢 fixed

**Environment:** World Journal — Lore. Entries include "Oakes: Ship's medic",
"Chen: Crew member…", "Rourke: Crew member…", "Cargo Manifest Discrepancy",
"Container C-47 Scorch Marks", "Unmarked Cases in Cargo Bay", "Cargo Bay External
Airlock Activity", "Wartime Munitions in Cargo Manifest", "External Airlock Cycling
Sequence Initiated".

**Observed:** Lore is **too in-the-minutia** — nearly every small scene detail becomes
its own entry — and the selected "External Airlock Cycling Sequence Initiated" page has
an **empty body** (same empty-entry symptom as F15). The crew NPCs Oakes/Chen/Rourke —
invented by the galley vignette (F9) — have also been promoted to Lore.

**Code:** `src/entities/entityExtractor.js` (detection granularity / Lore routing),
`src/world/worldJournal.js` (entry body population).

**Fix (post-audit 2026-06-01, tightened same-day):** closed.
- **Over-capture** closed by the salience gate + Session Log routing
  (v1.6.2). Below-floor lore items append to the running Session Log
  page per session via `appendSessionLogBeat({ kind: "lore", title,
  text: lore.text ?? "" })` (`src/entities/entityExtractor.js:615-617`)
  rather than spawning a fresh Lore page each. Scene beats like
  "Container C-47 Scorch Marks" route to the Session Log; durable
  world facts (`significant` and above) still land in Lore. Default
  floor is `significant`; the gate fails open (an item whose salience
  is absent or unrecognised is recorded rather than silently dropped)
  so model format drift degrades to the pre-salience capture-all
  baseline.
- **Empty body** closed via T3. `recordLoreDiscovery` builds
  `data.text = entry?.text ?? ""` (`worldJournal.js:128`); `upsertPage`
  writes the page body via `renderPageBody(FLAG_KEYS.lore, data)`
  (L927-932), which renders `<p>${data.text || data.fact}</p>` plus a
  "Confirmed" / "Pending confirmation" tag. The regression test
  `tests/unit/worldJournal.test.js:248` ("lore page body carries the
  discovery text") pins it.

---

## Theme T2 — World Journal auto-detect: granularity + empty bodies · 🟢 fixed

**F15** (Threats) and **F17** (Lore) share a root: the combined detection pass is
**over-eager** (transient scene beats become permanent WJ entries) and the created
entries have **empty / near-empty bodies**. Candidate direction: raise the salience
threshold for what's recorded, distinguish scene-transient from campaign-durable, and
write entries with a real summary/body (plus a severity for threats). Lives with the
World Journal + entity-discovery scopes.

**Fix (post-audit 2026-06-01):** both halves closed exactly along the
candidate direction.
- **Granularity** — the v1.6.2 five-tier salience gate with per-channel
  thresholds (`loreSalienceThreshold`, `threatSalienceThreshold`,
  `chronicleSalienceThreshold`) defaulting to `significant` distinguishes
  scene-transient from campaign-durable. Below-floor items route to the
  running Session Log via `appendSessionLogBeat` rather than spawning a
  WJ page. Fail-open semantics preserve unrated items so a model format
  drift degrades to the pre-salience baseline rather than silently
  dropping content.
- **Empty bodies** — closed at the `upsertPage` layer (see Theme T3
  below for the full trail), with a dedicated regression block at
  `tests/unit/worldJournal.test.js:242-291` pinning one body-write
  assertion per WJ type.

---

## F18 — Session Log blank; Lore is holding what should be the running Session Log · 🟢 fixed (v1.6.2)

**Environment:** World Journal — Session Log (zero pages, **blank**) vs World Journal —
Lore (9 scene-beat pages, F17).

**Observed:**
- The **Session Log is completely empty** despite `sessionLogAutoWrite` defaulting on —
  nothing was written across an active session of play.
- **Reporter's key insight:** the Lore entries *"look like things that could have been
  a running session log, where each new page is really just an appendment to the
  existing scene."* The F17 scene beats (Cargo Manifest Discrepancy, Scorch Marks,
  Unmarked Cases, Airlock Activity, …) are **session-log material**, mis-routed to
  **Lore as separate pages**, while the actual Session Log gets nothing.

**Direction (refines T2):** route transient scene beats into **one running Session Log
entry** (append, don't spawn a page each), reserve **Lore** for durable world facts,
and make `sessionLogAutoWrite` actually write. A single routing change could clear
**F15 / F17 / F18** together.

**Code:** `src/world/worldJournal.js` (session-log + lore write paths),
`src/entities/entityExtractor.js` (routing: scene-beat → session-log vs lore),
`sessionLogAutoWrite` wiring in `src/index.js`.

**Fix (post-audit 2026-06-01):** closed in v1.6.2. The direction lands as
described: below-floor (or fail-open) lore and threat beats now append to
one running Session Log page per session via `appendSessionLogBeat`
(`src/world/worldJournal.js`) — a live, scene-by-scene log. `writeSessionLog`
was refactored to fill the End-Session summary as a section on that *same*
running page (one page per session, matched by `sessionId`) rather than
spawning a fresh page each End Session, so the wrap-up and the live scene
log coexist on one entry. Above-floor lore and threats still land in
Lore/Threats; chronicle below-floor beats are still skipped (Chronicle is
the PC's personal record, not a scene log).

---

## F19 — Confirmed entity (Connection "Vance") journal page is blank — drops the chat description · 🟢 fixed

**Environment:** the combined detection pass proposed **[Connection] Vance — "Crew
member with a characteristic hitch in his stance; observant and detail-oriented,
notices discrepancies in cargo manifests."** The GM **Confirmed** it ("Magnus Hobbs
Added Vance").

**Observed:** the created record — *Starforged Entities: Vance → "Connection Data"*
page — has a **title but an empty body**. The descriptive flavor that **appeared in the
draft chat card is not written into the entity's journal page** on confirm.

**Code:** the entity Confirm path — `src/entities/entityExtractor.js`
(`routeEntityDrafts` / Confirm handler) and `src/entities/connection.js`
(`createConnection`, page-body authoring). The draft's description should be persisted
into the Connection Data page.

**Fix (post-audit 2026-06-01, tightened same-day):** closed.
`handleDraftConfirm` (`src/entities/entityExtractor.js:1204+`) constructs
`seedData` via `buildConnectionSeedData({ name, description: draft.description ?? "" }, ...)`
and passes `description: seedData.description` into `createConnection`.
`renderEntityBody` in `src/entities/connection.js:484` writes
`<p>${esc(connection.description)}</p>` into the page body when present, so
the draft's description reaches the Connection Data page. Theme T3 (the
broader "draft.description not written to page body" defect) is closed
at the `upsertPage` layer for all WJ types — see Theme T3 below for the
full trail and `tests/unit/worldJournal.test.js:242-291` for per-type
regression coverage. The entity-side else branch of `handleDraftConfirm`
(L1247-1252) passes `{ name, description }` to settlement / faction /
planet / location / creature creators, so the description reaches the
creator on every entity type.

---

## Theme T3 — Created records have empty bodies: the generated description isn't persisted · 🟢 fixed

The **empty-body** symptom spans the whole records layer, not one journal:
**F15** (Threats), **F17** (Lore), **F18** (Session Log), and now **F19** (entity
Connection). In each case a **title is created but the descriptive text — which the LLM
generated and often already displayed in chat — is not written into the record body.**

Distinct from **T2** (which is about *what* gets recorded and *where* it routes); **T3**
is about the body being empty even when the content existed. A shared fix — "persist the
generated/draft description into the created page body" — likely clears the empty-body
half of F15, F17, F18, and F19 at once.

**Refinement (F21):** the WJ *Pending Lore* drafts visibly carry full description bodies,
yet the **confirmed** page is empty — so T3 is specifically a **Confirm/persist drop**
(`draft.description` not written to the page body on confirm), **not** a generation gap.
That narrows the fix to the confirm → create-page path.

**Fix (post-audit 2026-06-01):** closed across the whole records layer.
The shared fix the theme predicted ("persist the generated/draft
description into the created page body") landed at the `upsertPage`
layer rather than per-recorder:

- `src/world/worldJournal.js:882-908` — `upsertPage` writes `data` into
  both the page flag *and* the page body on both the create branch
  (`createEmbeddedDocuments` with `text: { format: 1, content }`) and
  the update branch (`existing.update({ text: { format: 1, content } })`).
  The inline comment at L887-888 names F15/F17/F19/F21 directly:
  *"Keep the visible page body in sync with the flag data (F15/F17/F19/F21:
  the description lived only in the flag, so the page rendered blank)."*
- `src/world/worldJournal.js:920-943` — `renderPageBody` is the per-type
  body builder: lore renders `data.text || data.fact`; threat renders
  `Severity:` + `data.summary`; faction renders `Attitude:` +
  `data.knownGoal`; location renders `Type:` + `Status:` +
  `data.description`. Confirmation/pending tags are appended for lore.
- The routing layer (`src/entities/entityExtractor.js:605-651`,
  `routeWorldJournalResults`) hands the full draft object through to
  each recorder, so anything the detector populated (text / summary /
  description / knownGoal) reaches `data` and then `renderPageBody`.
- The Pending-Lore Confirm path (F21's exact surface) is not a
  separate body-write — `promoteLoreToConfirmed`
  (`src/world/worldJournal.js:384`) just flips `confirmed: true` and
  stamps `promotedAt` on the page that `recordLoreDiscovery` already
  created at capture-time with a non-empty body. No body re-write is
  needed.
- The entity-side `handleDraftConfirm`
  (`src/entities/entityExtractor.js:1204+`) has explicit branches that
  carry `draft.description` into `createConnection` and `createShip`
  via seed builders, plus an else branch (L1247-1252) that passes
  `{ name, description }` to settlement / faction / planet / location /
  creature creators.

Test coverage: `tests/unit/worldJournal.test.js:242-291` carries a
dedicated `describe("page body content (T3)", …)` block whose
docstring names the theme — one assertion per WJ type (lore, threat,
faction, location) plus an update-branch test confirming the body
stays in sync when an existing entry changes; plus L508
"writes a non-empty page body".

---

## F20 — Chronicle auto-entry: are the captured details "significant"? · 🟢 fixed (v1.6.2)

**Environment:** Character Chronicle panel for the PC (Magnus Hobbs). Auto-captured
entries, all tagged **"Revelation"** (5/30/2026):
- "Someone is cycling through the airlock sequence."
- "The medical supplies were a front; the container holds military munitions from the war."
- "The unmarked cases weren't medical supplies—their serial numbers had been lasered off
  and crudely re-etched."

**Positive:** the Chronicle UI itself is good — per-note **type dropdown** (Revelation,
…), pin, delete, **+ Add Note**. And unlike the WJ / entity records (T3), these entries
**have real bodies** (text is present).

**Observation (reporter):** *"not sure yet that it is capturing 'significant' detail."*
The captured beats are the **same transient scene minutiae** flooding Lore in **F17** —
moment-to-moment encounter details rather than campaign-significant turns. Same
**granularity / salience** concern as **T2**, now in the **Chronicle** subsystem (which
is separate from the World Journal — so T2's scope is broader than just WJ).

**Code:** `src/character/chronicleWriter.js` (what it deems chronicle-worthy + the
Revelation classification), `chronicleAutoEntry` / `chronicleContextCount` settings,
`src/character/chronicle.js` (storage), `src/character/chroniclePanel.js` (UI).

**Fix (post-audit 2026-06-01):** closed in v1.6.2. The chronicle writer
now rates every beat on the same five-tier salience scale, and a new
`chronicleSalienceThreshold` world setting (default `significant`) gates
what reaches the Chronicle. Below-floor beats are simply not written
(unlike the WJ side, the Chronicle has no per-session running log — it's
the PC's personal record, not a scene log).

---

## F21 — World Journal UI (Pending Lore) over-captures minutiae — and pinpoints the T3 root cause · 🟢 fixed

**Environment:** World Journal panel → **PENDING LORE** queue. Drafts awaiting
Confirm/Dismiss (each shown **with a full description body**):
- "External Airlock Cycling Sequence Initiated" — *"Someone is actively cycling through
  the external airlock sequence to open it, moving closer and more deliberately than
  previous sensor conta…"*
- "Wartime Munitions in Cargo Manifest" — *"The tampered cargo includes wartime
  munitions…"*
- "Cargo Bay External Airlock Activity" — *"Proximity sensor in cargo bay registered
  movement…"*
- "Unmarked Cases in Cargo Bay" — *"Rows of sleek, unmarked cases… serial numbers lasered
  off…"*
- "Container C-47 Scorch Marks" — *"Container C-47 bears faint scorch marks…"*

**Granularity (reporter):** *"the world journal UI is also focusing on minutia"* —
reinforces **T2 / F17 / F20**: the same moment-to-moment scene beats are queued as
pending Lore.

**Key diagnostic — pinpoints T3:** the **pending drafts DO carry full description
bodies** here (and in chat), yet the **confirmed** Lore page for the same item
("External Airlock Cycling Sequence Initiated") is **empty** (F17). So the description is
**present at draft time and lost on Confirm** → the empty-body defect (T3) is in the
**Confirm → create-page persist step** (`draft.description` not written to the page body),
**not** in generation.

**Code:** the WJ Confirm path — `src/world/worldJournalPanel.js` (Confirm action) →
`src/world/worldJournal.js` (page creation) and/or `src/entities/entityExtractor.js`
(`routeWorldJournalResults`): carry the draft's summary/description into the created
page body.

**Fix (post-audit 2026-06-01, tightened same-day):** closed.
- **Over-capture** closed by the v1.6.2 salience gate + Session Log
  routing — below-floor drafts route to `appendSessionLogBeat` rather
  than queueing as pending Lore.
- **T3 confirm-drop** closed. The reporter's diagnosis ("description
  is present at draft time and lost on Confirm") was almost right but
  one layer off: the description is written into the page body at
  *capture* time by `recordLoreDiscovery` → `upsertPage` →
  `renderPageBody` (the same path used for the Pending preview), and
  the Confirm action (`promoteLoreToConfirmed`,
  `src/world/worldJournal.js:384-405`) just flips
  `confirmed: true` + stamps `promotedAt` on the existing page — it
  does not (and does not need to) rewrite the body. The body the
  user saw blank for "External Airlock Cycling Sequence Initiated"
  was a pre-T3 artefact: the page existed because lore was captured,
  but the body was empty because the description only landed on the
  flag. T3's `upsertPage` fix (`worldJournal.js:887-893`, comment
  names F15/F17/F19/F21 directly) means new captures write a non-
  empty body, so the symptom no longer surfaces on Confirm.

---

## F22 — End Session vignette: lands well (real ship name + real NPC); only the `<npc>` leak is a bug · 🟢 (F22a) fixed

**Environment:** End Session → "Closing — Evander Sato" vignette.

**Positives:** the scene landed well (a wry, mundane NPC beat — Evander Sato eating a
ration pack in his courier ship's galley). Notably it **used the real ship name
*Kobayashi IV*** correctly ("…three berths down from where the *Kobayashi IV* was docked
six hours ago"). **Refines F9b:** the **end** vignette *has* ship context — the
missing-ship-name gap is specific to the **begin/galley** vignette path.

**F22a — `<npc>` tags leak (confirms F9a for the END card).** The closing shows
`<npc>"Paranoid,"</npc>` literally — confirming the `stripMarkup` gap also affects
`src/session/endSessionVignette.js:169`, exactly as F9a predicted.

**F22b — featured NPC is pre-existing, not invented (✅ working as intended).**
Initially flagged because the reporter didn't recognize "Evander Sato" — but the
**name + entity are pre-existing** (likely created during **sector creation**; possibly
a faction figurehead / future "big bad"). So `selectEndSessionNPC` correctly grounded
the vignette in a **real established NPC**; the PC simply hasn't met him yet. **Reporter:
"it worked well… not requesting a change."** This corroborates that the end-vignette NPC
selection grounds in real entities (counter-evidence to **T1** for this path).

**Code (remaining bug — F22a only):** `src/session/endSessionVignette.js:169` needs
`stripMarkup` (or styled `<npc>` spans) before posting the card. The NPC-selection logic
is behaving correctly.

**Fix (post-audit 2026-06-01):** F22a closed.
`src/session/endSessionVignette.js:170` now wraps the text in
`stripMarkup(text)` before rendering — the same fix as F9a for the begin
vignette. F22b was always not-a-bug. F22 is closed.
