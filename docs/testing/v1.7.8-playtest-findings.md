# v1.7.8 Playtest Findings

A running log of observations from the v1.7.8 playtest (2026-06-10), kept for
end-of-session triage. **Recording only — do not action the open items from this
file without explicit direction.** Triage and ordering happen at session recap.

Status key: 🔴 open (to diagnose) · 🟡 needs decision / enhancement · 🟢 fixed/resolved · ✅ positive.

---

## F1 — Inciting Incident lands well · ✅ positive

**Environment:** v1.7.8, Session panel ✦ Envision Inciting Incident (first live use).

**Observed:** the card delivered exactly what the scope intended — spark
**Lose / Relationship** interpreted into an opening grounded in established
campaign context: the PC's estranged mentor **Vance** (connection), a distress
beacon in **Lyra's orbital graveyard**, the PC's ship (**Kobayashi 7**) sensing
his failing life support, a debris-field obstacle, and an implied hidden motive.
The card closed with a parsed suggested vow line: *"I will reach Vance before
his life support fails (dangerous)"*.

**Reporter:** *"the inciting incident is working great!"*

---

## F2 — Suggested vow should be a clickable button that creates the vow on the character · 🟡 enhancement

**Reporter:** *"the recommended vow should be a clickable button that will
create that vow in your character card."*

**Desired:** instead of (or alongside) the plain *Suggested vow:* text line on
the inciting-incident card, a button that creates that vow — statement as the
name, suggested rank — as a progress item on the player character, visible on
the character sheet's Progress tab.

**Feasibility (anchored, not yet actioned):** mostly wiring; the hard parts exist.

- The vow is already parsed structurally: `splitSuggestedVow()`
  (`src/session/incitingIncident.js:62`) yields `{ statement, rank }`.
- Creation helper already exists: `createCharacterVowItem(actor, { name, rank,
  vowId })` (`src/character/actorBridge.js:455`) — creates the
  `type: "progress"` / `subtype: "vow"` Item with rank validation (defaults
  `dangerous`) and flag-keyed idempotency.
- Button wiring must use `onChatMessageRender` (`src/system/chatHooks.js`) —
  the V13-002 dual-hook helper, never a raw `renderChatMessage` hook.
- Resolve the target PC via `getPlayerActors()` (FOLDER-002: excludes NPC
  cards, which are also `character`-type Actors).
- Store the parsed `{ statement, rank }` in the chat-message flags at create
  time so the click handler reads flags rather than re-parsing card HTML; the
  message id can serve as `vowId` for free idempotency (re-clicks no-op).
- Plus the usual: unit tests for card render + flags, Quench coverage,
  `packs/help.json` + `CHANGELOG.md` updates.

**Estimate:** small-to-medium.

---

## F3 — Inciting incident introduces the vow-target NPC but never instantiates them · 🔴 open

**Reporter:** *"the action is good but the followthrough is needed — the
inciting incident introduces an NPC to make a vow to, but doesn't instantiate
them in the entities ui or the actors folder or the world journal."*

**Observed:** the inciting incident invented **Vance** (estranged mentor, the
suggested vow's target) — but after the card posted:
- Entities panel: Connections shows only the pre-existing **Amelia Gray**.
- Actors sidebar: `Sectors / Igneous Spine / NPCs` contains only Amelia Gray.
- World Journal: *"No entries awaiting review"* — no pending lore, no draft.

Vance exists only in the card prose. Notably the narrator **did** ground in
established entities (real ship *Kobayashi 7*, established settlement *Lyra*)
— inventing the mentor was appropriate; the gap is purely follow-through.

**Anchor (verified in source):** `runIncitingIncident`
(`src/session/incitingIncident.js:134`) posts the card directly with **no
detection pass**. `runCombinedDetectionPass` (`src/entities/entityExtractor.js:132`)
is wired into only two paths: the move pipeline (`runPostNarrationPasses` →
`runDiscoveryDetection`, `src/narration/narrator.js:346`) and paced narration
(`runPacedDetection`, `narrator.js:1165`). The `inciting_incident` narrator
mode — like the session-vignette mode — never runs it, so nothing the opening
fiction introduces can reach the draft-Confirm flow.

**Options for triage (not decided):**
- **(a)** Run the standard combined detection pass on the inciting prose —
  Vance arrives as a pending draft; GM Confirms → NPC card (sector NPCs
  folder), Entities panel row, description persisted. Matches the module's
  GM-confirms-entities philosophy.
- **(b)** Treat the vow-target NPC as first-class: the inciting incident
  *knows* it proposed a vow to a person — auto-create (or one-click-create)
  the connection NPC card, the way `make_a_connection` / the sector wizard do.
  Could fold into **F2**'s button: swearing the suggested vow also
  instantiates its target NPC.

**Confirmed during play (later in session):** once Vance appeared in *paced*
narration (the hail exchange), the paced-path detection fired correctly —
"New Entities Detected" proposed **[Connection] Vance** and **[Ship] Unknown
contact** with Confirm/Dismiss. This pins the F3 diagnosis: detection works;
the inciting-incident path alone skips it. **Quality asymmetry:** the
paced-path draft describes Vance from the current beat only (*"ragged,
annoyed voice responding to tight-beam hail"*) — the rich inciting framing
(estranged mentor, the Sepulcher-job falling-out, failing life support) is
not in the draft. Instantiating at inciting time (option b) captures the
backstory; instantiating later captures whatever thin slice the current
scene shows. Reinforces option (b), and see **F7** for where that backstory
goes meanwhile (nowhere).

---

## F4 — Time-dependent suggested vows should carry a clock · 🟡 enhancement

**Reporter** (after manually creating the suggested vow on the PC): *"this
incident, autopopulated as a 'before his life support fails', feels like it
might benefit from also having a clock. Need at least a quick pass to see if
there is a time dependence there."*

**Observed:** the suggested vow statement carries explicit time pressure
(*"…before his life support fails"*), and the foundry-ironsworn vow sheet
natively supports exactly this — a **Clock** checkbox right on the progress
item (visible in the reporter's screenshot).

**Anchor (verified in vendor source at the pinned commit):**
- Progress items carry `system.hasClock` / `system.clockTicks` /
  `system.clockMax`; the sheet offers 4/6/8/10/12 segments
  (`vendor/foundry-ironsworn/src/module/vue/progress-sheet.vue:86–119`).
  A vow created by F2's button could arrive with its clock pre-attached —
  `createCharacterVowItem` would just need to accept the extra fields.
- Module-side alternative/complement: tension clocks
  (`src/clocks/clocks.js`, `createClock`) advance against Ask-the-Oracle
  odds — mechanically a good model for "life support failing" — but they
  live in the Clocks panel, not on the character sheet.

**Detection options for "is there time dependence" (not decided):**
- **(a) lead:** the narrator emits it structurally — extend the
  `inciting_incident` output contract
  (`src/narration/narratorPrompt.js:676`, currently `Suggested vow:
  <statement> (<rank>)`) with an optional `Suggested clock: <label>
  (<segments> segments)` line, emitted only when the incident has explicit
  time pressure. The same generation that wrote the deadline already knows
  it's there — zero extra API calls; parse alongside `splitSuggestedVow`.
- **(b)** heuristic regex on the vow statement ("before / until / within…")
  — cheap but brittle.
- **(c)** a Haiku classification call — overkill for one line given (a).

**Relation:** F2 / F3 / F4 are converging into one **inciting-incident
follow-through cluster**: the suggested-vow button (F2) creates the vow,
pre-attaches a clock when the vow is time-dependent (F4), and instantiates
the vow-target NPC (F3).

---

## F5 — Ship positioning: narrator says Lyra, token sits at Sepulcher · 🔴 open · audited

**Reporter:** *"it is clear that the ship positioning feature isn't working.
Though the ship sits near Sepulcher, the narrator believes the ship is near
Lyra. It should have either moved the Kobayashi 7 to near Lyra, or reported
the position as Sepulcher. Need to audit that feature set and see why it
isn't working."*

**Observed:** sector-scene token for *Kobayashi 7* sits beside Sepulcher's
pin; `@scene where am I in this sector` answers *"threading the outer edge
of Lyra's graveyard… Lyra settlement itself sits another eighteen thousand
klicks spinward"* — consistent with the travel fiction, contradicting the map.

**Audit result (source-verified, fact-continuity §20):** the feature is
implemented per scope and enabled by default — the mismatch falls into two
designed-in gaps, not a broken trigger.

What shipped and works:
- All four §20.4 position **writers** exist: `!at` (`src/index.js:2300`),
  chat-typed `set_a_course` non-miss via `interpretation.moveTarget`
  (`src/index.js:865–876`; `moveTarget` confirmed in
  `src/moves/interpreter.js:152,367`), narrator sidecar
  `{subject:"ship", attribute:"position"}` (`src/narration/narrator.js:1803`),
  and token drag → synthetic `set_a_course`
  (`src/sectors/sectorSceneHooks.js:99`).
- Settings all default **on** (`src/ui/settingsPanel.js:647–662`).
- The SHIP POSITION line is built into the ledger block
  (`src/narration/narratorPrompt.js:186`), confirmed-lore tier, never
  dropped — so the narrator receives whatever the record says.

**Gap 1 — no position→token sync (primary).**
`moveCommandVehicleTokenToDestination` (`sectorSceneHooks.js:222`) runs
**only** when the move originated from a token drag
(`tokenDragSetCourse` payload, `src/index.js:883`). Fiction-side movement
— chat-typed `set_a_course`, narrator sidecar, `!at` — updates the
position record but **nothing moves the token**. §20.4b's snap-back
paragraph explicitly worried about "the visual lie where the Token sits at
the destination but the persistent position says otherwise" — this is the
same lie in the reverse direction, and the scope never specified the
reverse sync. Implementation matches scope; the scope has the hole.

**Gap 2 — trigger coverage: only `set_a_course` writes position.**
The interpreter emits `moveTarget` for `set_a_course`,
`undertake_an_expedition`, and `finish_an_expedition` — but only
`set_a_course` has the §20.4 post-resolution write. If this session's
travel resolved as `face_danger` (the debris run — *"you commit to the run
past the venting reactor pod"* reads exactly like one) or
`undertake_an_expedition`, **no writer fired at all**: the record still
says Sepulcher, the SHIP POSITION block said Sepulcher, and the narrator's
"Lyra" came from scene truths / chat recency overriding the stale line.

**Gap 3 — seed-name fragility (minor).** `inferShipPosition` matches
exact name or first word (`shipPosition.js:91`); a sidecar value like
*"Lyra's orbital graveyard"* misses "Lyra" (first word "lyra's") and falls
back to freeText with **no settlement id** — narration still works, but a
future token sync would have no pin to snap to.

**Live diagnostic (10 seconds, pins which gap fired this session):** in the
browser console —
`game.actors.getName("Kobayashi 7").flags["starforged-companion"].ship.position`
— `updatedBy`/`updatedAt` provenance shows the last writer (`at_command` /
`set_a_course` / `narrator_sidecar` / `scene_token`), or a stale/Sepulcher
record meaning no writer fired (Gap 2).

**Fix directions for triage (not actioned):**
- **(a)** Position→token sync: shared `syncCommandVehicleToken(position)`
  called from both write sites (`maybeUpdateShipPositionFromName` and the
  sidecar path), reusing `moveCommandVehicleTokenToDestination` + the
  Note-pin `settlementId` flags. Free-text positions (no settlement id)
  could park the token at sector centre or leave it with a "position
  uncertain" badge — needs a decision.
- **(b)** Wire `undertake_an_expedition` / `finish_an_expedition` into the
  same §20.4 post-resolution write.
- **(c)** Possessive/suffix-tolerant name matching in `inferShipPosition`
  ("Lyra's graveyard" → "Lyra").

**Settled by pacing telemetry (see F9): Gap 2 confirmed live.** The
Session 2 telemetry journal shows the entire Lyra approach was classified
**NARRATIVE** — *"I need a path through this mess"* and *"Let's pick the
fastest path. Getting there in time is crucial"* never became moves. **No
movement move resolved all session**, so the only wired §20.4 chat trigger
(`set_a_course`) provably never fired; the position record could only have
been touched by the discretionary narrator sidecar (and the token's
Sepulcher placement says it wasn't, or wrote unusable freeText). Re-ranks
the fixes: wiring more *moves* (b) helps less than expected when travel
routinely happens in plain narrative — deterministic sidecar emission of
ship position (F8 fix (b) family) or a narrative-travel detector is the
real coverage path, with (a) still required so the token follows.

---

## F6 — @scene tone: rescue target framed adversarially · 🟡 watch

**Reporter:** *"Watching here for potential narrator drift. I'm trying to
rescue someone, but the narrator's @scene functionality makes it seem
adversarial. Could be passing, let's see if it continues."*

**Observed:** `@scene where is Vance's shuttle now relative to me` →
*"…engines dark and running cold. The shuttle's position puts it between
you and the deeper salvage, close enough that you'd cross his line of
sight if you pushed further in. He hasn't pinged you, but the way his
craft is angled suggests he's watching the approaches, not the wreckage."*
Line-of-sight / being-watched language reads like an ambush setup, not a
dying man awaiting rescue.

**Two readings (undecided — that's why it's a watch item):**
- **Defensible fiction:** the inciting incident established Vance as
  *running from something* ("he was running from it more than he was
  running to anything") and hiding wounded — "watching the approaches"
  is consistent wariness toward whatever he fled, not hostility toward
  the PC. The scene truths from the incident may be legitimately driving
  this coloring.
- **Drift:** `@scene` is a factual interrogation surface; if it keeps
  escalating tension or recasts an ally as a threat (e.g. Vance acting
  against the PC), that's the narrator importing adversarial framing the
  fiction hasn't earned.

**Escalation criteria:** log follow-ups if Vance is narrated as hostile
*toward the PC*, or if @scene answers keep adding unprompted menace.
Knobs if confirmed: `@scene` system-prompt guidance (interrogation answers
describe, they don't escalate), pacing/tone dials, scene-truth phrasing
from the inciting incident.

---

## F7 — NPC contradicts the inciting incident: opening fiction has no durable home · 🔴 open · root cause traced

**Reporter:** *"Still looking at narrator continuity. The NPC doesn't
acknowledge that he sent a distress beacon, let's see how the narrator
responds when I directly challenge him on it."* (Live experiment in
progress — challenge response to be logged as a follow-up.)

**Observed:** PC hails Vance to confirm he's OK. Vance's voiced reply —
*"I'm fine, just get over here before someone else picks the bones clean"*
— is flatly inconsistent with the inciting incident: a distress beacon,
his voice *"ragged with pain"*, **failing life support**, and him hiding
from something. The narrator wrote him as a brusque salvager, not a dying
man who called for help.

**Root cause (source-verified): the inciting-incident fiction is invisible
to every subsequent narrator call.**
- `postIncitingIncidentCard` (`src/session/incitingIncident.js:119`) posts
  the card with **only** `{ incitingIncidentCard: true }` flags.
- The recent-narration ring (`getRecentNarrationContext`,
  `src/narration/narrator.js:419`) selects messages by
  `flags.narratorCard` + `flags.sessionId` and reads
  `flags.narrationText` — **none of which the inciting card carries**. So
  paced narration, `@scene`, and oracle follow-ups (its three consumers)
  never see the opening fiction.
- The other memory surfaces are equally empty at this point: Vance is an
  **unconfirmed draft** (no entity record / generative tier — F3), the
  World Journal has nothing, and scene truths hold only whatever sidecar
  the incident's own generation happened to emit (the beacon /
  life-support facts were in the prose, not guaranteed into the sidecar).

The narrator voicing Vance genuinely did not know about the beacon.
"I'm fine" is the *expected* output of this plumbing, not stochastic drift
— which also reframes part of **F6**: some of the adversarial coloring may
be the narrator improvising Vance with no backstory available.

**Blast radius — session recap too:** `postSessionRecap` builds from the
same `narratorCard` flags, so the inciting incident is also absent from
"Previously On" recaps. The campaign's opening event is the one beat a
recap must never lose.

**Fix directions for triage (not actioned):**
- **(a) Cheap, high-yield:** flag the inciting card as a narrator card —
  add `narratorCard: true`, `sessionId`, `narrationText` alongside
  `incitingIncidentCard` at post time. One-line-shaped change; puts the
  opening fiction into the recent-narration ring **and** session recaps.
- **(b)** F3's instantiation gives the backstory a durable home (entity
  record + generative tier) that survives beyond the 3-card ring.
- **(c)** Belt-and-braces: have the inciting narrator mode explicitly emit
  sidecar truths for its load-bearing facts (beacon sent, life support
  failing, estranged-mentor history) so they enter scene truths
  deterministically rather than at the model's discretion.

**Follow-up — challenge experiment confirms the diagnosis (and reveals an
escalation risk).** Player: *"Vance, what about the distress call you sent
me?"* Narrator (as Vance): *"Distress call? I sent a salvage beacon, clean
handshake protocol — you saying it came through garbled?"* — plus *"the
faint whine of a power drill cycling down"* (a man whose life support is
failing, doing salvage work mid-call), and a Gather Information hint to
*"press him on the discrepancy and see what he's hiding."*

Reading: graceful confabulation, exactly as the root cause predicts. With
no memory of the incident, the narrator picked up "distress call" from the
player's own message, invented a benign retcon, and converted its knowledge
gap into an in-fiction mystery hook. Reporter's read — *"I'm not convinced
that this isn't that it lost the thread because the inciting incident
didn't get logged"* — is correct per the F7 trace.

**Escalation risk — canon inversion.** This retcon narration runs through
the normal sidecar/scene-truth machinery, so *"Vance sent a salvage beacon,
not a distress call"* can now be persisted into `sceneTruths` — making the
**wrong** version durable while the true version still has no home. The
memory gap doesn't just lose the opening fiction; it lets later
contradictions overwrite it. Raises F7's priority: this rewrites the
campaign premise (the vow's entire stakes — *"before his life support
fails"* — are evaporating).

**In-session mitigation (designed tool, untested live this session):** use
**Correct a fact** on the narrator card (or `!truth`) to assert *"Vance
sent a distress call; his voice was ragged with pain; his life support is
failing"* into scene truths — the correction loop exists for exactly this,
and exercising it here doubles as a live test of that feature.

**Follow-up 2 — Gather Information (strong hit) reconciles the
discrepancy; consistency check fires; drift becomes a coherent fork.**
The narrator resolved the challenge with a third-party twist: *"someone
else retransmitted his signal as an SOS, which means they're close enough
to intercept — and they want you here."* Mechanically appropriate (strong
hit = clear answer), and it *reconciles* Vance's denial with the player
having received a distress call — but it still contradicts the
**experienced** opening fiction (the PC heard *Vance's own voice, ragged
with pain, calling their name* — a retransmitted salvage beacon produces
no such thing), and the life-support stakes are now fully written out:
Vance is well enough to be running a power drill. The campaign premise has
forked from "race to save a dying mentor" to "walk into a stranger's trap."

**Consistency check (Narrative Review) fired — and where it looked is the
F7 thesis in miniature.** The optional Haiku audit posted: *"The narrator
may have contradicted an established fact — Vance: Had a falling-out with
Venri three years ago over the Sepulcher job — Vance's stated reason for
the beacon (salvage operation) now questioned; his dishonesty about the
distress call suggests deeper deception."* Two lessons:
- The audit **works live** and caught the deception thread. ✅
- It audited against the **falling-out** fact — which evidently *does*
  live in a ledger (the incident's discretionary sidecar appears to have
  captured the relationship history) — and not against the beacon /
  life-support facts, which have no home. **Facts with a home get
  defended; facts without one get rewritten.** Refines fix (c): the
  incident's sidecar emission partially worked — it kept the backstory,
  dropped the stakes. Deterministic emission of the stakes is the gap.

**Reporter:** *"it is working with it, but the question is, will things
continue to drift."* Mechanism-based prediction logged: yes — and
*coherently*, not randomly. The recent-narration ring (last 3 cards) now
holds only the salvage-beacon/trap version, and this narration's own
sidecar may canonise the retransmission twist into scene truths. The
narrator will keep building on the fork because the fork is all it can
see; the original stakes cannot return on their own. Concrete divergence
already on the sheet: the sworn vow *"before his life support fails"*
references stakes that no longer exist in the narrator's working fiction.

---

## F8 — Narrator context is insufficient for scene-local continuity (generalization of F7) · 🔴 open · audited

**Reporter:** *"now Vance is going from being on a shuttle to on a station,
so I think we have enough datapoints to suggest that the context being fed
to the narrator is insufficient."*

**Observed (third drift instance, cleanest repro):** Vance's location has
morphed across the session — inciting incident: **his shuttle**, drifting in
Lyra's graveyard, life support failing → `@scene`: *"shuttle on thermal,
three klicks sunward… engines dark"* → now: a **power drill**, a **cargo
bay**, *"the cargo bay lights flicker… as if the station itself is holding
its breath."* Vance is now a healthy salvager on a station.

**Why, mechanically (source-verified):** the paced narrator's working
memory per call (`narratePacedInput`, `src/narration/narrator.js:1069–1086`)
is: system prompt (character state, current-location card, active-sector
block, ledger block, WJ injections) + the player's message + the last
**3** `narratorCard`-flagged messages. "Vance is on a shuttle" was
established in exactly two surfaces, **both invisible**:
- the inciting-incident card (F7 — wrong flags), and
- the `@scene` answer — **confirmed this session:** `postSceneCard`
  (`narrator.js:1359`) flags only `sceneResponse`/`sceneText`; the ring
  selects on `narratorCard` and reads `narrationText`, so **@scene answers
  never feed back into narrator context either.**

Meanwhile the last 3 visible cards are saturated with salvage texture
(power drill, cargo bay, picking bones clean) — so the narrator
crystallized "cargo bay" into "station." Each generation re-derives the
scene from a 3-card horizon plus whatever the discretionary sidecar
happened to ledger; nothing pins NPC location / vessel / condition.

**Fair framing:** the architecture for this **exists** — the fact-continuity
ledger (`sceneState.bySubject`) is injected on every call and supports
exactly `subject: Vance, attribute: location/condition`. The pipes are
right; the taps are starved: the sidecar instruction leaves emission to
model discretion, and two of the three fiction surfaces never reach the
ring. F6's adversarial coloring and F7's premise fork are both special
cases of this.

**Fix directions for triage (not actioned):**
- **(a) Feed the ring:** add `narratorCard`-family flags (+`narrationText`,
  `sessionId`) to the inciting card **and** `@scene` cards so all narrator
  prose shares one feed. Cheap, two small flag changes.
- **(b) Deterministic ledger for active NPCs:** strengthen the sidecar
  instruction to *require* `stateChanges` for any named NPC's location /
  vessel / physical condition when narration establishes or changes them —
  puts "Vance: aboard his shuttle, life support failing" into the
  always-injected, never-dropped ledger block. (Extends F7's fix (c).)
- **(c) Ring depth:** 3 cards is a very short horizon; consider a setting
  (e.g. 3–10) — plain-prose cost, bounded.
- **(d) Bigger (triage discussion):** a rolling compressed scene summary
  maintained across turns, instead of raw last-N cards — the durable
  answer if (a)+(b) prove insufficient.

**Relation:** F6 (tone watch) and F7 (premise fork) fold into this as
instances. Triage should treat **F7+F8 as one "narrator memory
architecture" work item** — fixes (a)/(b) here subsume F7 (a)/(c).

**Reporter's holistic framing (adopted as the finding's shape):** *"the
shuttle was also referenced in the inciting incident, but holistically
we're seeing drift of location, motivation, and context."* The session's
drift taxonomy, all from one root:

| Axis | Established (inciting + early @scene) | Drifted to (within ~30 min) |
|------|----------------------------------------|------------------------------|
| **Location** | Vance's *shuttle*, adrift in Lyra's graveyard, engines dark | a *station* with cargo bay and working power tools |
| **Motivation / character** | wounded mentor, ragged with pain, calling Venri's name for help, hiding from something | brusque self-sufficient salvager → knowing *bait* in someone's trap |
| **Context / stakes** | distress beacon; **life support failing**; race against time through a lethal debris field | routine salvage beacon retransmitted by a mystery third party; no clock, ambush premise |

Drift is not a single lost fact — it is the **default behavior of every
narrative dimension not pinned in a ledger**. Each generation re-infers
the scene from the 3-card window, and every unpinned axis regresses
toward whatever that window's texture implies. Notably the *one* pinned
fact (the Sepulcher falling-out, sidecar'd at incident time) held firm
across all of it and was even defended by the consistency check.

Sharpens fix **(b)**: deterministic sidecar emission must cover all three
axes — physical state (`stateChanges`: NPC location / vessel / condition)
**and** intent/stakes (`newTruths`: *why* the NPC is here, what fails if
the PC is late) — not location alone.

---

## F9 — Pacing: time-critical hazardous travel classified plain NARRATIVE — the journey cost nothing · 🟡 needs decision

**Source:** Pacing Telemetry journal, Session 2 (6/6 decisions captured —
the telemetry surface itself is working exactly as designed ✅; this
finding is its first real dial-tuning yield).

**Observed:** the approach to Lyra's graveyard — through a debris field
the inciting incident established as *"dense enough that one wrong move
will shred your fighter's hull"*, under explicit time pressure — ran as:

| Input | Decision | Category | Conf. |
|---|---|---|---|
| "I need a path through this mess. Nav computer, what can you do for me" | NARRATIVE | downtime | 85% |
| "Let's pick the fastest path. **Getting there in time is crucial**" | NARRATIVE | exploration | 85% |

No move fired or was offered for either. The PC crossed a lethal,
time-critical obstacle **for free** — no `set_a_course` /
`undertake_an_expedition` / `face_danger`, no supply/harm stakes, no
progress, and (F5) no position write. Textbook *"if there's a risk, there's
a move"* territory; the second input is practically a Set a Course recital.

**Contrast (working well):** the social beats were classified sensibly —
the two challenges to Vance got NARRATIVE_WITH_MOVE_AVAILABLE with
reasonable nominations (`gather_information` for pressing the discrepancy;
`compel` for the hail is more debatable), and the pure conversational
follow-ups stayed NARRATIVE at high confidence. The classifier's social
calibration looks good; **movement-with-stakes** is the gap.

**Candidate directions (not decided):**
- Classifier guidance: stated **time pressure / established hazard +
  movement intent** should bias toward `NARRATIVE_WITH_MOVE_AVAILABLE`
  nominating `set_a_course` or `face_danger`, even mid-exploration.
- Category quibble: nav-computer use during an active crisis classified
  as *downtime* — worth a category-definition pass in the classifier
  prompt (downtime should imply absence of immediate stakes).
- Interaction with the exploration pacing dial: check the dial defaults
  before blaming the prompt — this may be the dial doing what it was told.

**Relation:** directly starves F5 (the only wired chat position-trigger
is a move that pacing never routes to when travel is narrated); the
free-travel outcome also erodes the F4 time-pressure premise (a clock
nobody can fail against isn't a clock).

---

## Session triage (recap, 2026-06-10)

**Session shape:** the v1.7.8 core loop held up well — inciting incident
quality (F1), paced entity detection, the consistency-check audit, pacing
telemetry, momentum-to-correct-PC, and actor foldering all worked as
designed. The findings concentrate in two seams: **narrator memory** and
**inciting-incident follow-through**.

### Proposed work order (pending maintainer decision)

**1. Cluster A — Narrator memory architecture (F7 + F8, absorbs F6) · 🔴
highest play impact.** The campaign premise drifted out from under the
player within ~30 minutes; this is the most play-breaking class of defect
a narrator module can have, and the session produced a complete causal
trace for it.
- **A1 (small):** unified narrator-prose feed — `narratorCard` +
  `sessionId` + `narrationText` flags on the inciting-incident card and
  `@scene` cards. Also fixes the inciting incident's absence from session
  recaps. Cheapest single change with the largest continuity yield.
- **A2 (medium):** deterministic sidecar emission — require
  `stateChanges` for named-NPC location / vessel / condition and
  `newTruths` for intent/stakes when narration establishes or changes
  them; inciting mode emits its load-bearing facts explicitly. Shared
  infrastructure: also delivers the ship-position emission F5 needs.
- **A3 (small):** ring-depth setting (default 3 → configurable).
- **A4 (deferred):** rolling compressed scene summary — only if A1–A3
  prove insufficient in the next playtest.

**2. Cluster B — Inciting-incident follow-through (F2 + F3 + F4) ·
🟡 high delight, well-scoped.** One affordance on the inciting card:
**⚔ Swear this vow** — creates the vow on the PC (`createCharacterVowItem`,
exists), pre-attaches a clock when the narrator flagged time dependence
(`Suggested clock:` output line; native `hasClock`/`clockMax` fields,
verified), and instantiates the vow-target NPC with the *rich* inciting
backstory (creation helpers exist; paced-path drafts proved too thin).
Belt-and-braces: also run the combined detection pass on inciting prose.

**3. Cluster C — Ship positioning completion (F5) · 🔴 but partially
delivered by A2.** Position→token sync for fiction-side movement (reuse
`moveCommandVehicleTokenToDestination`), possessive-tolerant name matching
("Lyra's graveyard" → Lyra). Telemetry demoted wiring-more-moves; the
emission half ships with A2 — do C after A.

**4. Cluster D — Pacing movement-with-stakes (F9) · 🟡 small tuning
pass.** Check the exploration dial default first; then classifier guidance
(hazard + time pressure + movement intent → move-available nominating
`set_a_course`/`face_danger`) and a category-definition pass (downtime ≠
mid-crisis nav work). Verify against next session's telemetry.

**F6** remains a watch item — expected to resolve as a side effect of
Cluster A; re-evaluate next playtest.
